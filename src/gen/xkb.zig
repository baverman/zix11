// zig fmt: off
// This file is generated by tools/genproto.py

const std = @import("std");
const io = @import("../io.zig");
const wire = @import("../_wire.zig");
const DecodeError = @import("../_errors.zig").DecodeError;
const global_events = @import("events.zig");
const xproto = @import("xproto.zig");

pub const Const = enum(u32) {
    MaxLegalKeyCode = 255,
    PerKeyBitArraySize = 32,
    KeyNameLength = 4,
};

pub const EventType = enum(u32) {
    NewKeyboardNotify = 1,
    MapNotify = 2,
    StateNotify = 4,
    ControlsNotify = 8,
    IndicatorStateNotify = 16,
    IndicatorMapNotify = 32,
    NamesNotify = 64,
    CompatMapNotify = 128,
    BellNotify = 256,
    ActionMessage = 512,
    AccessXNotify = 1024,
    ExtensionDeviceNotify = 2048,
};

pub const NKNDetail = enum(u32) {
    Keycodes = 1,
    Geometry = 2,
    DeviceID = 4,
};

pub const AXNDetail = enum(u32) {
    SKPress = 1,
    SKAccept = 2,
    SKReject = 4,
    SKRelease = 8,
    BKAccept = 16,
    BKReject = 32,
    AXKWarning = 64,
};

pub const MapPart = enum(u32) {
    KeyTypes = 1,
    KeySyms = 2,
    ModifierMap = 4,
    ExplicitComponents = 8,
    KeyActions = 16,
    KeyBehaviors = 32,
    VirtualMods = 64,
    VirtualModMap = 128,
};

pub const SetMapFlags = enum(u32) {
    ResizeTypes = 1,
    RecomputeActions = 2,
};

pub const StatePart = enum(u32) {
    ModifierState = 1,
    ModifierBase = 2,
    ModifierLatch = 4,
    ModifierLock = 8,
    GroupState = 16,
    GroupBase = 32,
    GroupLatch = 64,
    GroupLock = 128,
    CompatState = 256,
    GrabMods = 512,
    CompatGrabMods = 1024,
    LookupMods = 2048,
    CompatLookupMods = 4096,
    PointerButtons = 8192,
};

pub const BoolCtrl = enum(u32) {
    RepeatKeys = 1,
    SlowKeys = 2,
    BounceKeys = 4,
    StickyKeys = 8,
    MouseKeys = 16,
    MouseKeysAccel = 32,
    AccessXKeys = 64,
    AccessXTimeoutMask = 128,
    AccessXFeedbackMask = 256,
    AudibleBellMask = 512,
    Overlay1Mask = 1024,
    Overlay2Mask = 2048,
    IgnoreGroupLockMask = 4096,
};

pub const Control = enum(u32) {
    GroupsWrap = 134217728,
    InternalMods = 268435456,
    IgnoreLockMods = 536870912,
    PerKeyRepeat = 1073741824,
    ControlsEnabled = 2147483648,
};

pub const AXOption = enum(u32) {
    SKPressFB = 1,
    SKAcceptFB = 2,
    FeatureFB = 4,
    SlowWarnFB = 8,
    IndicatorFB = 16,
    StickyKeysFB = 32,
    TwoKeys = 64,
    LatchToLock = 128,
    SKReleaseFB = 256,
    SKRejectFB = 512,
    BKRejectFB = 1024,
    DumbBell = 2048,
};

pub const LedClassResult = enum(u32) {
    KbdFeedbackClass = 0,
    LedFeedbackClass = 4,
};

pub const LedClass = enum(u32) {
    KbdFeedbackClass = 0,
    LedFeedbackClass = 4,
    DfltXIClass = 768,
    AllXIClasses = 1280,
};

pub const BellClassResult = enum(u32) {
    KbdFeedbackClass = 0,
    BellFeedbackClass = 5,
};

pub const BellClass = enum(u32) {
    KbdFeedbackClass = 0,
    BellFeedbackClass = 5,
    DfltXIClass = 768,
};

pub const ID = enum(u32) {
    UseCoreKbd = 256,
    UseCorePtr = 512,
    DfltXIClass = 768,
    DfltXIId = 1024,
    AllXIClass = 1280,
    AllXIId = 1536,
    XINone = 65280,
};

pub const Group = enum(u32) {
    @"1" = 0,
    @"2" = 1,
    @"3" = 2,
    @"4" = 3,
};

pub const Groups = enum(u32) {
    Any = 254,
    All = 255,
};

pub const SetOfGroup = enum(u32) {
    Group1 = 1,
    Group2 = 2,
    Group3 = 4,
    Group4 = 8,
};

pub const SetOfGroups = enum(u32) {
    Any = 128,
};

pub const GroupsWrap = enum(u32) {
    WrapIntoRange = 0,
    ClampIntoRange = 64,
    RedirectIntoRange = 128,
};

pub const VModsHigh = enum(u32) {
    @"15" = 128,
    @"14" = 64,
    @"13" = 32,
    @"12" = 16,
    @"11" = 8,
    @"10" = 4,
    @"9" = 2,
    @"8" = 1,
};

pub const VModsLow = enum(u32) {
    @"7" = 128,
    @"6" = 64,
    @"5" = 32,
    @"4" = 16,
    @"3" = 8,
    @"2" = 4,
    @"1" = 2,
    @"0" = 1,
};

pub const VMod = enum(u32) {
    @"15" = 32768,
    @"14" = 16384,
    @"13" = 8192,
    @"12" = 4096,
    @"11" = 2048,
    @"10" = 1024,
    @"9" = 512,
    @"8" = 256,
    @"7" = 128,
    @"6" = 64,
    @"5" = 32,
    @"4" = 16,
    @"3" = 8,
    @"2" = 4,
    @"1" = 2,
    @"0" = 1,
};

pub const Explicit = enum(u32) {
    VModMap = 128,
    Behavior = 64,
    AutoRepeat = 32,
    Interpret = 16,
    KeyType4 = 8,
    KeyType3 = 4,
    KeyType2 = 2,
    KeyType1 = 1,
};

pub const SymInterpretMatch = enum(u32) {
    NoneOf = 0,
    AnyOfOrNone = 1,
    AnyOf = 2,
    AllOf = 3,
    Exactly = 4,
};

pub const SymInterpMatch = enum(u32) {
    LevelOneOnly = 128,
    OpMask = 127,
};

pub const IMFlag = enum(u32) {
    NoExplicit = 128,
    NoAutomatic = 64,
    LEDDrivesKB = 32,
};

pub const IMModsWhich = enum(u32) {
    UseCompat = 16,
    UseEffective = 8,
    UseLocked = 4,
    UseLatched = 2,
    UseBase = 1,
};

pub const IMGroupsWhich = enum(u32) {
    UseCompat = 16,
    UseEffective = 8,
    UseLocked = 4,
    UseLatched = 2,
    UseBase = 1,
};

pub const CMDetail = enum(u32) {
    SymInterp = 1,
    GroupCompat = 2,
};

pub const NameDetail = enum(u32) {
    Keycodes = 1,
    Geometry = 2,
    Symbols = 4,
    PhysSymbols = 8,
    Types = 16,
    Compat = 32,
    KeyTypeNames = 64,
    KTLevelNames = 128,
    IndicatorNames = 256,
    KeyNames = 512,
    KeyAliases = 1024,
    VirtualModNames = 2048,
    GroupNames = 4096,
    RGNames = 8192,
};

pub const GBNDetail = enum(u32) {
    Types = 1,
    CompatMap = 2,
    ClientSymbols = 4,
    ServerSymbols = 8,
    IndicatorMaps = 16,
    KeyNames = 32,
    Geometry = 64,
    OtherNames = 128,
};

pub const XIFeature = enum(u32) {
    Keyboards = 1,
    ButtonActions = 2,
    IndicatorNames = 4,
    IndicatorMaps = 8,
    IndicatorState = 16,
};

pub const PerClientFlag = enum(u32) {
    DetectableAutoRepeat = 1,
    GrabsUseXKBState = 2,
    AutoResetControls = 4,
    LookupStateWhenGrabbed = 8,
    SendEventUsesXKBState = 16,
};

pub const BehaviorType = enum(u32) {
    Default = 0,
    Lock = 1,
    RadioGroup = 2,
    Overlay1 = 3,
    Overlay2 = 4,
    PermamentLock = 129,
    PermamentRadioGroup = 130,
    PermamentOverlay1 = 131,
    PermamentOverlay2 = 132,
};

pub const DoodadType = enum(u32) {
    Outline = 1,
    Solid = 2,
    Text = 3,
    Indicator = 4,
    Logo = 5,
};

pub const Error = enum(u32) {
    BadDevice = 255,
    BadClass = 254,
    BadId = 253,
};

pub const SA = enum(u32) {
    ClearLocks = 1,
    LatchToLock = 2,
    UseModMapMods = 4,

    pub const GroupAbsolute: @This() = @enumFromInt(4);
};

pub const SAType = enum(u32) {
    NoAction = 0,
    SetMods = 1,
    LatchMods = 2,
    LockMods = 3,
    SetGroup = 4,
    LatchGroup = 5,
    LockGroup = 6,
    MovePtr = 7,
    PtrBtn = 8,
    LockPtrBtn = 9,
    SetPtrDflt = 10,
    ISOLock = 11,
    Terminate = 12,
    SwitchScreen = 13,
    SetControls = 14,
    LockControls = 15,
    ActionMessage = 16,
    RedirectKey = 17,
    DeviceBtn = 18,
    LockDeviceBtn = 19,
    DeviceValuator = 20,
};

pub const SAMovePtrFlag = enum(u32) {
    NoAcceleration = 1,
    MoveAbsoluteX = 2,
    MoveAbsoluteY = 4,
};

pub const SASetPtrDfltFlag = enum(u32) {
    DfltBtnAbsolute = 4,
    AffectDfltButton = 1,
};

pub const SAIsoLockFlag = enum(u32) {
    NoLock = 1,
    NoUnlock = 2,
    UseModMapMods = 4,
    ISODfltIsGroup = 8,

    pub const GroupAbsolute: @This() = @enumFromInt(4);
};

pub const SAIsoLockNoAffect = enum(u32) {
    Ctrls = 8,
    Ptr = 16,
    Group = 32,
    Mods = 64,
};

pub const SwitchScreenFlag = enum(u32) {
    Application = 1,
    Absolute = 4,
};

pub const BoolCtrlsHigh = enum(u32) {
    AccessXFeedback = 1,
    AudibleBell = 2,
    Overlay1 = 4,
    Overlay2 = 8,
    IgnoreGroupLock = 16,
};

pub const BoolCtrlsLow = enum(u32) {
    RepeatKeys = 1,
    SlowKeys = 2,
    BounceKeys = 4,
    StickyKeys = 8,
    MouseKeys = 16,
    MouseKeysAccel = 32,
    AccessXKeys = 64,
    AccessXTimeout = 128,
};

pub const ActionMessageFlag = enum(u32) {
    OnPress = 1,
    OnRelease = 2,
    GenKeyEvent = 4,
};

pub const LockDeviceFlags = enum(u32) {
    NoLock = 1,
    NoUnlock = 2,
};

pub const SAValWhat = enum(u32) {
    IgnoreVal = 0,
    SetValMin = 1,
    SetValCenter = 2,
    SetValMax = 3,
    SetValRelative = 4,
    SetValAbsolute = 5,
};

pub const IndicatorMap = struct {
    flags: IMFlag,
    whichGroups: IMGroupsWhich,
    groups: SetOfGroup,
    whichMods: IMModsWhich,
    mods: u8,
    realMods: u8,
    vmods: u16,
    ctrls: u32,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeByte(@intCast(@intFromEnum(self.flags)));
        writer.writeByte(@intCast(@intFromEnum(self.whichGroups)));
        writer.writeByte(@intCast(@intFromEnum(self.groups)));
        writer.writeByte(@intCast(@intFromEnum(self.whichMods)));
        writer.writeByte(self.mods);
        writer.writeByte(self.realMods);
        writer.writeInt(u16, self.vmods);
        writer.writeInt(u32, self.ctrls);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.flags = @as(IMFlag, @enumFromInt(try reader.takeByte()));
        result.whichGroups = @as(IMGroupsWhich, @enumFromInt(try reader.takeByte()));
        result.groups = @as(SetOfGroup, @enumFromInt(try reader.takeByte()));
        result.whichMods = @as(IMModsWhich, @enumFromInt(try reader.takeByte()));
        result.mods = try reader.takeByte();
        result.realMods = try reader.takeByte();
        result.vmods = try reader.takeInt(u16, .native);
        result.ctrls = try reader.takeInt(u32, .native);
        return result;
    }
};

pub const ModDef = struct {
    mask: u8,
    realMods: u8,
    vmods: u16,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeByte(self.mask);
        writer.writeByte(self.realMods);
        writer.writeInt(u16, self.vmods);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.mask = try reader.takeByte();
        result.realMods = try reader.takeByte();
        result.vmods = try reader.takeInt(u16, .native);
        return result;
    }
};

pub const KeyName = struct {
    name: [4]u8,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.write(self.name[0..]);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        for (&result.name) |*elem| {
            elem.* = try reader.takeByte();
        }
        return result;
    }
};

pub const KeyAlias = struct {
    real: [4]u8,
    alias: [4]u8,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.write(self.real[0..]);
        writer.write(self.alias[0..]);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        for (&result.real) |*elem| {
            elem.* = try reader.takeByte();
        }
        for (&result.alias) |*elem| {
            elem.* = try reader.takeByte();
        }
        return result;
    }
};

pub const CountedString16 = struct {
    string: []const u8,
    decoded_string_buf: ?[]u8 = null,
    alignment_pad: []const u8,
    decoded_alignment_pad_buf: ?[]u8 = null,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u16, @intCast(self.string.len));
        writer.write(self.string);
        writer.write(self.alignment_pad);
    }

    pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        const length = try reader.takeInt(u16, .native);
        const decoded_string_buf = try allocator.dupe(u8, try reader.take(@intCast(length)));
        result.string = decoded_string_buf;
        result.decoded_string_buf = decoded_string_buf;
        const decoded_alignment_pad_buf = try allocator.dupe(u8, try reader.take(@intCast((((result.length + 5) & (~3)) - (result.length + 2)))));
        result.alignment_pad = decoded_alignment_pad_buf;
        result.decoded_alignment_pad_buf = decoded_alignment_pad_buf;
        return result;
    }

    pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
        if (self.decoded_string_buf) |buf| {
            allocator.free(buf);
            self.decoded_string_buf = null;
            self.string = &.{};
        }
        if (self.decoded_alignment_pad_buf) |buf| {
            allocator.free(buf);
            self.decoded_alignment_pad_buf = null;
            self.alignment_pad = &.{};
        }
    }
};

pub const KTMapEntry = struct {
    active: bool,
    mods_mask: u8,
    level: u8,
    mods_mods: u8,
    mods_vmods: u16,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeByte(@intFromBool(self.active));
        writer.writeByte(self.mods_mask);
        writer.writeByte(self.level);
        writer.writeByte(self.mods_mods);
        writer.writeInt(u16, self.mods_vmods);
        writer.splatByte(0, 2);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.active = (try reader.takeByte()) != 0;
        result.mods_mask = try reader.takeByte();
        result.level = try reader.takeByte();
        result.mods_mods = try reader.takeByte();
        result.mods_vmods = try reader.takeInt(u16, .native);
        _ = try reader.take(2);
        return result;
    }
};

pub const KeyType = struct {
    mods_mask: u8,
    mods_mods: u8,
    mods_vmods: u16,
    numLevels: u8,
    hasPreserve: bool,
    map: []const KTMapEntry,
    decoded_map_buf: ?[]KTMapEntry = null,
    preserve: []const ModDef,
    decoded_preserve_buf: ?[]ModDef = null,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeByte(self.mods_mask);
        writer.writeByte(self.mods_mods);
        writer.writeInt(u16, self.mods_vmods);
        writer.writeByte(self.numLevels);
        writer.writeByte(@intCast(self.map.len));
        writer.writeByte(@intFromBool(self.hasPreserve));
        writer.splatByte(0, 1);
        for (self.map) |elem| {
            try elem.encode(writer);
        }
        for (self.preserve) |elem| {
            try elem.encode(writer);
        }
    }

    pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.mods_mask = try reader.takeByte();
        result.mods_mods = try reader.takeByte();
        result.mods_vmods = try reader.takeInt(u16, .native);
        result.numLevels = try reader.takeByte();
        const nMapEntries = try reader.takeByte();
        result.hasPreserve = (try reader.takeByte()) != 0;
        _ = try reader.take(1);
        const decoded_map_buf = try allocator.alloc(KTMapEntry, @intCast(nMapEntries));
        for (decoded_map_buf) |*elem| {
            elem.* = try KTMapEntry.decode(reader);
        }
        result.map = decoded_map_buf;
        result.decoded_map_buf = decoded_map_buf;
        const decoded_preserve_buf = try allocator.alloc(ModDef, @intCast((result.hasPreserve * result.nMapEntries)));
        for (decoded_preserve_buf) |*elem| {
            elem.* = try ModDef.decode(reader);
        }
        result.preserve = decoded_preserve_buf;
        result.decoded_preserve_buf = decoded_preserve_buf;
        return result;
    }

    pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
        if (self.decoded_map_buf) |buf| {
            allocator.free(buf);
            self.decoded_map_buf = null;
            self.map = &.{};
        }
        if (self.decoded_preserve_buf) |buf| {
            allocator.free(buf);
            self.decoded_preserve_buf = null;
            self.preserve = &.{};
        }
    }
};

pub const KeySymMap = struct {
    kt_index: [4]u8,
    groupInfo: u8,
    width: u8,
    syms: []const u32,
    decoded_syms_buf: ?[]u32 = null,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.write(self.kt_index[0..]);
        writer.writeByte(self.groupInfo);
        writer.writeByte(self.width);
        writer.writeInt(u16, @intCast(self.syms.len));
        for (self.syms) |elem| {
            writer.writeInt(u32, elem);
        }
    }

    pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        for (&result.kt_index) |*elem| {
            elem.* = try reader.takeByte();
        }
        result.groupInfo = try reader.takeByte();
        result.width = try reader.takeByte();
        const nSyms = try reader.takeInt(u16, .native);
        const decoded_syms_buf = try allocator.alloc(u32, @intCast(nSyms));
        for (decoded_syms_buf) |*elem| {
            elem.* = try reader.takeInt(u32, .native);
        }
        result.syms = decoded_syms_buf;
        result.decoded_syms_buf = decoded_syms_buf;
        return result;
    }

    pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
        if (self.decoded_syms_buf) |buf| {
            allocator.free(buf);
            self.decoded_syms_buf = null;
            self.syms = &.{};
        }
    }
};

pub const CommonBehavior = struct {
    type: u8,
    data: u8,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeByte(self.type);
        writer.writeByte(self.data);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.type = try reader.takeByte();
        result.data = try reader.takeByte();
        return result;
    }
};

pub const DefaultBehavior = struct {
    type: u8,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeByte(self.type);
        writer.splatByte(0, 1);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.type = try reader.takeByte();
        _ = try reader.take(1);
        return result;
    }
};

pub const RadioGroupBehavior = struct {
    type: u8,
    group: u8,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeByte(self.type);
        writer.writeByte(self.group);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.type = try reader.takeByte();
        result.group = try reader.takeByte();
        return result;
    }
};

pub const OverlayBehavior = struct {
    type: u8,
    key: u8,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeByte(self.type);
        writer.writeByte(self.key);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.type = try reader.takeByte();
        result.key = try reader.takeByte();
        return result;
    }
};

pub const SetBehavior = struct {
    keycode: u8,
    behavior: Behavior,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeByte(self.keycode);
        try self.behavior.encode(writer);
        writer.splatByte(0, 1);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.keycode = try reader.takeByte();
        result.behavior = try Behavior.decode(reader);
        _ = try reader.take(1);
        return result;
    }
};

pub const SetExplicit = struct {
    keycode: u8,
    explicit: u8,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeByte(self.keycode);
        writer.writeByte(self.explicit);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.keycode = try reader.takeByte();
        result.explicit = try reader.takeByte();
        return result;
    }
};

pub const KeyModMap = struct {
    keycode: u8,
    mods: u8,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeByte(self.keycode);
        writer.writeByte(self.mods);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.keycode = try reader.takeByte();
        result.mods = try reader.takeByte();
        return result;
    }
};

pub const KeyVModMap = struct {
    keycode: u8,
    vmods: u16,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeByte(self.keycode);
        writer.splatByte(0, 1);
        writer.writeInt(u16, self.vmods);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.keycode = try reader.takeByte();
        _ = try reader.take(1);
        result.vmods = try reader.takeInt(u16, .native);
        return result;
    }
};

pub const KTSetMapEntry = struct {
    level: u8,
    realMods: u8,
    virtualMods: u16,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeByte(self.level);
        writer.writeByte(self.realMods);
        writer.writeInt(u16, self.virtualMods);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.level = try reader.takeByte();
        result.realMods = try reader.takeByte();
        result.virtualMods = try reader.takeInt(u16, .native);
        return result;
    }
};

pub const SetKeyType = struct {
    mask: u8,
    realMods: u8,
    virtualMods: u16,
    numLevels: u8,
    preserve: bool,
    entries: []const KTSetMapEntry,
    decoded_entries_buf: ?[]KTSetMapEntry = null,
    preserve_entries: []const KTSetMapEntry,
    decoded_preserve_entries_buf: ?[]KTSetMapEntry = null,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeByte(self.mask);
        writer.writeByte(self.realMods);
        writer.writeInt(u16, self.virtualMods);
        writer.writeByte(self.numLevels);
        writer.writeByte(@intCast(self.entries.len));
        writer.writeByte(@intFromBool(self.preserve));
        writer.splatByte(0, 1);
        for (self.entries) |elem| {
            try elem.encode(writer);
        }
        for (self.preserve_entries) |elem| {
            try elem.encode(writer);
        }
    }

    pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.mask = try reader.takeByte();
        result.realMods = try reader.takeByte();
        result.virtualMods = try reader.takeInt(u16, .native);
        result.numLevels = try reader.takeByte();
        const nMapEntries = try reader.takeByte();
        result.preserve = (try reader.takeByte()) != 0;
        _ = try reader.take(1);
        const decoded_entries_buf = try allocator.alloc(KTSetMapEntry, @intCast(nMapEntries));
        for (decoded_entries_buf) |*elem| {
            elem.* = try KTSetMapEntry.decode(reader);
        }
        result.entries = decoded_entries_buf;
        result.decoded_entries_buf = decoded_entries_buf;
        const decoded_preserve_entries_buf = try allocator.alloc(KTSetMapEntry, @intCast((result.preserve * result.nMapEntries)));
        for (decoded_preserve_entries_buf) |*elem| {
            elem.* = try KTSetMapEntry.decode(reader);
        }
        result.preserve_entries = decoded_preserve_entries_buf;
        result.decoded_preserve_entries_buf = decoded_preserve_entries_buf;
        return result;
    }

    pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
        if (self.decoded_entries_buf) |buf| {
            allocator.free(buf);
            self.decoded_entries_buf = null;
            self.entries = &.{};
        }
        if (self.decoded_preserve_entries_buf) |buf| {
            allocator.free(buf);
            self.decoded_preserve_entries_buf = null;
            self.preserve_entries = &.{};
        }
    }
};

pub const Outline = struct {
    cornerRadius: u8,
    points: []const xproto.POINT,
    decoded_points_buf: ?[]xproto.POINT = null,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeByte(@intCast(self.points.len));
        writer.writeByte(self.cornerRadius);
        writer.splatByte(0, 2);
        for (self.points) |elem| {
            try elem.encode(writer);
        }
    }

    pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        const nPoints = try reader.takeByte();
        result.cornerRadius = try reader.takeByte();
        _ = try reader.take(2);
        const decoded_points_buf = try allocator.alloc(xproto.POINT, @intCast(nPoints));
        for (decoded_points_buf) |*elem| {
            elem.* = try xproto.POINT.decode(reader);
        }
        result.points = decoded_points_buf;
        result.decoded_points_buf = decoded_points_buf;
        return result;
    }

    pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
        if (self.decoded_points_buf) |buf| {
            allocator.free(buf);
            self.decoded_points_buf = null;
            self.points = &.{};
        }
    }
};

pub const Shape = struct {
    name: xproto.Atom,
    primaryNdx: u8,
    approxNdx: u8,
    outlines: []const Outline,
    decoded_outlines_buf: ?[]Outline = null,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u32, @intCast(@intFromEnum(self.name)));
        writer.writeByte(@intCast(self.outlines.len));
        writer.writeByte(self.primaryNdx);
        writer.writeByte(self.approxNdx);
        writer.splatByte(0, 1);
        for (self.outlines) |elem| {
            try elem.encode(writer);
        }
    }

    pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.name = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
        const nOutlines = try reader.takeByte();
        result.primaryNdx = try reader.takeByte();
        result.approxNdx = try reader.takeByte();
        _ = try reader.take(1);
        const decoded_outlines_buf = try allocator.alloc(Outline, @intCast(nOutlines));
        for (decoded_outlines_buf) |*elem| {
            elem.* = try Outline.decode(allocator, reader);
        }
        result.outlines = decoded_outlines_buf;
        result.decoded_outlines_buf = decoded_outlines_buf;
        return result;
    }

    pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
        if (self.decoded_outlines_buf) |buf| {
            for (buf) |*it| {
                it.deinit(allocator);
            }
            allocator.free(buf);
            self.decoded_outlines_buf = null;
            self.outlines = &.{};
        }
    }
};

pub const Key = struct {
    name: [4]u8,
    gap: i16,
    shapeNdx: u8,
    colorNdx: u8,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.write(self.name[0..]);
        writer.writeInt(i16, self.gap);
        writer.writeByte(self.shapeNdx);
        writer.writeByte(self.colorNdx);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        for (&result.name) |*elem| {
            elem.* = try reader.takeByte();
        }
        result.gap = try reader.takeInt(i16, .native);
        result.shapeNdx = try reader.takeByte();
        result.colorNdx = try reader.takeByte();
        return result;
    }
};

pub const OverlayKey = struct {
    over: [4]u8,
    under: [4]u8,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.write(self.over[0..]);
        writer.write(self.under[0..]);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        for (&result.over) |*elem| {
            elem.* = try reader.takeByte();
        }
        for (&result.under) |*elem| {
            elem.* = try reader.takeByte();
        }
        return result;
    }
};

pub const OverlayRow = struct {
    rowUnder: u8,
    keys: []const OverlayKey,
    decoded_keys_buf: ?[]OverlayKey = null,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeByte(self.rowUnder);
        writer.writeByte(@intCast(self.keys.len));
        writer.splatByte(0, 2);
        for (self.keys) |elem| {
            try elem.encode(writer);
        }
    }

    pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.rowUnder = try reader.takeByte();
        const nKeys = try reader.takeByte();
        _ = try reader.take(2);
        const decoded_keys_buf = try allocator.alloc(OverlayKey, @intCast(nKeys));
        for (decoded_keys_buf) |*elem| {
            elem.* = try OverlayKey.decode(reader);
        }
        result.keys = decoded_keys_buf;
        result.decoded_keys_buf = decoded_keys_buf;
        return result;
    }

    pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
        if (self.decoded_keys_buf) |buf| {
            allocator.free(buf);
            self.decoded_keys_buf = null;
            self.keys = &.{};
        }
    }
};

pub const Overlay = struct {
    name: xproto.Atom,
    rows: []const OverlayRow,
    decoded_rows_buf: ?[]OverlayRow = null,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u32, @intCast(@intFromEnum(self.name)));
        writer.writeByte(@intCast(self.rows.len));
        writer.splatByte(0, 3);
        for (self.rows) |elem| {
            try elem.encode(writer);
        }
    }

    pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.name = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
        const nRows = try reader.takeByte();
        _ = try reader.take(3);
        const decoded_rows_buf = try allocator.alloc(OverlayRow, @intCast(nRows));
        for (decoded_rows_buf) |*elem| {
            elem.* = try OverlayRow.decode(allocator, reader);
        }
        result.rows = decoded_rows_buf;
        result.decoded_rows_buf = decoded_rows_buf;
        return result;
    }

    pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
        if (self.decoded_rows_buf) |buf| {
            for (buf) |*it| {
                it.deinit(allocator);
            }
            allocator.free(buf);
            self.decoded_rows_buf = null;
            self.rows = &.{};
        }
    }
};

pub const Row = struct {
    top: i16,
    left: i16,
    vertical: bool,
    keys: []const Key,
    decoded_keys_buf: ?[]Key = null,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(i16, self.top);
        writer.writeInt(i16, self.left);
        writer.writeByte(@intCast(self.keys.len));
        writer.writeByte(@intFromBool(self.vertical));
        writer.splatByte(0, 2);
        for (self.keys) |elem| {
            try elem.encode(writer);
        }
    }

    pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.top = try reader.takeInt(i16, .native);
        result.left = try reader.takeInt(i16, .native);
        const nKeys = try reader.takeByte();
        result.vertical = (try reader.takeByte()) != 0;
        _ = try reader.take(2);
        const decoded_keys_buf = try allocator.alloc(Key, @intCast(nKeys));
        for (decoded_keys_buf) |*elem| {
            elem.* = try Key.decode(reader);
        }
        result.keys = decoded_keys_buf;
        result.decoded_keys_buf = decoded_keys_buf;
        return result;
    }

    pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
        if (self.decoded_keys_buf) |buf| {
            allocator.free(buf);
            self.decoded_keys_buf = null;
            self.keys = &.{};
        }
    }
};

pub const Listing = struct {
    flags: u16,
    string: []const u8,
    decoded_string_buf: ?[]u8 = null,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u16, self.flags);
        writer.writeInt(u16, @intCast(self.string.len));
        writer.write(self.string);
        writer.splatByte(0, wire.pad(writer.seek, 2));
    }

    pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.flags = try reader.takeInt(u16, .native);
        const length = try reader.takeInt(u16, .native);
        const decoded_string_buf = try allocator.dupe(u8, try reader.take(@intCast(length)));
        result.string = decoded_string_buf;
        result.decoded_string_buf = decoded_string_buf;
        _ = try reader.take(wire.pad(reader.seek, 2));
        return result;
    }

    pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
        if (self.decoded_string_buf) |buf| {
            allocator.free(buf);
            self.decoded_string_buf = null;
            self.string = &.{};
        }
    }
};

pub const DeviceLedInfo = struct {
    ledClass: LedClass,
    ledID: u16,
    namesPresent: u32,
    mapsPresent: u32,
    physIndicators: u32,
    state: u32,
    names: []const xproto.Atom,
    decoded_names_buf: ?[]xproto.Atom = null,
    maps: []const IndicatorMap,
    decoded_maps_buf: ?[]IndicatorMap = null,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u16, @intCast(@intFromEnum(self.ledClass)));
        writer.writeInt(u16, self.ledID);
        writer.writeInt(u32, self.namesPresent);
        writer.writeInt(u32, self.mapsPresent);
        writer.writeInt(u32, self.physIndicators);
        writer.writeInt(u32, self.state);
        for (self.names) |elem| {
            writer.writeInt(u32, @intCast(@intFromEnum(elem)));
        }
        for (self.maps) |elem| {
            try elem.encode(writer);
        }
    }

    pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.ledClass = @as(LedClass, @enumFromInt(try reader.takeInt(u16, .native)));
        result.ledID = try reader.takeInt(u16, .native);
        result.namesPresent = try reader.takeInt(u32, .native);
        result.mapsPresent = try reader.takeInt(u32, .native);
        result.physIndicators = try reader.takeInt(u32, .native);
        result.state = try reader.takeInt(u32, .native);
        const decoded_names_buf = try allocator.alloc(xproto.Atom, @intCast(@popCount(result.namesPresent)));
        for (decoded_names_buf) |*elem| {
            elem.* = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
        }
        result.names = decoded_names_buf;
        result.decoded_names_buf = decoded_names_buf;
        const decoded_maps_buf = try allocator.alloc(IndicatorMap, @intCast(@popCount(result.mapsPresent)));
        for (decoded_maps_buf) |*elem| {
            elem.* = try IndicatorMap.decode(reader);
        }
        result.maps = decoded_maps_buf;
        result.decoded_maps_buf = decoded_maps_buf;
        return result;
    }

    pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
        if (self.decoded_names_buf) |buf| {
            allocator.free(buf);
            self.decoded_names_buf = null;
            self.names = &.{};
        }
        if (self.decoded_maps_buf) |buf| {
            allocator.free(buf);
            self.decoded_maps_buf = null;
            self.maps = &.{};
        }
    }
};

pub const SANoAction = struct {
    type: SAType,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeByte(@intCast(@intFromEnum(self.type)));
        writer.splatByte(0, 7);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.type = @as(SAType, @enumFromInt(try reader.takeByte()));
        _ = try reader.take(7);
        return result;
    }
};

pub const SASetMods = struct {
    type: SAType,
    flags: u8,
    mask: u8,
    realMods: u8,
    vmodsHigh: u8,
    vmodsLow: u8,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeByte(@intCast(@intFromEnum(self.type)));
        writer.writeByte(self.flags);
        writer.writeByte(self.mask);
        writer.writeByte(self.realMods);
        writer.writeByte(self.vmodsHigh);
        writer.writeByte(self.vmodsLow);
        writer.splatByte(0, 2);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.type = @as(SAType, @enumFromInt(try reader.takeByte()));
        result.flags = try reader.takeByte();
        result.mask = try reader.takeByte();
        result.realMods = try reader.takeByte();
        result.vmodsHigh = try reader.takeByte();
        result.vmodsLow = try reader.takeByte();
        _ = try reader.take(2);
        return result;
    }
};

pub const SASetGroup = struct {
    type: SAType,
    flags: u8,
    group: i8,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeByte(@intCast(@intFromEnum(self.type)));
        writer.writeByte(self.flags);
        writer.writeInt(i8, self.group);
        writer.splatByte(0, 5);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.type = @as(SAType, @enumFromInt(try reader.takeByte()));
        result.flags = try reader.takeByte();
        result.group = try reader.takeInt(i8, .native);
        _ = try reader.take(5);
        return result;
    }
};

pub const SAMovePtr = struct {
    type: SAType,
    flags: u8,
    xHigh: i8,
    xLow: u8,
    yHigh: i8,
    yLow: u8,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeByte(@intCast(@intFromEnum(self.type)));
        writer.writeByte(self.flags);
        writer.writeInt(i8, self.xHigh);
        writer.writeByte(self.xLow);
        writer.writeInt(i8, self.yHigh);
        writer.writeByte(self.yLow);
        writer.splatByte(0, 2);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.type = @as(SAType, @enumFromInt(try reader.takeByte()));
        result.flags = try reader.takeByte();
        result.xHigh = try reader.takeInt(i8, .native);
        result.xLow = try reader.takeByte();
        result.yHigh = try reader.takeInt(i8, .native);
        result.yLow = try reader.takeByte();
        _ = try reader.take(2);
        return result;
    }
};

pub const SAPtrBtn = struct {
    type: SAType,
    flags: u8,
    count: u8,
    button: u8,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeByte(@intCast(@intFromEnum(self.type)));
        writer.writeByte(self.flags);
        writer.writeByte(self.count);
        writer.writeByte(self.button);
        writer.splatByte(0, 4);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.type = @as(SAType, @enumFromInt(try reader.takeByte()));
        result.flags = try reader.takeByte();
        result.count = try reader.takeByte();
        result.button = try reader.takeByte();
        _ = try reader.take(4);
        return result;
    }
};

pub const SALockPtrBtn = struct {
    type: SAType,
    flags: u8,
    button: u8,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeByte(@intCast(@intFromEnum(self.type)));
        writer.writeByte(self.flags);
        writer.splatByte(0, 1);
        writer.writeByte(self.button);
        writer.splatByte(0, 4);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.type = @as(SAType, @enumFromInt(try reader.takeByte()));
        result.flags = try reader.takeByte();
        _ = try reader.take(1);
        result.button = try reader.takeByte();
        _ = try reader.take(4);
        return result;
    }
};

pub const SASetPtrDflt = struct {
    type: SAType,
    flags: u8,
    affect: u8,
    value: i8,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeByte(@intCast(@intFromEnum(self.type)));
        writer.writeByte(self.flags);
        writer.writeByte(self.affect);
        writer.writeInt(i8, self.value);
        writer.splatByte(0, 4);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.type = @as(SAType, @enumFromInt(try reader.takeByte()));
        result.flags = try reader.takeByte();
        result.affect = try reader.takeByte();
        result.value = try reader.takeInt(i8, .native);
        _ = try reader.take(4);
        return result;
    }
};

pub const SAIsoLock = struct {
    type: SAType,
    flags: u8,
    mask: u8,
    realMods: u8,
    group: i8,
    affect: u8,
    vmodsHigh: u8,
    vmodsLow: u8,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeByte(@intCast(@intFromEnum(self.type)));
        writer.writeByte(self.flags);
        writer.writeByte(self.mask);
        writer.writeByte(self.realMods);
        writer.writeInt(i8, self.group);
        writer.writeByte(self.affect);
        writer.writeByte(self.vmodsHigh);
        writer.writeByte(self.vmodsLow);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.type = @as(SAType, @enumFromInt(try reader.takeByte()));
        result.flags = try reader.takeByte();
        result.mask = try reader.takeByte();
        result.realMods = try reader.takeByte();
        result.group = try reader.takeInt(i8, .native);
        result.affect = try reader.takeByte();
        result.vmodsHigh = try reader.takeByte();
        result.vmodsLow = try reader.takeByte();
        return result;
    }
};

pub const SATerminate = struct {
    type: SAType,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeByte(@intCast(@intFromEnum(self.type)));
        writer.splatByte(0, 7);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.type = @as(SAType, @enumFromInt(try reader.takeByte()));
        _ = try reader.take(7);
        return result;
    }
};

pub const SASwitchScreen = struct {
    type: SAType,
    flags: u8,
    newScreen: i8,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeByte(@intCast(@intFromEnum(self.type)));
        writer.writeByte(self.flags);
        writer.writeInt(i8, self.newScreen);
        writer.splatByte(0, 5);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.type = @as(SAType, @enumFromInt(try reader.takeByte()));
        result.flags = try reader.takeByte();
        result.newScreen = try reader.takeInt(i8, .native);
        _ = try reader.take(5);
        return result;
    }
};

pub const SASetControls = struct {
    type: SAType,
    boolCtrlsHigh: u8,
    boolCtrlsLow: u8,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeByte(@intCast(@intFromEnum(self.type)));
        writer.splatByte(0, 3);
        writer.writeByte(self.boolCtrlsHigh);
        writer.writeByte(self.boolCtrlsLow);
        writer.splatByte(0, 2);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.type = @as(SAType, @enumFromInt(try reader.takeByte()));
        _ = try reader.take(3);
        result.boolCtrlsHigh = try reader.takeByte();
        result.boolCtrlsLow = try reader.takeByte();
        _ = try reader.take(2);
        return result;
    }
};

pub const SAActionMessage = struct {
    type: SAType,
    flags: u8,
    message: [6]u8,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeByte(@intCast(@intFromEnum(self.type)));
        writer.writeByte(self.flags);
        writer.write(self.message[0..]);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.type = @as(SAType, @enumFromInt(try reader.takeByte()));
        result.flags = try reader.takeByte();
        for (&result.message) |*elem| {
            elem.* = try reader.takeByte();
        }
        return result;
    }
};

pub const SARedirectKey = struct {
    type: SAType,
    newkey: u8,
    mask: u8,
    realModifiers: u8,
    vmodsMaskHigh: u8,
    vmodsMaskLow: u8,
    vmodsHigh: u8,
    vmodsLow: u8,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeByte(@intCast(@intFromEnum(self.type)));
        writer.writeByte(self.newkey);
        writer.writeByte(self.mask);
        writer.writeByte(self.realModifiers);
        writer.writeByte(self.vmodsMaskHigh);
        writer.writeByte(self.vmodsMaskLow);
        writer.writeByte(self.vmodsHigh);
        writer.writeByte(self.vmodsLow);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.type = @as(SAType, @enumFromInt(try reader.takeByte()));
        result.newkey = try reader.takeByte();
        result.mask = try reader.takeByte();
        result.realModifiers = try reader.takeByte();
        result.vmodsMaskHigh = try reader.takeByte();
        result.vmodsMaskLow = try reader.takeByte();
        result.vmodsHigh = try reader.takeByte();
        result.vmodsLow = try reader.takeByte();
        return result;
    }
};

pub const SADeviceBtn = struct {
    type: SAType,
    flags: u8,
    count: u8,
    button: u8,
    device: u8,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeByte(@intCast(@intFromEnum(self.type)));
        writer.writeByte(self.flags);
        writer.writeByte(self.count);
        writer.writeByte(self.button);
        writer.writeByte(self.device);
        writer.splatByte(0, 3);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.type = @as(SAType, @enumFromInt(try reader.takeByte()));
        result.flags = try reader.takeByte();
        result.count = try reader.takeByte();
        result.button = try reader.takeByte();
        result.device = try reader.takeByte();
        _ = try reader.take(3);
        return result;
    }
};

pub const SALockDeviceBtn = struct {
    type: SAType,
    flags: u8,
    button: u8,
    device: u8,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeByte(@intCast(@intFromEnum(self.type)));
        writer.writeByte(self.flags);
        writer.splatByte(0, 1);
        writer.writeByte(self.button);
        writer.writeByte(self.device);
        writer.splatByte(0, 3);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.type = @as(SAType, @enumFromInt(try reader.takeByte()));
        result.flags = try reader.takeByte();
        _ = try reader.take(1);
        result.button = try reader.takeByte();
        result.device = try reader.takeByte();
        _ = try reader.take(3);
        return result;
    }
};

pub const SADeviceValuator = struct {
    type: SAType,
    device: u8,
    val1what: SAValWhat,
    val1index: u8,
    val1value: u8,
    val2what: SAValWhat,
    val2index: u8,
    val2value: u8,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeByte(@intCast(@intFromEnum(self.type)));
        writer.writeByte(self.device);
        writer.writeByte(@intCast(@intFromEnum(self.val1what)));
        writer.writeByte(self.val1index);
        writer.writeByte(self.val1value);
        writer.writeByte(@intCast(@intFromEnum(self.val2what)));
        writer.writeByte(self.val2index);
        writer.writeByte(self.val2value);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.type = @as(SAType, @enumFromInt(try reader.takeByte()));
        result.device = try reader.takeByte();
        result.val1what = @as(SAValWhat, @enumFromInt(try reader.takeByte()));
        result.val1index = try reader.takeByte();
        result.val1value = try reader.takeByte();
        result.val2what = @as(SAValWhat, @enumFromInt(try reader.takeByte()));
        result.val2index = try reader.takeByte();
        result.val2value = try reader.takeByte();
        return result;
    }
};

pub const SIAction = struct {
    type: SAType,
    data: [7]u8,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeByte(@intCast(@intFromEnum(self.type)));
        writer.write(self.data[0..]);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.type = @as(SAType, @enumFromInt(try reader.takeByte()));
        for (&result.data) |*elem| {
            elem.* = try reader.takeByte();
        }
        return result;
    }
};

pub const SymInterpret = struct {
    sym: u32,
    mods: u8,
    match: u8,
    virtualMod: u8,
    flags: u8,
    action: SIAction,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u32, self.sym);
        writer.writeByte(self.mods);
        writer.writeByte(self.match);
        writer.writeByte(self.virtualMod);
        writer.writeByte(self.flags);
        try self.action.encode(writer);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.sym = try reader.takeInt(u32, .native);
        result.mods = try reader.takeByte();
        result.match = try reader.takeByte();
        result.virtualMod = try reader.takeByte();
        result.flags = try reader.takeByte();
        result.action = try SIAction.decode(reader);
        return result;
    }
};

pub const Behavior = struct {
    raw: [2]u8,

    pub fn fromRaw(raw: [2]u8) @This() {
        return .{ .raw = raw };
    }

    pub fn asRaw(self: @This()) [2]u8 {
        return self.raw;
    }

    pub fn encode(self: @This(), writer: anytype) !void {
        writer.write(self.raw[0..]);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var raw: [2]u8 = undefined;
        @memcpy(raw[0..], try reader.take(2));
        return .{ .raw = raw };
    }

    pub fn fromCommon(value: CommonBehavior) @This() {
        var raw = std.mem.zeroes([2]u8);
        var writer_impl = io.FixedBufferWriter.init(&raw);
        const writer = &writer_impl;
        try value.encode(writer);
        return .{ .raw = raw };
    }

    pub fn asCommon(self: @This()) !CommonBehavior {
        var reader_impl: std.Io.Reader = .fixed(&self.raw);
        const reader = &reader_impl;
        var value: CommonBehavior = undefined;
        value = try CommonBehavior.decode(reader);
        return value;
    }

    pub fn fromDefault(value: DefaultBehavior) @This() {
        var raw = std.mem.zeroes([2]u8);
        var writer_impl = io.FixedBufferWriter.init(&raw);
        const writer = &writer_impl;
        try value.encode(writer);
        return .{ .raw = raw };
    }

    pub fn asDefault(self: @This()) !DefaultBehavior {
        var reader_impl: std.Io.Reader = .fixed(&self.raw);
        const reader = &reader_impl;
        var value: DefaultBehavior = undefined;
        value = try DefaultBehavior.decode(reader);
        return value;
    }

    pub fn fromLock(value: DefaultBehavior) @This() {
        var raw = std.mem.zeroes([2]u8);
        var writer_impl = io.FixedBufferWriter.init(&raw);
        const writer = &writer_impl;
        try value.encode(writer);
        return .{ .raw = raw };
    }

    pub fn asLock(self: @This()) !DefaultBehavior {
        var reader_impl: std.Io.Reader = .fixed(&self.raw);
        const reader = &reader_impl;
        var value: DefaultBehavior = undefined;
        value = try DefaultBehavior.decode(reader);
        return value;
    }

    pub fn fromRadioGroup(value: RadioGroupBehavior) @This() {
        var raw = std.mem.zeroes([2]u8);
        var writer_impl = io.FixedBufferWriter.init(&raw);
        const writer = &writer_impl;
        try value.encode(writer);
        return .{ .raw = raw };
    }

    pub fn asRadioGroup(self: @This()) !RadioGroupBehavior {
        var reader_impl: std.Io.Reader = .fixed(&self.raw);
        const reader = &reader_impl;
        var value: RadioGroupBehavior = undefined;
        value = try RadioGroupBehavior.decode(reader);
        return value;
    }

    pub fn fromOverlay1(value: OverlayBehavior) @This() {
        var raw = std.mem.zeroes([2]u8);
        var writer_impl = io.FixedBufferWriter.init(&raw);
        const writer = &writer_impl;
        try value.encode(writer);
        return .{ .raw = raw };
    }

    pub fn asOverlay1(self: @This()) !OverlayBehavior {
        var reader_impl: std.Io.Reader = .fixed(&self.raw);
        const reader = &reader_impl;
        var value: OverlayBehavior = undefined;
        value = try OverlayBehavior.decode(reader);
        return value;
    }

    pub fn fromOverlay2(value: OverlayBehavior) @This() {
        var raw = std.mem.zeroes([2]u8);
        var writer_impl = io.FixedBufferWriter.init(&raw);
        const writer = &writer_impl;
        try value.encode(writer);
        return .{ .raw = raw };
    }

    pub fn asOverlay2(self: @This()) !OverlayBehavior {
        var reader_impl: std.Io.Reader = .fixed(&self.raw);
        const reader = &reader_impl;
        var value: OverlayBehavior = undefined;
        value = try OverlayBehavior.decode(reader);
        return value;
    }

    pub fn fromPermamentLock(value: DefaultBehavior) @This() {
        var raw = std.mem.zeroes([2]u8);
        var writer_impl = io.FixedBufferWriter.init(&raw);
        const writer = &writer_impl;
        try value.encode(writer);
        return .{ .raw = raw };
    }

    pub fn asPermamentLock(self: @This()) !DefaultBehavior {
        var reader_impl: std.Io.Reader = .fixed(&self.raw);
        const reader = &reader_impl;
        var value: DefaultBehavior = undefined;
        value = try DefaultBehavior.decode(reader);
        return value;
    }

    pub fn fromPermamentRadioGroup(value: RadioGroupBehavior) @This() {
        var raw = std.mem.zeroes([2]u8);
        var writer_impl = io.FixedBufferWriter.init(&raw);
        const writer = &writer_impl;
        try value.encode(writer);
        return .{ .raw = raw };
    }

    pub fn asPermamentRadioGroup(self: @This()) !RadioGroupBehavior {
        var reader_impl: std.Io.Reader = .fixed(&self.raw);
        const reader = &reader_impl;
        var value: RadioGroupBehavior = undefined;
        value = try RadioGroupBehavior.decode(reader);
        return value;
    }

    pub fn fromPermamentOverlay1(value: OverlayBehavior) @This() {
        var raw = std.mem.zeroes([2]u8);
        var writer_impl = io.FixedBufferWriter.init(&raw);
        const writer = &writer_impl;
        try value.encode(writer);
        return .{ .raw = raw };
    }

    pub fn asPermamentOverlay1(self: @This()) !OverlayBehavior {
        var reader_impl: std.Io.Reader = .fixed(&self.raw);
        const reader = &reader_impl;
        var value: OverlayBehavior = undefined;
        value = try OverlayBehavior.decode(reader);
        return value;
    }

    pub fn fromPermamentOverlay2(value: OverlayBehavior) @This() {
        var raw = std.mem.zeroes([2]u8);
        var writer_impl = io.FixedBufferWriter.init(&raw);
        const writer = &writer_impl;
        try value.encode(writer);
        return .{ .raw = raw };
    }

    pub fn asPermamentOverlay2(self: @This()) !OverlayBehavior {
        var reader_impl: std.Io.Reader = .fixed(&self.raw);
        const reader = &reader_impl;
        var value: OverlayBehavior = undefined;
        value = try OverlayBehavior.decode(reader);
        return value;
    }

    pub fn fromType(value: u8) @This() {
        var raw = std.mem.zeroes([2]u8);
        var writer_impl = io.FixedBufferWriter.init(&raw);
        const writer = &writer_impl;
        writer.writeByte(value);
        return .{ .raw = raw };
    }

    pub fn asType(self: @This()) !u8 {
        var reader_impl: std.Io.Reader = .fixed(&self.raw);
        const reader = &reader_impl;
        var value: u8 = undefined;
        value = try reader.takeByte();
        return value;
    }
};

pub const Action = struct {
    raw: [8]u8,

    pub fn fromRaw(raw: [8]u8) @This() {
        return .{ .raw = raw };
    }

    pub fn asRaw(self: @This()) [8]u8 {
        return self.raw;
    }

    pub fn encode(self: @This(), writer: anytype) !void {
        writer.write(self.raw[0..]);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var raw: [8]u8 = undefined;
        @memcpy(raw[0..], try reader.take(8));
        return .{ .raw = raw };
    }

    pub fn fromNoaction(value: SANoAction) @This() {
        var raw = std.mem.zeroes([8]u8);
        var writer_impl = io.FixedBufferWriter.init(&raw);
        const writer = &writer_impl;
        try value.encode(writer);
        return .{ .raw = raw };
    }

    pub fn asNoaction(self: @This()) !SANoAction {
        var reader_impl: std.Io.Reader = .fixed(&self.raw);
        const reader = &reader_impl;
        var value: SANoAction = undefined;
        value = try SANoAction.decode(reader);
        return value;
    }

    pub fn fromSetmods(value: SASetMods) @This() {
        var raw = std.mem.zeroes([8]u8);
        var writer_impl = io.FixedBufferWriter.init(&raw);
        const writer = &writer_impl;
        try value.encode(writer);
        return .{ .raw = raw };
    }

    pub fn asSetmods(self: @This()) !SASetMods {
        var reader_impl: std.Io.Reader = .fixed(&self.raw);
        const reader = &reader_impl;
        var value: SASetMods = undefined;
        value = try SASetMods.decode(reader);
        return value;
    }

    pub fn fromLatchmods(value: SASetMods) @This() {
        var raw = std.mem.zeroes([8]u8);
        var writer_impl = io.FixedBufferWriter.init(&raw);
        const writer = &writer_impl;
        try value.encode(writer);
        return .{ .raw = raw };
    }

    pub fn asLatchmods(self: @This()) !SASetMods {
        var reader_impl: std.Io.Reader = .fixed(&self.raw);
        const reader = &reader_impl;
        var value: SASetMods = undefined;
        value = try SASetMods.decode(reader);
        return value;
    }

    pub fn fromLockmods(value: SASetMods) @This() {
        var raw = std.mem.zeroes([8]u8);
        var writer_impl = io.FixedBufferWriter.init(&raw);
        const writer = &writer_impl;
        try value.encode(writer);
        return .{ .raw = raw };
    }

    pub fn asLockmods(self: @This()) !SASetMods {
        var reader_impl: std.Io.Reader = .fixed(&self.raw);
        const reader = &reader_impl;
        var value: SASetMods = undefined;
        value = try SASetMods.decode(reader);
        return value;
    }

    pub fn fromSetgroup(value: SASetGroup) @This() {
        var raw = std.mem.zeroes([8]u8);
        var writer_impl = io.FixedBufferWriter.init(&raw);
        const writer = &writer_impl;
        try value.encode(writer);
        return .{ .raw = raw };
    }

    pub fn asSetgroup(self: @This()) !SASetGroup {
        var reader_impl: std.Io.Reader = .fixed(&self.raw);
        const reader = &reader_impl;
        var value: SASetGroup = undefined;
        value = try SASetGroup.decode(reader);
        return value;
    }

    pub fn fromLatchgroup(value: SASetGroup) @This() {
        var raw = std.mem.zeroes([8]u8);
        var writer_impl = io.FixedBufferWriter.init(&raw);
        const writer = &writer_impl;
        try value.encode(writer);
        return .{ .raw = raw };
    }

    pub fn asLatchgroup(self: @This()) !SASetGroup {
        var reader_impl: std.Io.Reader = .fixed(&self.raw);
        const reader = &reader_impl;
        var value: SASetGroup = undefined;
        value = try SASetGroup.decode(reader);
        return value;
    }

    pub fn fromLockgroup(value: SASetGroup) @This() {
        var raw = std.mem.zeroes([8]u8);
        var writer_impl = io.FixedBufferWriter.init(&raw);
        const writer = &writer_impl;
        try value.encode(writer);
        return .{ .raw = raw };
    }

    pub fn asLockgroup(self: @This()) !SASetGroup {
        var reader_impl: std.Io.Reader = .fixed(&self.raw);
        const reader = &reader_impl;
        var value: SASetGroup = undefined;
        value = try SASetGroup.decode(reader);
        return value;
    }

    pub fn fromMoveptr(value: SAMovePtr) @This() {
        var raw = std.mem.zeroes([8]u8);
        var writer_impl = io.FixedBufferWriter.init(&raw);
        const writer = &writer_impl;
        try value.encode(writer);
        return .{ .raw = raw };
    }

    pub fn asMoveptr(self: @This()) !SAMovePtr {
        var reader_impl: std.Io.Reader = .fixed(&self.raw);
        const reader = &reader_impl;
        var value: SAMovePtr = undefined;
        value = try SAMovePtr.decode(reader);
        return value;
    }

    pub fn fromPtrbtn(value: SAPtrBtn) @This() {
        var raw = std.mem.zeroes([8]u8);
        var writer_impl = io.FixedBufferWriter.init(&raw);
        const writer = &writer_impl;
        try value.encode(writer);
        return .{ .raw = raw };
    }

    pub fn asPtrbtn(self: @This()) !SAPtrBtn {
        var reader_impl: std.Io.Reader = .fixed(&self.raw);
        const reader = &reader_impl;
        var value: SAPtrBtn = undefined;
        value = try SAPtrBtn.decode(reader);
        return value;
    }

    pub fn fromLockptrbtn(value: SALockPtrBtn) @This() {
        var raw = std.mem.zeroes([8]u8);
        var writer_impl = io.FixedBufferWriter.init(&raw);
        const writer = &writer_impl;
        try value.encode(writer);
        return .{ .raw = raw };
    }

    pub fn asLockptrbtn(self: @This()) !SALockPtrBtn {
        var reader_impl: std.Io.Reader = .fixed(&self.raw);
        const reader = &reader_impl;
        var value: SALockPtrBtn = undefined;
        value = try SALockPtrBtn.decode(reader);
        return value;
    }

    pub fn fromSetptrdflt(value: SASetPtrDflt) @This() {
        var raw = std.mem.zeroes([8]u8);
        var writer_impl = io.FixedBufferWriter.init(&raw);
        const writer = &writer_impl;
        try value.encode(writer);
        return .{ .raw = raw };
    }

    pub fn asSetptrdflt(self: @This()) !SASetPtrDflt {
        var reader_impl: std.Io.Reader = .fixed(&self.raw);
        const reader = &reader_impl;
        var value: SASetPtrDflt = undefined;
        value = try SASetPtrDflt.decode(reader);
        return value;
    }

    pub fn fromIsolock(value: SAIsoLock) @This() {
        var raw = std.mem.zeroes([8]u8);
        var writer_impl = io.FixedBufferWriter.init(&raw);
        const writer = &writer_impl;
        try value.encode(writer);
        return .{ .raw = raw };
    }

    pub fn asIsolock(self: @This()) !SAIsoLock {
        var reader_impl: std.Io.Reader = .fixed(&self.raw);
        const reader = &reader_impl;
        var value: SAIsoLock = undefined;
        value = try SAIsoLock.decode(reader);
        return value;
    }

    pub fn fromTerminate(value: SATerminate) @This() {
        var raw = std.mem.zeroes([8]u8);
        var writer_impl = io.FixedBufferWriter.init(&raw);
        const writer = &writer_impl;
        try value.encode(writer);
        return .{ .raw = raw };
    }

    pub fn asTerminate(self: @This()) !SATerminate {
        var reader_impl: std.Io.Reader = .fixed(&self.raw);
        const reader = &reader_impl;
        var value: SATerminate = undefined;
        value = try SATerminate.decode(reader);
        return value;
    }

    pub fn fromSwitchscreen(value: SASwitchScreen) @This() {
        var raw = std.mem.zeroes([8]u8);
        var writer_impl = io.FixedBufferWriter.init(&raw);
        const writer = &writer_impl;
        try value.encode(writer);
        return .{ .raw = raw };
    }

    pub fn asSwitchscreen(self: @This()) !SASwitchScreen {
        var reader_impl: std.Io.Reader = .fixed(&self.raw);
        const reader = &reader_impl;
        var value: SASwitchScreen = undefined;
        value = try SASwitchScreen.decode(reader);
        return value;
    }

    pub fn fromSetcontrols(value: SASetControls) @This() {
        var raw = std.mem.zeroes([8]u8);
        var writer_impl = io.FixedBufferWriter.init(&raw);
        const writer = &writer_impl;
        try value.encode(writer);
        return .{ .raw = raw };
    }

    pub fn asSetcontrols(self: @This()) !SASetControls {
        var reader_impl: std.Io.Reader = .fixed(&self.raw);
        const reader = &reader_impl;
        var value: SASetControls = undefined;
        value = try SASetControls.decode(reader);
        return value;
    }

    pub fn fromLockcontrols(value: SASetControls) @This() {
        var raw = std.mem.zeroes([8]u8);
        var writer_impl = io.FixedBufferWriter.init(&raw);
        const writer = &writer_impl;
        try value.encode(writer);
        return .{ .raw = raw };
    }

    pub fn asLockcontrols(self: @This()) !SASetControls {
        var reader_impl: std.Io.Reader = .fixed(&self.raw);
        const reader = &reader_impl;
        var value: SASetControls = undefined;
        value = try SASetControls.decode(reader);
        return value;
    }

    pub fn fromMessage(value: SAActionMessage) @This() {
        var raw = std.mem.zeroes([8]u8);
        var writer_impl = io.FixedBufferWriter.init(&raw);
        const writer = &writer_impl;
        try value.encode(writer);
        return .{ .raw = raw };
    }

    pub fn asMessage(self: @This()) !SAActionMessage {
        var reader_impl: std.Io.Reader = .fixed(&self.raw);
        const reader = &reader_impl;
        var value: SAActionMessage = undefined;
        value = try SAActionMessage.decode(reader);
        return value;
    }

    pub fn fromRedirect(value: SARedirectKey) @This() {
        var raw = std.mem.zeroes([8]u8);
        var writer_impl = io.FixedBufferWriter.init(&raw);
        const writer = &writer_impl;
        try value.encode(writer);
        return .{ .raw = raw };
    }

    pub fn asRedirect(self: @This()) !SARedirectKey {
        var reader_impl: std.Io.Reader = .fixed(&self.raw);
        const reader = &reader_impl;
        var value: SARedirectKey = undefined;
        value = try SARedirectKey.decode(reader);
        return value;
    }

    pub fn fromDevbtn(value: SADeviceBtn) @This() {
        var raw = std.mem.zeroes([8]u8);
        var writer_impl = io.FixedBufferWriter.init(&raw);
        const writer = &writer_impl;
        try value.encode(writer);
        return .{ .raw = raw };
    }

    pub fn asDevbtn(self: @This()) !SADeviceBtn {
        var reader_impl: std.Io.Reader = .fixed(&self.raw);
        const reader = &reader_impl;
        var value: SADeviceBtn = undefined;
        value = try SADeviceBtn.decode(reader);
        return value;
    }

    pub fn fromLockdevbtn(value: SALockDeviceBtn) @This() {
        var raw = std.mem.zeroes([8]u8);
        var writer_impl = io.FixedBufferWriter.init(&raw);
        const writer = &writer_impl;
        try value.encode(writer);
        return .{ .raw = raw };
    }

    pub fn asLockdevbtn(self: @This()) !SALockDeviceBtn {
        var reader_impl: std.Io.Reader = .fixed(&self.raw);
        const reader = &reader_impl;
        var value: SALockDeviceBtn = undefined;
        value = try SALockDeviceBtn.decode(reader);
        return value;
    }

    pub fn fromDevval(value: SADeviceValuator) @This() {
        var raw = std.mem.zeroes([8]u8);
        var writer_impl = io.FixedBufferWriter.init(&raw);
        const writer = &writer_impl;
        try value.encode(writer);
        return .{ .raw = raw };
    }

    pub fn asDevval(self: @This()) !SADeviceValuator {
        var reader_impl: std.Io.Reader = .fixed(&self.raw);
        const reader = &reader_impl;
        var value: SADeviceValuator = undefined;
        value = try SADeviceValuator.decode(reader);
        return value;
    }

    pub fn fromType(value: SAType) @This() {
        var raw = std.mem.zeroes([8]u8);
        var writer_impl = io.FixedBufferWriter.init(&raw);
        const writer = &writer_impl;
        writer.writeByte(@intCast(@intFromEnum(value)));
        return .{ .raw = raw };
    }

    pub fn asType(self: @This()) !SAType {
        var reader_impl: std.Io.Reader = .fixed(&self.raw);
        const reader = &reader_impl;
        var value: SAType = undefined;
        value = @as(SAType, @enumFromInt(try reader.takeByte()));
        return value;
    }
};

pub const UseExtension = struct {
    pub const opcode: u8 = 0;

    wantedMajor: u16,
    wantedMinor: u16,

    pub const Reply = struct {
        supported: bool,
        serverMajor: u16,
        serverMinor: u16,

        pub fn decode(reader: *std.Io.Reader, header_: wire.ReplyHeader) !@This() {
            var result: @This() = undefined;
            _ = header_;
            result.supported = (try reader.takeByte()) != 0;
            result.serverMajor = try reader.takeInt(u16, .native);
            result.serverMinor = try reader.takeInt(u16, .native);
            _ = try reader.take(20);
            return result;
        }
    };


    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u16, self.wantedMajor);
        writer.writeInt(u16, self.wantedMinor);
    }
};

pub const SelectEvents = struct {
    pub const opcode: u8 = 1;

    deviceSpec: u16,
    affectWhich: u16,
    clear: u16,
    selectAll: u16,
    affectMap: u16,
    map: u16,
    details: Details,

    pub const Details = struct {
        NewKeyboardNotify: ?struct {
            affectNewKeyboard: u16,
            newKeyboardDetails: u16,
        } = null,
        StateNotify: ?struct {
            affectState: u16,
            stateDetails: u16,
        } = null,
        ControlsNotify: ?struct {
            affectCtrls: u32,
            ctrlDetails: u32,
        } = null,
        IndicatorStateNotify: ?struct {
            affectIndicatorState: u32,
            indicatorStateDetails: u32,
        } = null,
        IndicatorMapNotify: ?struct {
            affectIndicatorMap: u32,
            indicatorMapDetails: u32,
        } = null,
        NamesNotify: ?struct {
            affectNames: u16,
            namesDetails: u16,
        } = null,
        CompatMapNotify: ?struct {
            affectCompat: u8,
            compatDetails: u8,
        } = null,
        BellNotify: ?struct {
            affectBell: u8,
            bellDetails: u8,
        } = null,
        ActionMessage: ?struct {
            affectMsgDetails: u8,
            msgDetails: u8,
        } = null,
        AccessXNotify: ?struct {
            affectAccessX: u16,
            accessXDetails: u16,
        } = null,
        ExtensionDeviceNotify: ?struct {
            affectExtDev: u16,
            extdevDetails: u16,
        } = null,

        pub fn encode(self: *const @This(), writer: anytype) !void {
            if (self.NewKeyboardNotify) |it| {
                writer.writeInt(u16, it.affectNewKeyboard);
                writer.writeInt(u16, it.newKeyboardDetails);
            }
            if (self.StateNotify) |it| {
                writer.writeInt(u16, it.affectState);
                writer.writeInt(u16, it.stateDetails);
            }
            if (self.ControlsNotify) |it| {
                writer.writeInt(u32, it.affectCtrls);
                writer.writeInt(u32, it.ctrlDetails);
            }
            if (self.IndicatorStateNotify) |it| {
                writer.writeInt(u32, it.affectIndicatorState);
                writer.writeInt(u32, it.indicatorStateDetails);
            }
            if (self.IndicatorMapNotify) |it| {
                writer.writeInt(u32, it.affectIndicatorMap);
                writer.writeInt(u32, it.indicatorMapDetails);
            }
            if (self.NamesNotify) |it| {
                writer.writeInt(u16, it.affectNames);
                writer.writeInt(u16, it.namesDetails);
            }
            if (self.CompatMapNotify) |it| {
                writer.writeByte(it.affectCompat);
                writer.writeByte(it.compatDetails);
            }
            if (self.BellNotify) |it| {
                writer.writeByte(it.affectBell);
                writer.writeByte(it.bellDetails);
            }
            if (self.ActionMessage) |it| {
                writer.writeByte(it.affectMsgDetails);
                writer.writeByte(it.msgDetails);
            }
            if (self.AccessXNotify) |it| {
                writer.writeInt(u16, it.affectAccessX);
                writer.writeInt(u16, it.accessXDetails);
            }
            if (self.ExtensionDeviceNotify) |it| {
                writer.writeInt(u16, it.affectExtDev);
                writer.writeInt(u16, it.extdevDetails);
            }
        }

        pub fn switchValue(self: *const @This()) u32 {
            var result: u32 = 0;
            if (self.NewKeyboardNotify != null) result |= 1;
            if (self.StateNotify != null) result |= 4;
            if (self.ControlsNotify != null) result |= 8;
            if (self.IndicatorStateNotify != null) result |= 16;
            if (self.IndicatorMapNotify != null) result |= 32;
            if (self.NamesNotify != null) result |= 64;
            if (self.CompatMapNotify != null) result |= 128;
            if (self.BellNotify != null) result |= 256;
            if (self.ActionMessage != null) result |= 512;
            if (self.AccessXNotify != null) result |= 1024;
            if (self.ExtensionDeviceNotify != null) result |= 2048;
            return result;
        }

        pub fn decode(reader: *std.Io.Reader, switch_value: u32) !@This() {
            var result: @This() = .{};
            if ((switch_value & 1) != 0) {
                var payload: @typeInfo(@TypeOf(result.NewKeyboardNotify)).optional.child = undefined;
                payload.affectNewKeyboard = try reader.takeInt(u16, .native);
                payload.newKeyboardDetails = try reader.takeInt(u16, .native);
                result.NewKeyboardNotify = payload;
            }
            if ((switch_value & 4) != 0) {
                var payload: @typeInfo(@TypeOf(result.StateNotify)).optional.child = undefined;
                payload.affectState = try reader.takeInt(u16, .native);
                payload.stateDetails = try reader.takeInt(u16, .native);
                result.StateNotify = payload;
            }
            if ((switch_value & 8) != 0) {
                var payload: @typeInfo(@TypeOf(result.ControlsNotify)).optional.child = undefined;
                payload.affectCtrls = try reader.takeInt(u32, .native);
                payload.ctrlDetails = try reader.takeInt(u32, .native);
                result.ControlsNotify = payload;
            }
            if ((switch_value & 16) != 0) {
                var payload: @typeInfo(@TypeOf(result.IndicatorStateNotify)).optional.child = undefined;
                payload.affectIndicatorState = try reader.takeInt(u32, .native);
                payload.indicatorStateDetails = try reader.takeInt(u32, .native);
                result.IndicatorStateNotify = payload;
            }
            if ((switch_value & 32) != 0) {
                var payload: @typeInfo(@TypeOf(result.IndicatorMapNotify)).optional.child = undefined;
                payload.affectIndicatorMap = try reader.takeInt(u32, .native);
                payload.indicatorMapDetails = try reader.takeInt(u32, .native);
                result.IndicatorMapNotify = payload;
            }
            if ((switch_value & 64) != 0) {
                var payload: @typeInfo(@TypeOf(result.NamesNotify)).optional.child = undefined;
                payload.affectNames = try reader.takeInt(u16, .native);
                payload.namesDetails = try reader.takeInt(u16, .native);
                result.NamesNotify = payload;
            }
            if ((switch_value & 128) != 0) {
                var payload: @typeInfo(@TypeOf(result.CompatMapNotify)).optional.child = undefined;
                payload.affectCompat = try reader.takeByte();
                payload.compatDetails = try reader.takeByte();
                result.CompatMapNotify = payload;
            }
            if ((switch_value & 256) != 0) {
                var payload: @typeInfo(@TypeOf(result.BellNotify)).optional.child = undefined;
                payload.affectBell = try reader.takeByte();
                payload.bellDetails = try reader.takeByte();
                result.BellNotify = payload;
            }
            if ((switch_value & 512) != 0) {
                var payload: @typeInfo(@TypeOf(result.ActionMessage)).optional.child = undefined;
                payload.affectMsgDetails = try reader.takeByte();
                payload.msgDetails = try reader.takeByte();
                result.ActionMessage = payload;
            }
            if ((switch_value & 1024) != 0) {
                var payload: @typeInfo(@TypeOf(result.AccessXNotify)).optional.child = undefined;
                payload.affectAccessX = try reader.takeInt(u16, .native);
                payload.accessXDetails = try reader.takeInt(u16, .native);
                result.AccessXNotify = payload;
            }
            if ((switch_value & 2048) != 0) {
                var payload: @typeInfo(@TypeOf(result.ExtensionDeviceNotify)).optional.child = undefined;
                payload.affectExtDev = try reader.takeInt(u16, .native);
                payload.extdevDetails = try reader.takeInt(u16, .native);
                result.ExtensionDeviceNotify = payload;
            }
            return result;
        }
    };

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u16, self.deviceSpec);
        writer.writeInt(u16, self.affectWhich);
        writer.writeInt(u16, self.clear);
        writer.writeInt(u16, self.selectAll);
        writer.writeInt(u16, self.affectMap);
        writer.writeInt(u16, self.map);
        try self.details.encode(writer);
    }
};

pub const Bell = struct {
    pub const opcode: u8 = 3;

    deviceSpec: u16,
    bellClass: u16,
    bellID: u16,
    percent: i8,
    forceSound: bool,
    eventOnly: bool,
    pitch: i16,
    duration: i16,
    name: xproto.Atom,
    window: xproto.Window,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u16, self.deviceSpec);
        writer.writeInt(u16, self.bellClass);
        writer.writeInt(u16, self.bellID);
        writer.writeInt(i8, self.percent);
        writer.writeByte(@intFromBool(self.forceSound));
        writer.writeByte(@intFromBool(self.eventOnly));
        writer.splatByte(0, 1);
        writer.writeInt(i16, self.pitch);
        writer.writeInt(i16, self.duration);
        writer.splatByte(0, 2);
        writer.writeInt(u32, @intCast(@intFromEnum(self.name)));
        writer.writeInt(u32, @intCast(@intFromEnum(self.window)));
    }
};

pub const GetState = struct {
    pub const opcode: u8 = 4;

    deviceSpec: u16,

    pub const Reply = struct {
        deviceID: u8,
        mods: u8,
        baseMods: u8,
        latchedMods: u8,
        lockedMods: u8,
        group: Group,
        lockedGroup: Group,
        baseGroup: i16,
        latchedGroup: i16,
        compatState: u8,
        grabMods: u8,
        compatGrabMods: u8,
        lookupMods: u8,
        compatLookupMods: u8,
        ptrBtnState: u16,

        pub fn decode(reader: *std.Io.Reader, header_: wire.ReplyHeader) !@This() {
            var result: @This() = undefined;
            _ = header_;
            result.deviceID = try reader.takeByte();
            result.mods = try reader.takeByte();
            result.baseMods = try reader.takeByte();
            result.latchedMods = try reader.takeByte();
            result.lockedMods = try reader.takeByte();
            result.group = @as(Group, @enumFromInt(try reader.takeByte()));
            result.lockedGroup = @as(Group, @enumFromInt(try reader.takeByte()));
            result.baseGroup = try reader.takeInt(i16, .native);
            result.latchedGroup = try reader.takeInt(i16, .native);
            result.compatState = try reader.takeByte();
            result.grabMods = try reader.takeByte();
            result.compatGrabMods = try reader.takeByte();
            result.lookupMods = try reader.takeByte();
            result.compatLookupMods = try reader.takeByte();
            _ = try reader.take(1);
            result.ptrBtnState = try reader.takeInt(u16, .native);
            _ = try reader.take(6);
            return result;
        }
    };


    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u16, self.deviceSpec);
        writer.splatByte(0, 2);
    }
};

pub const LatchLockState = struct {
    pub const opcode: u8 = 5;

    deviceSpec: u16,
    affectModLocks: u8,
    modLocks: u8,
    lockGroup: bool,
    groupLock: Group,
    affectModLatches: u8,
    latchGroup: bool,
    groupLatch: u16,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u16, self.deviceSpec);
        writer.writeByte(self.affectModLocks);
        writer.writeByte(self.modLocks);
        writer.writeByte(@intFromBool(self.lockGroup));
        writer.writeByte(@intCast(@intFromEnum(self.groupLock)));
        writer.writeByte(self.affectModLatches);
        writer.splatByte(0, 1);
        writer.splatByte(0, 1);
        writer.writeByte(@intFromBool(self.latchGroup));
        writer.writeInt(u16, self.groupLatch);
    }
};

pub const GetControls = struct {
    pub const opcode: u8 = 6;

    deviceSpec: u16,

    pub const Reply = struct {
        deviceID: u8,
        mouseKeysDfltBtn: u8,
        numGroups: u8,
        groupsWrap: u8,
        internalModsMask: u8,
        ignoreLockModsMask: u8,
        internalModsRealMods: u8,
        ignoreLockModsRealMods: u8,
        internalModsVmods: u16,
        ignoreLockModsVmods: u16,
        repeatDelay: u16,
        repeatInterval: u16,
        slowKeysDelay: u16,
        debounceDelay: u16,
        mouseKeysDelay: u16,
        mouseKeysInterval: u16,
        mouseKeysTimeToMax: u16,
        mouseKeysMaxSpeed: u16,
        mouseKeysCurve: i16,
        accessXOption: u16,
        accessXTimeout: u16,
        accessXTimeoutOptionsMask: u16,
        accessXTimeoutOptionsValues: u16,
        accessXTimeoutMask: u32,
        accessXTimeoutValues: u32,
        enabledControls: u32,
        perKeyRepeat: [32]u8,

        pub fn decode(reader: *std.Io.Reader, header_: wire.ReplyHeader) !@This() {
            var result: @This() = undefined;
            _ = header_;
            result.deviceID = try reader.takeByte();
            result.mouseKeysDfltBtn = try reader.takeByte();
            result.numGroups = try reader.takeByte();
            result.groupsWrap = try reader.takeByte();
            result.internalModsMask = try reader.takeByte();
            result.ignoreLockModsMask = try reader.takeByte();
            result.internalModsRealMods = try reader.takeByte();
            result.ignoreLockModsRealMods = try reader.takeByte();
            _ = try reader.take(1);
            result.internalModsVmods = try reader.takeInt(u16, .native);
            result.ignoreLockModsVmods = try reader.takeInt(u16, .native);
            result.repeatDelay = try reader.takeInt(u16, .native);
            result.repeatInterval = try reader.takeInt(u16, .native);
            result.slowKeysDelay = try reader.takeInt(u16, .native);
            result.debounceDelay = try reader.takeInt(u16, .native);
            result.mouseKeysDelay = try reader.takeInt(u16, .native);
            result.mouseKeysInterval = try reader.takeInt(u16, .native);
            result.mouseKeysTimeToMax = try reader.takeInt(u16, .native);
            result.mouseKeysMaxSpeed = try reader.takeInt(u16, .native);
            result.mouseKeysCurve = try reader.takeInt(i16, .native);
            result.accessXOption = try reader.takeInt(u16, .native);
            result.accessXTimeout = try reader.takeInt(u16, .native);
            result.accessXTimeoutOptionsMask = try reader.takeInt(u16, .native);
            result.accessXTimeoutOptionsValues = try reader.takeInt(u16, .native);
            _ = try reader.take(2);
            result.accessXTimeoutMask = try reader.takeInt(u32, .native);
            result.accessXTimeoutValues = try reader.takeInt(u32, .native);
            result.enabledControls = try reader.takeInt(u32, .native);
            for (&result.perKeyRepeat) |*elem| {
                elem.* = try reader.takeByte();
            }
            return result;
        }
    };


    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u16, self.deviceSpec);
        writer.splatByte(0, 2);
    }
};

pub const SetControls = struct {
    pub const opcode: u8 = 7;

    deviceSpec: u16,
    affectInternalRealMods: u8,
    internalRealMods: u8,
    affectIgnoreLockRealMods: u8,
    ignoreLockRealMods: u8,
    affectInternalVirtualMods: u16,
    internalVirtualMods: u16,
    affectIgnoreLockVirtualMods: u16,
    ignoreLockVirtualMods: u16,
    mouseKeysDfltBtn: u8,
    groupsWrap: u8,
    accessXOptions: u16,
    affectEnabledControls: u32,
    enabledControls: u32,
    changeControls: u32,
    repeatDelay: u16,
    repeatInterval: u16,
    slowKeysDelay: u16,
    debounceDelay: u16,
    mouseKeysDelay: u16,
    mouseKeysInterval: u16,
    mouseKeysTimeToMax: u16,
    mouseKeysMaxSpeed: u16,
    mouseKeysCurve: i16,
    accessXTimeout: u16,
    accessXTimeoutMask: u32,
    accessXTimeoutValues: u32,
    accessXTimeoutOptionsMask: u16,
    accessXTimeoutOptionsValues: u16,
    perKeyRepeat: [32]u8,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u16, self.deviceSpec);
        writer.writeByte(self.affectInternalRealMods);
        writer.writeByte(self.internalRealMods);
        writer.writeByte(self.affectIgnoreLockRealMods);
        writer.writeByte(self.ignoreLockRealMods);
        writer.writeInt(u16, self.affectInternalVirtualMods);
        writer.writeInt(u16, self.internalVirtualMods);
        writer.writeInt(u16, self.affectIgnoreLockVirtualMods);
        writer.writeInt(u16, self.ignoreLockVirtualMods);
        writer.writeByte(self.mouseKeysDfltBtn);
        writer.writeByte(self.groupsWrap);
        writer.writeInt(u16, self.accessXOptions);
        writer.splatByte(0, 2);
        writer.writeInt(u32, self.affectEnabledControls);
        writer.writeInt(u32, self.enabledControls);
        writer.writeInt(u32, self.changeControls);
        writer.writeInt(u16, self.repeatDelay);
        writer.writeInt(u16, self.repeatInterval);
        writer.writeInt(u16, self.slowKeysDelay);
        writer.writeInt(u16, self.debounceDelay);
        writer.writeInt(u16, self.mouseKeysDelay);
        writer.writeInt(u16, self.mouseKeysInterval);
        writer.writeInt(u16, self.mouseKeysTimeToMax);
        writer.writeInt(u16, self.mouseKeysMaxSpeed);
        writer.writeInt(i16, self.mouseKeysCurve);
        writer.writeInt(u16, self.accessXTimeout);
        writer.writeInt(u32, self.accessXTimeoutMask);
        writer.writeInt(u32, self.accessXTimeoutValues);
        writer.writeInt(u16, self.accessXTimeoutOptionsMask);
        writer.writeInt(u16, self.accessXTimeoutOptionsValues);
        writer.write(self.perKeyRepeat[0..]);
    }
};

pub const GetMap = struct {
    pub const opcode: u8 = 8;

    deviceSpec: u16,
    full: u16,
    partial: u16,
    firstType: u8,
    nTypes: u8,
    firstKeySym: u8,
    nKeySyms: u8,
    firstKeyAction: u8,
    nKeyActions: u8,
    firstKeyBehavior: u8,
    nKeyBehaviors: u8,
    virtualMods: u16,
    firstKeyExplicit: u8,
    nKeyExplicit: u8,
    firstModMapKey: u8,
    nModMapKeys: u8,
    firstVModMapKey: u8,
    nVModMapKeys: u8,

    pub const Reply = struct {
        deviceID: u8,
        minKeyCode: u8,
        maxKeyCode: u8,
        firstType: u8,
        nTypes: u8,
        totalTypes: u8,
        firstKeySym: u8,
        totalSyms: u16,
        nKeySyms: u8,
        firstKeyAction: u8,
        totalActions: u16,
        nKeyActions: u8,
        firstKeyBehavior: u8,
        nKeyBehaviors: u8,
        totalKeyBehaviors: u8,
        firstKeyExplicit: u8,
        nKeyExplicit: u8,
        totalKeyExplicit: u8,
        firstModMapKey: u8,
        nModMapKeys: u8,
        totalModMapKeys: u8,
        firstVModMapKey: u8,
        nVModMapKeys: u8,
        totalVModMapKeys: u8,
        virtualMods: u16,
        map: Map,

        pub const Map = struct {
            types_rtrn: ?struct {
                types_rtrn: []const KeyType,
                decoded_types_rtrn_buf: ?[]KeyType = null,
            } = null,
            syms_rtrn: ?struct {
                syms_rtrn: []const KeySymMap,
                decoded_syms_rtrn_buf: ?[]KeySymMap = null,
            } = null,
            KeyActions: ?struct {
                acts_rtrn_count: []const u8,
                decoded_acts_rtrn_count_buf: ?[]u8 = null,
                acts_rtrn_acts: []const Action,
                decoded_acts_rtrn_acts_buf: ?[]Action = null,
            } = null,
            behaviors_rtrn: ?struct {
                behaviors_rtrn: []const SetBehavior,
                decoded_behaviors_rtrn_buf: ?[]SetBehavior = null,
            } = null,
            VirtualMods: ?struct {
                vmods_rtrn: []const u8,
                decoded_vmods_rtrn_buf: ?[]u8 = null,
            } = null,
            ExplicitComponents: ?struct {
                explicit_rtrn: []const SetExplicit,
                decoded_explicit_rtrn_buf: ?[]SetExplicit = null,
            } = null,
            ModifierMap: ?struct {
                modmap_rtrn: []const KeyModMap,
                decoded_modmap_rtrn_buf: ?[]KeyModMap = null,
            } = null,
            vmodmap_rtrn: ?struct {
                vmodmap_rtrn: []const KeyVModMap,
                decoded_vmodmap_rtrn_buf: ?[]KeyVModMap = null,
            } = null,

            pub fn encode(self: *const @This(), writer: anytype) !void {
                if (self.types_rtrn) |it| {
                    for (it.types_rtrn) |elem| {
                        try elem.encode(writer);
                    }
                }
                if (self.syms_rtrn) |it| {
                    for (it.syms_rtrn) |elem| {
                        try elem.encode(writer);
                    }
                }
                if (self.KeyActions) |it| {
                    writer.write(it.acts_rtrn_count);
                    writer.splatByte(0, wire.pad(writer.seek, 4));
                    for (it.acts_rtrn_acts) |elem| {
                        try elem.encode(writer);
                    }
                }
                if (self.behaviors_rtrn) |it| {
                    for (it.behaviors_rtrn) |elem| {
                        try elem.encode(writer);
                    }
                }
                if (self.VirtualMods) |it| {
                    writer.write(it.vmods_rtrn);
                    writer.splatByte(0, wire.pad(writer.seek, 4));
                }
                if (self.ExplicitComponents) |it| {
                    for (it.explicit_rtrn) |elem| {
                        try elem.encode(writer);
                    }
                    writer.splatByte(0, wire.pad(writer.seek, 4));
                }
                if (self.ModifierMap) |it| {
                    for (it.modmap_rtrn) |elem| {
                        try elem.encode(writer);
                    }
                    writer.splatByte(0, wire.pad(writer.seek, 4));
                }
                if (self.vmodmap_rtrn) |it| {
                    for (it.vmodmap_rtrn) |elem| {
                        try elem.encode(writer);
                    }
                }
            }

            pub fn switchValue(self: *const @This()) u32 {
                var result: u32 = 0;
                if (self.types_rtrn != null) result |= 1;
                if (self.syms_rtrn != null) result |= 2;
                if (self.KeyActions != null) result |= 16;
                if (self.behaviors_rtrn != null) result |= 32;
                if (self.VirtualMods != null) result |= 64;
                if (self.ExplicitComponents != null) result |= 8;
                if (self.ModifierMap != null) result |= 4;
                if (self.vmodmap_rtrn != null) result |= 128;
                return result;
            }

            pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader, switch_value: u32, nTypes: u8, nKeySyms: u8, nKeyActions: u8, totalActions: u16, totalKeyBehaviors: u8, totalKeyExplicit: u8, totalModMapKeys: u8, totalVModMapKeys: u8) !@This() {
                var result: @This() = .{};
                if ((switch_value & 1) != 0) {
                    var payload: @typeInfo(@TypeOf(result.types_rtrn)).optional.child = undefined;
                    const decoded_types_rtrn_buf = try allocator.alloc(KeyType, @intCast(nTypes));
                    for (decoded_types_rtrn_buf) |*elem| {
                        elem.* = try KeyType.decode(allocator, reader);
                    }
                    payload.types_rtrn = decoded_types_rtrn_buf;
                    payload.decoded_types_rtrn_buf = decoded_types_rtrn_buf;
                    result.types_rtrn = payload;
                }
                if ((switch_value & 2) != 0) {
                    var payload: @typeInfo(@TypeOf(result.syms_rtrn)).optional.child = undefined;
                    const decoded_syms_rtrn_buf = try allocator.alloc(KeySymMap, @intCast(nKeySyms));
                    for (decoded_syms_rtrn_buf) |*elem| {
                        elem.* = try KeySymMap.decode(allocator, reader);
                    }
                    payload.syms_rtrn = decoded_syms_rtrn_buf;
                    payload.decoded_syms_rtrn_buf = decoded_syms_rtrn_buf;
                    result.syms_rtrn = payload;
                }
                if ((switch_value & 16) != 0) {
                    var payload: @typeInfo(@TypeOf(result.KeyActions)).optional.child = undefined;
                    const decoded_acts_rtrn_count_buf = try allocator.dupe(u8, try reader.take(@intCast(nKeyActions)));
                    payload.acts_rtrn_count = decoded_acts_rtrn_count_buf;
                    payload.decoded_acts_rtrn_count_buf = decoded_acts_rtrn_count_buf;
                    _ = try reader.take(wire.pad(reader.seek, 4));
                    const decoded_acts_rtrn_acts_buf = try allocator.alloc(Action, @intCast(totalActions));
                    for (decoded_acts_rtrn_acts_buf) |*elem| {
                        elem.* = try Action.decode(reader);
                    }
                    payload.acts_rtrn_acts = decoded_acts_rtrn_acts_buf;
                    payload.decoded_acts_rtrn_acts_buf = decoded_acts_rtrn_acts_buf;
                    result.KeyActions = payload;
                }
                if ((switch_value & 32) != 0) {
                    var payload: @typeInfo(@TypeOf(result.behaviors_rtrn)).optional.child = undefined;
                    const decoded_behaviors_rtrn_buf = try allocator.alloc(SetBehavior, @intCast(totalKeyBehaviors));
                    for (decoded_behaviors_rtrn_buf) |*elem| {
                        elem.* = try SetBehavior.decode(reader);
                    }
                    payload.behaviors_rtrn = decoded_behaviors_rtrn_buf;
                    payload.decoded_behaviors_rtrn_buf = decoded_behaviors_rtrn_buf;
                    result.behaviors_rtrn = payload;
                }
                if ((switch_value & 64) != 0) {
                    var payload: @typeInfo(@TypeOf(result.VirtualMods)).optional.child = undefined;
                    const decoded_vmods_rtrn_buf = try allocator.dupe(u8, try reader.take(@intCast(@popCount(payload.virtualMods))));
                    payload.vmods_rtrn = decoded_vmods_rtrn_buf;
                    payload.decoded_vmods_rtrn_buf = decoded_vmods_rtrn_buf;
                    _ = try reader.take(wire.pad(reader.seek, 4));
                    result.VirtualMods = payload;
                }
                if ((switch_value & 8) != 0) {
                    var payload: @typeInfo(@TypeOf(result.ExplicitComponents)).optional.child = undefined;
                    const decoded_explicit_rtrn_buf = try allocator.alloc(SetExplicit, @intCast(totalKeyExplicit));
                    for (decoded_explicit_rtrn_buf) |*elem| {
                        elem.* = try SetExplicit.decode(reader);
                    }
                    payload.explicit_rtrn = decoded_explicit_rtrn_buf;
                    payload.decoded_explicit_rtrn_buf = decoded_explicit_rtrn_buf;
                    _ = try reader.take(wire.pad(reader.seek, 4));
                    result.ExplicitComponents = payload;
                }
                if ((switch_value & 4) != 0) {
                    var payload: @typeInfo(@TypeOf(result.ModifierMap)).optional.child = undefined;
                    const decoded_modmap_rtrn_buf = try allocator.alloc(KeyModMap, @intCast(totalModMapKeys));
                    for (decoded_modmap_rtrn_buf) |*elem| {
                        elem.* = try KeyModMap.decode(reader);
                    }
                    payload.modmap_rtrn = decoded_modmap_rtrn_buf;
                    payload.decoded_modmap_rtrn_buf = decoded_modmap_rtrn_buf;
                    _ = try reader.take(wire.pad(reader.seek, 4));
                    result.ModifierMap = payload;
                }
                if ((switch_value & 128) != 0) {
                    var payload: @typeInfo(@TypeOf(result.vmodmap_rtrn)).optional.child = undefined;
                    const decoded_vmodmap_rtrn_buf = try allocator.alloc(KeyVModMap, @intCast(totalVModMapKeys));
                    for (decoded_vmodmap_rtrn_buf) |*elem| {
                        elem.* = try KeyVModMap.decode(reader);
                    }
                    payload.vmodmap_rtrn = decoded_vmodmap_rtrn_buf;
                    payload.decoded_vmodmap_rtrn_buf = decoded_vmodmap_rtrn_buf;
                    result.vmodmap_rtrn = payload;
                }
                return result;
            }

            pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
                if (self.types_rtrn) |*payload| {
                    if (payload.decoded_types_rtrn_buf) |buf| {
                        for (buf) |*it| {
                            it.deinit(allocator);
                        }
                        allocator.free(buf);
                        payload.decoded_types_rtrn_buf = null;
                        payload.types_rtrn = &.{};
                    }
                }
                if (self.syms_rtrn) |*payload| {
                    if (payload.decoded_syms_rtrn_buf) |buf| {
                        for (buf) |*it| {
                            it.deinit(allocator);
                        }
                        allocator.free(buf);
                        payload.decoded_syms_rtrn_buf = null;
                        payload.syms_rtrn = &.{};
                    }
                }
                if (self.KeyActions) |*payload| {
                    if (payload.decoded_acts_rtrn_count_buf) |buf| {
                        allocator.free(buf);
                        payload.decoded_acts_rtrn_count_buf = null;
                        payload.acts_rtrn_count = &.{};
                    }
                    if (payload.decoded_acts_rtrn_acts_buf) |buf| {
                        allocator.free(buf);
                        payload.decoded_acts_rtrn_acts_buf = null;
                        payload.acts_rtrn_acts = &.{};
                    }
                }
                if (self.behaviors_rtrn) |*payload| {
                    if (payload.decoded_behaviors_rtrn_buf) |buf| {
                        allocator.free(buf);
                        payload.decoded_behaviors_rtrn_buf = null;
                        payload.behaviors_rtrn = &.{};
                    }
                }
                if (self.VirtualMods) |*payload| {
                    if (payload.decoded_vmods_rtrn_buf) |buf| {
                        allocator.free(buf);
                        payload.decoded_vmods_rtrn_buf = null;
                        payload.vmods_rtrn = &.{};
                    }
                }
                if (self.ExplicitComponents) |*payload| {
                    if (payload.decoded_explicit_rtrn_buf) |buf| {
                        allocator.free(buf);
                        payload.decoded_explicit_rtrn_buf = null;
                        payload.explicit_rtrn = &.{};
                    }
                }
                if (self.ModifierMap) |*payload| {
                    if (payload.decoded_modmap_rtrn_buf) |buf| {
                        allocator.free(buf);
                        payload.decoded_modmap_rtrn_buf = null;
                        payload.modmap_rtrn = &.{};
                    }
                }
                if (self.vmodmap_rtrn) |*payload| {
                    if (payload.decoded_vmodmap_rtrn_buf) |buf| {
                        allocator.free(buf);
                        payload.decoded_vmodmap_rtrn_buf = null;
                        payload.vmodmap_rtrn = &.{};
                    }
                }
            }
        };

        pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader, header_: wire.ReplyHeader) !@This() {
            var result: @This() = undefined;
            _ = header_;
            result.deviceID = try reader.takeByte();
            _ = try reader.take(2);
            result.minKeyCode = try reader.takeByte();
            result.maxKeyCode = try reader.takeByte();
            const present = try reader.takeInt(u16, .native);
            result.firstType = try reader.takeByte();
            result.nTypes = try reader.takeByte();
            result.totalTypes = try reader.takeByte();
            result.firstKeySym = try reader.takeByte();
            result.totalSyms = try reader.takeInt(u16, .native);
            result.nKeySyms = try reader.takeByte();
            result.firstKeyAction = try reader.takeByte();
            result.totalActions = try reader.takeInt(u16, .native);
            result.nKeyActions = try reader.takeByte();
            result.firstKeyBehavior = try reader.takeByte();
            result.nKeyBehaviors = try reader.takeByte();
            result.totalKeyBehaviors = try reader.takeByte();
            result.firstKeyExplicit = try reader.takeByte();
            result.nKeyExplicit = try reader.takeByte();
            result.totalKeyExplicit = try reader.takeByte();
            result.firstModMapKey = try reader.takeByte();
            result.nModMapKeys = try reader.takeByte();
            result.totalModMapKeys = try reader.takeByte();
            result.firstVModMapKey = try reader.takeByte();
            result.nVModMapKeys = try reader.takeByte();
            result.totalVModMapKeys = try reader.takeByte();
            _ = try reader.take(1);
            result.virtualMods = try reader.takeInt(u16, .native);
            result.map = try @TypeOf(result).Map.decode(allocator, reader, present, result.nTypes, result.nKeySyms, result.nKeyActions, result.totalActions, result.totalKeyBehaviors, result.totalKeyExplicit, result.totalModMapKeys, result.totalVModMapKeys);
            return result;
        }

        pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
            self.map.deinit(allocator);
        }
    };


    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u16, self.deviceSpec);
        writer.writeInt(u16, self.full);
        writer.writeInt(u16, self.partial);
        writer.writeByte(self.firstType);
        writer.writeByte(self.nTypes);
        writer.writeByte(self.firstKeySym);
        writer.writeByte(self.nKeySyms);
        writer.writeByte(self.firstKeyAction);
        writer.writeByte(self.nKeyActions);
        writer.writeByte(self.firstKeyBehavior);
        writer.writeByte(self.nKeyBehaviors);
        writer.writeInt(u16, self.virtualMods);
        writer.writeByte(self.firstKeyExplicit);
        writer.writeByte(self.nKeyExplicit);
        writer.writeByte(self.firstModMapKey);
        writer.writeByte(self.nModMapKeys);
        writer.writeByte(self.firstVModMapKey);
        writer.writeByte(self.nVModMapKeys);
        writer.splatByte(0, 2);
    }
};

pub const SetMap = struct {
    pub const opcode: u8 = 9;

    deviceSpec: u16,
    flags: u16,
    minKeyCode: u8,
    maxKeyCode: u8,
    firstType: u8,
    nTypes: u8,
    firstKeySym: u8,
    nKeySyms: u8,
    totalSyms: u16,
    firstKeyAction: u8,
    nKeyActions: u8,
    totalActions: u16,
    firstKeyBehavior: u8,
    nKeyBehaviors: u8,
    totalKeyBehaviors: u8,
    firstKeyExplicit: u8,
    nKeyExplicit: u8,
    totalKeyExplicit: u8,
    firstModMapKey: u8,
    nModMapKeys: u8,
    totalModMapKeys: u8,
    firstVModMapKey: u8,
    nVModMapKeys: u8,
    totalVModMapKeys: u8,
    virtualMods: u16,
    values: Values,

    pub const Values = struct {
        types: ?struct {
            types: []const SetKeyType,
            decoded_types_buf: ?[]SetKeyType = null,
        } = null,
        syms: ?struct {
            syms: []const KeySymMap,
            decoded_syms_buf: ?[]KeySymMap = null,
        } = null,
        KeyActions: ?struct {
            actionsCount: []const u8,
            decoded_actionsCount_buf: ?[]u8 = null,
            actions: []const Action,
            decoded_actions_buf: ?[]Action = null,
        } = null,
        behaviors: ?struct {
            behaviors: []const SetBehavior,
            decoded_behaviors_buf: ?[]SetBehavior = null,
        } = null,
        VirtualMods: ?struct {
            vmods: []const u8,
            decoded_vmods_buf: ?[]u8 = null,
        } = null,
        explicit: ?struct {
            explicit: []const SetExplicit,
            decoded_explicit_buf: ?[]SetExplicit = null,
        } = null,
        modmap: ?struct {
            modmap: []const KeyModMap,
            decoded_modmap_buf: ?[]KeyModMap = null,
        } = null,
        vmodmap: ?struct {
            vmodmap: []const KeyVModMap,
            decoded_vmodmap_buf: ?[]KeyVModMap = null,
        } = null,

        pub fn encode(self: *const @This(), writer: anytype) !void {
            if (self.types) |it| {
                for (it.types) |elem| {
                    try elem.encode(writer);
                }
            }
            if (self.syms) |it| {
                for (it.syms) |elem| {
                    try elem.encode(writer);
                }
            }
            if (self.KeyActions) |it| {
                writer.write(it.actionsCount);
                writer.splatByte(0, wire.pad(writer.seek, 4));
                for (it.actions) |elem| {
                    try elem.encode(writer);
                }
            }
            if (self.behaviors) |it| {
                for (it.behaviors) |elem| {
                    try elem.encode(writer);
                }
            }
            if (self.VirtualMods) |it| {
                writer.write(it.vmods);
                writer.splatByte(0, wire.pad(writer.seek, 4));
            }
            if (self.explicit) |it| {
                for (it.explicit) |elem| {
                    try elem.encode(writer);
                }
            }
            if (self.modmap) |it| {
                for (it.modmap) |elem| {
                    try elem.encode(writer);
                }
            }
            if (self.vmodmap) |it| {
                for (it.vmodmap) |elem| {
                    try elem.encode(writer);
                }
            }
        }

        pub fn switchValue(self: *const @This()) u32 {
            var result: u32 = 0;
            if (self.types != null) result |= 1;
            if (self.syms != null) result |= 2;
            if (self.KeyActions != null) result |= 16;
            if (self.behaviors != null) result |= 32;
            if (self.VirtualMods != null) result |= 64;
            if (self.explicit != null) result |= 8;
            if (self.modmap != null) result |= 4;
            if (self.vmodmap != null) result |= 128;
            return result;
        }

        pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader, switch_value: u32, nTypes: u8, nKeySyms: u8, nKeyActions: u8, totalActions: u16, totalKeyBehaviors: u8, totalKeyExplicit: u8, totalModMapKeys: u8, totalVModMapKeys: u8) !@This() {
            var result: @This() = .{};
            if ((switch_value & 1) != 0) {
                var payload: @typeInfo(@TypeOf(result.types)).optional.child = undefined;
                const decoded_types_buf = try allocator.alloc(SetKeyType, @intCast(nTypes));
                for (decoded_types_buf) |*elem| {
                    elem.* = try SetKeyType.decode(allocator, reader);
                }
                payload.types = decoded_types_buf;
                payload.decoded_types_buf = decoded_types_buf;
                result.types = payload;
            }
            if ((switch_value & 2) != 0) {
                var payload: @typeInfo(@TypeOf(result.syms)).optional.child = undefined;
                const decoded_syms_buf = try allocator.alloc(KeySymMap, @intCast(nKeySyms));
                for (decoded_syms_buf) |*elem| {
                    elem.* = try KeySymMap.decode(allocator, reader);
                }
                payload.syms = decoded_syms_buf;
                payload.decoded_syms_buf = decoded_syms_buf;
                result.syms = payload;
            }
            if ((switch_value & 16) != 0) {
                var payload: @typeInfo(@TypeOf(result.KeyActions)).optional.child = undefined;
                const decoded_actionsCount_buf = try allocator.dupe(u8, try reader.take(@intCast(nKeyActions)));
                payload.actionsCount = decoded_actionsCount_buf;
                payload.decoded_actionsCount_buf = decoded_actionsCount_buf;
                _ = try reader.take(wire.pad(reader.seek, 4));
                const decoded_actions_buf = try allocator.alloc(Action, @intCast(totalActions));
                for (decoded_actions_buf) |*elem| {
                    elem.* = try Action.decode(reader);
                }
                payload.actions = decoded_actions_buf;
                payload.decoded_actions_buf = decoded_actions_buf;
                result.KeyActions = payload;
            }
            if ((switch_value & 32) != 0) {
                var payload: @typeInfo(@TypeOf(result.behaviors)).optional.child = undefined;
                const decoded_behaviors_buf = try allocator.alloc(SetBehavior, @intCast(totalKeyBehaviors));
                for (decoded_behaviors_buf) |*elem| {
                    elem.* = try SetBehavior.decode(reader);
                }
                payload.behaviors = decoded_behaviors_buf;
                payload.decoded_behaviors_buf = decoded_behaviors_buf;
                result.behaviors = payload;
            }
            if ((switch_value & 64) != 0) {
                var payload: @typeInfo(@TypeOf(result.VirtualMods)).optional.child = undefined;
                const decoded_vmods_buf = try allocator.dupe(u8, try reader.take(@intCast(@popCount(payload.virtualMods))));
                payload.vmods = decoded_vmods_buf;
                payload.decoded_vmods_buf = decoded_vmods_buf;
                _ = try reader.take(wire.pad(reader.seek, 4));
                result.VirtualMods = payload;
            }
            if ((switch_value & 8) != 0) {
                var payload: @typeInfo(@TypeOf(result.explicit)).optional.child = undefined;
                const decoded_explicit_buf = try allocator.alloc(SetExplicit, @intCast(totalKeyExplicit));
                for (decoded_explicit_buf) |*elem| {
                    elem.* = try SetExplicit.decode(reader);
                }
                payload.explicit = decoded_explicit_buf;
                payload.decoded_explicit_buf = decoded_explicit_buf;
                result.explicit = payload;
            }
            if ((switch_value & 4) != 0) {
                var payload: @typeInfo(@TypeOf(result.modmap)).optional.child = undefined;
                const decoded_modmap_buf = try allocator.alloc(KeyModMap, @intCast(totalModMapKeys));
                for (decoded_modmap_buf) |*elem| {
                    elem.* = try KeyModMap.decode(reader);
                }
                payload.modmap = decoded_modmap_buf;
                payload.decoded_modmap_buf = decoded_modmap_buf;
                result.modmap = payload;
            }
            if ((switch_value & 128) != 0) {
                var payload: @typeInfo(@TypeOf(result.vmodmap)).optional.child = undefined;
                const decoded_vmodmap_buf = try allocator.alloc(KeyVModMap, @intCast(totalVModMapKeys));
                for (decoded_vmodmap_buf) |*elem| {
                    elem.* = try KeyVModMap.decode(reader);
                }
                payload.vmodmap = decoded_vmodmap_buf;
                payload.decoded_vmodmap_buf = decoded_vmodmap_buf;
                result.vmodmap = payload;
            }
            return result;
        }

        pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
            if (self.types) |*payload| {
                if (payload.decoded_types_buf) |buf| {
                    for (buf) |*it| {
                        it.deinit(allocator);
                    }
                    allocator.free(buf);
                    payload.decoded_types_buf = null;
                    payload.types = &.{};
                }
            }
            if (self.syms) |*payload| {
                if (payload.decoded_syms_buf) |buf| {
                    for (buf) |*it| {
                        it.deinit(allocator);
                    }
                    allocator.free(buf);
                    payload.decoded_syms_buf = null;
                    payload.syms = &.{};
                }
            }
            if (self.KeyActions) |*payload| {
                if (payload.decoded_actionsCount_buf) |buf| {
                    allocator.free(buf);
                    payload.decoded_actionsCount_buf = null;
                    payload.actionsCount = &.{};
                }
                if (payload.decoded_actions_buf) |buf| {
                    allocator.free(buf);
                    payload.decoded_actions_buf = null;
                    payload.actions = &.{};
                }
            }
            if (self.behaviors) |*payload| {
                if (payload.decoded_behaviors_buf) |buf| {
                    allocator.free(buf);
                    payload.decoded_behaviors_buf = null;
                    payload.behaviors = &.{};
                }
            }
            if (self.VirtualMods) |*payload| {
                if (payload.decoded_vmods_buf) |buf| {
                    allocator.free(buf);
                    payload.decoded_vmods_buf = null;
                    payload.vmods = &.{};
                }
            }
            if (self.explicit) |*payload| {
                if (payload.decoded_explicit_buf) |buf| {
                    allocator.free(buf);
                    payload.decoded_explicit_buf = null;
                    payload.explicit = &.{};
                }
            }
            if (self.modmap) |*payload| {
                if (payload.decoded_modmap_buf) |buf| {
                    allocator.free(buf);
                    payload.decoded_modmap_buf = null;
                    payload.modmap = &.{};
                }
            }
            if (self.vmodmap) |*payload| {
                if (payload.decoded_vmodmap_buf) |buf| {
                    allocator.free(buf);
                    payload.decoded_vmodmap_buf = null;
                    payload.vmodmap = &.{};
                }
            }
        }
    };

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u16, self.deviceSpec);
        writer.writeInt(u16, @intCast(self.values.switchValue()));
        writer.writeInt(u16, self.flags);
        writer.writeByte(self.minKeyCode);
        writer.writeByte(self.maxKeyCode);
        writer.writeByte(self.firstType);
        writer.writeByte(self.nTypes);
        writer.writeByte(self.firstKeySym);
        writer.writeByte(self.nKeySyms);
        writer.writeInt(u16, self.totalSyms);
        writer.writeByte(self.firstKeyAction);
        writer.writeByte(self.nKeyActions);
        writer.writeInt(u16, self.totalActions);
        writer.writeByte(self.firstKeyBehavior);
        writer.writeByte(self.nKeyBehaviors);
        writer.writeByte(self.totalKeyBehaviors);
        writer.writeByte(self.firstKeyExplicit);
        writer.writeByte(self.nKeyExplicit);
        writer.writeByte(self.totalKeyExplicit);
        writer.writeByte(self.firstModMapKey);
        writer.writeByte(self.nModMapKeys);
        writer.writeByte(self.totalModMapKeys);
        writer.writeByte(self.firstVModMapKey);
        writer.writeByte(self.nVModMapKeys);
        writer.writeByte(self.totalVModMapKeys);
        writer.writeInt(u16, self.virtualMods);
        try self.values.encode(writer);
    }
};

pub const GetCompatMap = struct {
    pub const opcode: u8 = 10;

    deviceSpec: u16,
    groups: u8,
    getAllSI: bool,
    firstSI: u16,
    nSI: u16,

    pub const Reply = struct {
        deviceID: u8,
        groupsRtrn: u8,
        firstSIRtrn: u16,
        nTotalSI: u16,
        si_rtrn: []const SymInterpret,
        decoded_si_rtrn_buf: ?[]SymInterpret = null,
        group_rtrn: []const ModDef,
        decoded_group_rtrn_buf: ?[]ModDef = null,

        pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader, header_: wire.ReplyHeader) !@This() {
            var result: @This() = undefined;
            _ = header_;
            result.deviceID = try reader.takeByte();
            result.groupsRtrn = try reader.takeByte();
            _ = try reader.take(1);
            result.firstSIRtrn = try reader.takeInt(u16, .native);
            const nSIRtrn = try reader.takeInt(u16, .native);
            result.nTotalSI = try reader.takeInt(u16, .native);
            _ = try reader.take(16);
            const decoded_si_rtrn_buf = try allocator.alloc(SymInterpret, @intCast(nSIRtrn));
            for (decoded_si_rtrn_buf) |*elem| {
                elem.* = try SymInterpret.decode(reader);
            }
            result.si_rtrn = decoded_si_rtrn_buf;
            result.decoded_si_rtrn_buf = decoded_si_rtrn_buf;
            const decoded_group_rtrn_buf = try allocator.alloc(ModDef, @intCast(@popCount(result.groupsRtrn)));
            for (decoded_group_rtrn_buf) |*elem| {
                elem.* = try ModDef.decode(reader);
            }
            result.group_rtrn = decoded_group_rtrn_buf;
            result.decoded_group_rtrn_buf = decoded_group_rtrn_buf;
            return result;
        }

        pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
            if (self.decoded_si_rtrn_buf) |buf| {
                allocator.free(buf);
                self.decoded_si_rtrn_buf = null;
                self.si_rtrn = &.{};
            }
            if (self.decoded_group_rtrn_buf) |buf| {
                allocator.free(buf);
                self.decoded_group_rtrn_buf = null;
                self.group_rtrn = &.{};
            }
        }
    };


    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u16, self.deviceSpec);
        writer.writeByte(self.groups);
        writer.writeByte(@intFromBool(self.getAllSI));
        writer.writeInt(u16, self.firstSI);
        writer.writeInt(u16, self.nSI);
    }
};

pub const SetCompatMap = struct {
    pub const opcode: u8 = 11;

    deviceSpec: u16,
    recomputeActions: bool,
    truncateSI: bool,
    groups: u8,
    firstSI: u16,
    si: []const SymInterpret,
    decoded_si_buf: ?[]SymInterpret = null,
    groupMaps: []const ModDef,
    decoded_groupMaps_buf: ?[]ModDef = null,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u16, self.deviceSpec);
        writer.splatByte(0, 1);
        writer.writeByte(@intFromBool(self.recomputeActions));
        writer.writeByte(@intFromBool(self.truncateSI));
        writer.writeByte(self.groups);
        writer.writeInt(u16, self.firstSI);
        writer.writeInt(u16, @intCast(self.si.len));
        writer.splatByte(0, 2);
        for (self.si) |elem| {
            try elem.encode(writer);
        }
        for (self.groupMaps) |elem| {
            try elem.encode(writer);
        }
    }
};

pub const GetIndicatorState = struct {
    pub const opcode: u8 = 12;

    deviceSpec: u16,

    pub const Reply = struct {
        deviceID: u8,
        state: u32,

        pub fn decode(reader: *std.Io.Reader, header_: wire.ReplyHeader) !@This() {
            var result: @This() = undefined;
            _ = header_;
            result.deviceID = try reader.takeByte();
            result.state = try reader.takeInt(u32, .native);
            _ = try reader.take(20);
            return result;
        }
    };


    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u16, self.deviceSpec);
        writer.splatByte(0, 2);
    }
};

pub const GetIndicatorMap = struct {
    pub const opcode: u8 = 13;

    deviceSpec: u16,
    which: u32,

    pub const Reply = struct {
        deviceID: u8,
        which: u32,
        realIndicators: u32,
        nIndicators: u8,
        maps: []const IndicatorMap,
        decoded_maps_buf: ?[]IndicatorMap = null,

        pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader, header_: wire.ReplyHeader) !@This() {
            var result: @This() = undefined;
            _ = header_;
            result.deviceID = try reader.takeByte();
            result.which = try reader.takeInt(u32, .native);
            result.realIndicators = try reader.takeInt(u32, .native);
            result.nIndicators = try reader.takeByte();
            _ = try reader.take(15);
            const decoded_maps_buf = try allocator.alloc(IndicatorMap, @intCast(@popCount(result.which)));
            for (decoded_maps_buf) |*elem| {
                elem.* = try IndicatorMap.decode(reader);
            }
            result.maps = decoded_maps_buf;
            result.decoded_maps_buf = decoded_maps_buf;
            return result;
        }

        pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
            if (self.decoded_maps_buf) |buf| {
                allocator.free(buf);
                self.decoded_maps_buf = null;
                self.maps = &.{};
            }
        }
    };


    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u16, self.deviceSpec);
        writer.splatByte(0, 2);
        writer.writeInt(u32, self.which);
    }
};

pub const SetIndicatorMap = struct {
    pub const opcode: u8 = 14;

    deviceSpec: u16,
    which: u32,
    maps: []const IndicatorMap,
    decoded_maps_buf: ?[]IndicatorMap = null,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u16, self.deviceSpec);
        writer.splatByte(0, 2);
        writer.writeInt(u32, self.which);
        for (self.maps) |elem| {
            try elem.encode(writer);
        }
    }
};

pub const GetNamedIndicator = struct {
    pub const opcode: u8 = 15;

    deviceSpec: u16,
    ledClass: LedClass,
    ledID: u16,
    indicator: xproto.Atom,

    pub const Reply = struct {
        deviceID: u8,
        indicator: xproto.Atom,
        found: bool,
        on: bool,
        realIndicator: bool,
        ndx: u8,
        map_flags: u8,
        map_whichGroups: u8,
        map_groups: u8,
        map_whichMods: u8,
        map_mods: u8,
        map_realMods: u8,
        map_vmod: u16,
        map_ctrls: u32,
        supported: bool,

        pub fn decode(reader: *std.Io.Reader, header_: wire.ReplyHeader) !@This() {
            var result: @This() = undefined;
            _ = header_;
            result.deviceID = try reader.takeByte();
            result.indicator = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
            result.found = (try reader.takeByte()) != 0;
            result.on = (try reader.takeByte()) != 0;
            result.realIndicator = (try reader.takeByte()) != 0;
            result.ndx = try reader.takeByte();
            result.map_flags = try reader.takeByte();
            result.map_whichGroups = try reader.takeByte();
            result.map_groups = try reader.takeByte();
            result.map_whichMods = try reader.takeByte();
            result.map_mods = try reader.takeByte();
            result.map_realMods = try reader.takeByte();
            result.map_vmod = try reader.takeInt(u16, .native);
            result.map_ctrls = try reader.takeInt(u32, .native);
            result.supported = (try reader.takeByte()) != 0;
            _ = try reader.take(3);
            return result;
        }
    };


    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u16, self.deviceSpec);
        writer.writeInt(u16, @intCast(@intFromEnum(self.ledClass)));
        writer.writeInt(u16, self.ledID);
        writer.splatByte(0, 2);
        writer.writeInt(u32, @intCast(@intFromEnum(self.indicator)));
    }
};

pub const SetNamedIndicator = struct {
    pub const opcode: u8 = 16;

    deviceSpec: u16,
    ledClass: LedClass,
    ledID: u16,
    indicator: xproto.Atom,
    setState: bool,
    on: bool,
    setMap: bool,
    createMap: bool,
    map_flags: u8,
    map_whichGroups: u8,
    map_groups: u8,
    map_whichMods: u8,
    map_realMods: u8,
    map_vmods: u16,
    map_ctrls: u32,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u16, self.deviceSpec);
        writer.writeInt(u16, @intCast(@intFromEnum(self.ledClass)));
        writer.writeInt(u16, self.ledID);
        writer.splatByte(0, 2);
        writer.writeInt(u32, @intCast(@intFromEnum(self.indicator)));
        writer.writeByte(@intFromBool(self.setState));
        writer.writeByte(@intFromBool(self.on));
        writer.writeByte(@intFromBool(self.setMap));
        writer.writeByte(@intFromBool(self.createMap));
        writer.splatByte(0, 1);
        writer.writeByte(self.map_flags);
        writer.writeByte(self.map_whichGroups);
        writer.writeByte(self.map_groups);
        writer.writeByte(self.map_whichMods);
        writer.writeByte(self.map_realMods);
        writer.writeInt(u16, self.map_vmods);
        writer.writeInt(u32, self.map_ctrls);
    }
};

pub const GetNames = struct {
    pub const opcode: u8 = 17;

    deviceSpec: u16,
    which: u32,

    pub const Reply = struct {
        deviceID: u8,
        minKeyCode: u8,
        maxKeyCode: u8,
        nTypes: u8,
        groupNames: u8,
        virtualMods: u16,
        firstKey: u8,
        nKeys: u8,
        indicators: u32,
        nRadioGroups: u8,
        nKeyAliases: u8,
        nKTLevels: u16,
        valueList: ValueList,

        pub const ValueList = struct {
            keycodesName: ?xproto.Atom = null,
            geometryName: ?xproto.Atom = null,
            symbolsName: ?xproto.Atom = null,
            physSymbolsName: ?xproto.Atom = null,
            typesName: ?xproto.Atom = null,
            compatName: ?xproto.Atom = null,
            typeNames: ?struct {
                typeNames: []const xproto.Atom,
                decoded_typeNames_buf: ?[]xproto.Atom = null,
            } = null,
            KTLevelNames: ?struct {
                nLevelsPerType: []const u8,
                decoded_nLevelsPerType_buf: ?[]u8 = null,
                ktLevelNames: []const xproto.Atom,
                decoded_ktLevelNames_buf: ?[]xproto.Atom = null,
            } = null,
            indicatorNames: ?struct {
                indicatorNames: []const xproto.Atom,
                decoded_indicatorNames_buf: ?[]xproto.Atom = null,
            } = null,
            virtualModNames: ?struct {
                virtualModNames: []const xproto.Atom,
                decoded_virtualModNames_buf: ?[]xproto.Atom = null,
            } = null,
            groups: ?struct {
                groups: []const xproto.Atom,
                decoded_groups_buf: ?[]xproto.Atom = null,
            } = null,
            keyNames: ?struct {
                keyNames: []const KeyName,
                decoded_keyNames_buf: ?[]KeyName = null,
            } = null,
            keyAliases: ?struct {
                keyAliases: []const KeyAlias,
                decoded_keyAliases_buf: ?[]KeyAlias = null,
            } = null,
            radioGroupNames: ?struct {
                radioGroupNames: []const xproto.Atom,
                decoded_radioGroupNames_buf: ?[]xproto.Atom = null,
            } = null,

            pub fn encode(self: *const @This(), writer: anytype) !void {
                if (self.keycodesName) |it| {
                    writer.writeInt(u32, @intCast(@intFromEnum(it)));
                }
                if (self.geometryName) |it| {
                    writer.writeInt(u32, @intCast(@intFromEnum(it)));
                }
                if (self.symbolsName) |it| {
                    writer.writeInt(u32, @intCast(@intFromEnum(it)));
                }
                if (self.physSymbolsName) |it| {
                    writer.writeInt(u32, @intCast(@intFromEnum(it)));
                }
                if (self.typesName) |it| {
                    writer.writeInt(u32, @intCast(@intFromEnum(it)));
                }
                if (self.compatName) |it| {
                    writer.writeInt(u32, @intCast(@intFromEnum(it)));
                }
                if (self.typeNames) |it| {
                    for (it.typeNames) |elem| {
                        writer.writeInt(u32, @intCast(@intFromEnum(elem)));
                    }
                }
                if (self.KTLevelNames) |it| {
                    writer.write(it.nLevelsPerType);
                    writer.splatByte(0, wire.pad(writer.seek, 4));
                    for (it.ktLevelNames) |elem| {
                        writer.writeInt(u32, @intCast(@intFromEnum(elem)));
                    }
                }
                if (self.indicatorNames) |it| {
                    for (it.indicatorNames) |elem| {
                        writer.writeInt(u32, @intCast(@intFromEnum(elem)));
                    }
                }
                if (self.virtualModNames) |it| {
                    for (it.virtualModNames) |elem| {
                        writer.writeInt(u32, @intCast(@intFromEnum(elem)));
                    }
                }
                if (self.groups) |it| {
                    for (it.groups) |elem| {
                        writer.writeInt(u32, @intCast(@intFromEnum(elem)));
                    }
                }
                if (self.keyNames) |it| {
                    for (it.keyNames) |elem| {
                        try elem.encode(writer);
                    }
                }
                if (self.keyAliases) |it| {
                    for (it.keyAliases) |elem| {
                        try elem.encode(writer);
                    }
                }
                if (self.radioGroupNames) |it| {
                    for (it.radioGroupNames) |elem| {
                        writer.writeInt(u32, @intCast(@intFromEnum(elem)));
                    }
                }
            }

            pub fn switchValue(self: *const @This()) u32 {
                var result: u32 = 0;
                if (self.keycodesName != null) result |= 1;
                if (self.geometryName != null) result |= 2;
                if (self.symbolsName != null) result |= 4;
                if (self.physSymbolsName != null) result |= 8;
                if (self.typesName != null) result |= 16;
                if (self.compatName != null) result |= 32;
                if (self.typeNames != null) result |= 64;
                if (self.KTLevelNames != null) result |= 128;
                if (self.indicatorNames != null) result |= 256;
                if (self.virtualModNames != null) result |= 2048;
                if (self.groups != null) result |= 4096;
                if (self.keyNames != null) result |= 512;
                if (self.keyAliases != null) result |= 1024;
                if (self.radioGroupNames != null) result |= 8192;
                return result;
            }

            pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader, switch_value: u32, nTypes: u8, nKeys: u8, nKeyAliases: u8, nRadioGroups: u8) !@This() {
                var result: @This() = .{};
                if ((switch_value & 1) != 0) {
                    const keycodesName = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
                    result.keycodesName = keycodesName;
                }
                if ((switch_value & 2) != 0) {
                    const geometryName = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
                    result.geometryName = geometryName;
                }
                if ((switch_value & 4) != 0) {
                    const symbolsName = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
                    result.symbolsName = symbolsName;
                }
                if ((switch_value & 8) != 0) {
                    const physSymbolsName = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
                    result.physSymbolsName = physSymbolsName;
                }
                if ((switch_value & 16) != 0) {
                    const typesName = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
                    result.typesName = typesName;
                }
                if ((switch_value & 32) != 0) {
                    const compatName = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
                    result.compatName = compatName;
                }
                if ((switch_value & 64) != 0) {
                    var payload: @typeInfo(@TypeOf(result.typeNames)).optional.child = undefined;
                    const decoded_typeNames_buf = try allocator.alloc(xproto.Atom, @intCast(nTypes));
                    for (decoded_typeNames_buf) |*elem| {
                        elem.* = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
                    }
                    payload.typeNames = decoded_typeNames_buf;
                    payload.decoded_typeNames_buf = decoded_typeNames_buf;
                    result.typeNames = payload;
                }
                if ((switch_value & 128) != 0) {
                    var payload: @typeInfo(@TypeOf(result.KTLevelNames)).optional.child = undefined;
                    const decoded_nLevelsPerType_buf = try allocator.dupe(u8, try reader.take(@intCast(nTypes)));
                    payload.nLevelsPerType = decoded_nLevelsPerType_buf;
                    payload.decoded_nLevelsPerType_buf = decoded_nLevelsPerType_buf;
                    _ = try reader.take(wire.pad(reader.seek, 4));
                    const decoded_ktLevelNames_buf = try allocator.alloc(xproto.Atom, @intCast((blk: { var total: usize = 0; for (payload.nLevelsPerType) |elem| total += @as(usize, elem); break :blk total; })));
                    for (decoded_ktLevelNames_buf) |*elem| {
                        elem.* = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
                    }
                    payload.ktLevelNames = decoded_ktLevelNames_buf;
                    payload.decoded_ktLevelNames_buf = decoded_ktLevelNames_buf;
                    result.KTLevelNames = payload;
                }
                if ((switch_value & 256) != 0) {
                    var payload: @typeInfo(@TypeOf(result.indicatorNames)).optional.child = undefined;
                    const decoded_indicatorNames_buf = try allocator.alloc(xproto.Atom, @intCast(@popCount(payload.indicators)));
                    for (decoded_indicatorNames_buf) |*elem| {
                        elem.* = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
                    }
                    payload.indicatorNames = decoded_indicatorNames_buf;
                    payload.decoded_indicatorNames_buf = decoded_indicatorNames_buf;
                    result.indicatorNames = payload;
                }
                if ((switch_value & 2048) != 0) {
                    var payload: @typeInfo(@TypeOf(result.virtualModNames)).optional.child = undefined;
                    const decoded_virtualModNames_buf = try allocator.alloc(xproto.Atom, @intCast(@popCount(payload.virtualMods)));
                    for (decoded_virtualModNames_buf) |*elem| {
                        elem.* = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
                    }
                    payload.virtualModNames = decoded_virtualModNames_buf;
                    payload.decoded_virtualModNames_buf = decoded_virtualModNames_buf;
                    result.virtualModNames = payload;
                }
                if ((switch_value & 4096) != 0) {
                    var payload: @typeInfo(@TypeOf(result.groups)).optional.child = undefined;
                    const decoded_groups_buf = try allocator.alloc(xproto.Atom, @intCast(@popCount(payload.groupNames)));
                    for (decoded_groups_buf) |*elem| {
                        elem.* = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
                    }
                    payload.groups = decoded_groups_buf;
                    payload.decoded_groups_buf = decoded_groups_buf;
                    result.groups = payload;
                }
                if ((switch_value & 512) != 0) {
                    var payload: @typeInfo(@TypeOf(result.keyNames)).optional.child = undefined;
                    const decoded_keyNames_buf = try allocator.alloc(KeyName, @intCast(nKeys));
                    for (decoded_keyNames_buf) |*elem| {
                        elem.* = try KeyName.decode(reader);
                    }
                    payload.keyNames = decoded_keyNames_buf;
                    payload.decoded_keyNames_buf = decoded_keyNames_buf;
                    result.keyNames = payload;
                }
                if ((switch_value & 1024) != 0) {
                    var payload: @typeInfo(@TypeOf(result.keyAliases)).optional.child = undefined;
                    const decoded_keyAliases_buf = try allocator.alloc(KeyAlias, @intCast(nKeyAliases));
                    for (decoded_keyAliases_buf) |*elem| {
                        elem.* = try KeyAlias.decode(reader);
                    }
                    payload.keyAliases = decoded_keyAliases_buf;
                    payload.decoded_keyAliases_buf = decoded_keyAliases_buf;
                    result.keyAliases = payload;
                }
                if ((switch_value & 8192) != 0) {
                    var payload: @typeInfo(@TypeOf(result.radioGroupNames)).optional.child = undefined;
                    const decoded_radioGroupNames_buf = try allocator.alloc(xproto.Atom, @intCast(nRadioGroups));
                    for (decoded_radioGroupNames_buf) |*elem| {
                        elem.* = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
                    }
                    payload.radioGroupNames = decoded_radioGroupNames_buf;
                    payload.decoded_radioGroupNames_buf = decoded_radioGroupNames_buf;
                    result.radioGroupNames = payload;
                }
                return result;
            }

            pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
                if (self.typeNames) |*payload| {
                    if (payload.decoded_typeNames_buf) |buf| {
                        allocator.free(buf);
                        payload.decoded_typeNames_buf = null;
                        payload.typeNames = &.{};
                    }
                }
                if (self.KTLevelNames) |*payload| {
                    if (payload.decoded_nLevelsPerType_buf) |buf| {
                        allocator.free(buf);
                        payload.decoded_nLevelsPerType_buf = null;
                        payload.nLevelsPerType = &.{};
                    }
                    if (payload.decoded_ktLevelNames_buf) |buf| {
                        allocator.free(buf);
                        payload.decoded_ktLevelNames_buf = null;
                        payload.ktLevelNames = &.{};
                    }
                }
                if (self.indicatorNames) |*payload| {
                    if (payload.decoded_indicatorNames_buf) |buf| {
                        allocator.free(buf);
                        payload.decoded_indicatorNames_buf = null;
                        payload.indicatorNames = &.{};
                    }
                }
                if (self.virtualModNames) |*payload| {
                    if (payload.decoded_virtualModNames_buf) |buf| {
                        allocator.free(buf);
                        payload.decoded_virtualModNames_buf = null;
                        payload.virtualModNames = &.{};
                    }
                }
                if (self.groups) |*payload| {
                    if (payload.decoded_groups_buf) |buf| {
                        allocator.free(buf);
                        payload.decoded_groups_buf = null;
                        payload.groups = &.{};
                    }
                }
                if (self.keyNames) |*payload| {
                    if (payload.decoded_keyNames_buf) |buf| {
                        allocator.free(buf);
                        payload.decoded_keyNames_buf = null;
                        payload.keyNames = &.{};
                    }
                }
                if (self.keyAliases) |*payload| {
                    if (payload.decoded_keyAliases_buf) |buf| {
                        allocator.free(buf);
                        payload.decoded_keyAliases_buf = null;
                        payload.keyAliases = &.{};
                    }
                }
                if (self.radioGroupNames) |*payload| {
                    if (payload.decoded_radioGroupNames_buf) |buf| {
                        allocator.free(buf);
                        payload.decoded_radioGroupNames_buf = null;
                        payload.radioGroupNames = &.{};
                    }
                }
            }
        };

        pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader, header_: wire.ReplyHeader) !@This() {
            var result: @This() = undefined;
            _ = header_;
            result.deviceID = try reader.takeByte();
            const which = try reader.takeInt(u32, .native);
            result.minKeyCode = try reader.takeByte();
            result.maxKeyCode = try reader.takeByte();
            result.nTypes = try reader.takeByte();
            result.groupNames = try reader.takeByte();
            result.virtualMods = try reader.takeInt(u16, .native);
            result.firstKey = try reader.takeByte();
            result.nKeys = try reader.takeByte();
            result.indicators = try reader.takeInt(u32, .native);
            result.nRadioGroups = try reader.takeByte();
            result.nKeyAliases = try reader.takeByte();
            result.nKTLevels = try reader.takeInt(u16, .native);
            _ = try reader.take(4);
            result.valueList = try @TypeOf(result).ValueList.decode(allocator, reader, which, result.nTypes, result.nKeys, result.nKeyAliases, result.nRadioGroups);
            return result;
        }

        pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
            self.valueList.deinit(allocator);
        }
    };


    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u16, self.deviceSpec);
        writer.splatByte(0, 2);
        writer.writeInt(u32, self.which);
    }
};

pub const SetNames = struct {
    pub const opcode: u8 = 18;

    deviceSpec: u16,
    virtualMods: u16,
    firstType: u8,
    nTypes: u8,
    firstKTLevelt: u8,
    nKTLevels: u8,
    indicators: u32,
    groupNames: u8,
    nRadioGroups: u8,
    firstKey: u8,
    nKeys: u8,
    nKeyAliases: u8,
    totalKTLevelNames: u16,
    values: Values,

    pub const Values = struct {
        keycodesName: ?xproto.Atom = null,
        geometryName: ?xproto.Atom = null,
        symbolsName: ?xproto.Atom = null,
        physSymbolsName: ?xproto.Atom = null,
        typesName: ?xproto.Atom = null,
        compatName: ?xproto.Atom = null,
        typeNames: ?struct {
            typeNames: []const xproto.Atom,
            decoded_typeNames_buf: ?[]xproto.Atom = null,
        } = null,
        KTLevelNames: ?struct {
            nLevelsPerType: []const u8,
            decoded_nLevelsPerType_buf: ?[]u8 = null,
            ktLevelNames: []const xproto.Atom,
            decoded_ktLevelNames_buf: ?[]xproto.Atom = null,
        } = null,
        indicatorNames: ?struct {
            indicatorNames: []const xproto.Atom,
            decoded_indicatorNames_buf: ?[]xproto.Atom = null,
        } = null,
        virtualModNames: ?struct {
            virtualModNames: []const xproto.Atom,
            decoded_virtualModNames_buf: ?[]xproto.Atom = null,
        } = null,
        groups: ?struct {
            groups: []const xproto.Atom,
            decoded_groups_buf: ?[]xproto.Atom = null,
        } = null,
        keyNames: ?struct {
            keyNames: []const KeyName,
            decoded_keyNames_buf: ?[]KeyName = null,
        } = null,
        keyAliases: ?struct {
            keyAliases: []const KeyAlias,
            decoded_keyAliases_buf: ?[]KeyAlias = null,
        } = null,
        radioGroupNames: ?struct {
            radioGroupNames: []const xproto.Atom,
            decoded_radioGroupNames_buf: ?[]xproto.Atom = null,
        } = null,

        pub fn encode(self: *const @This(), writer: anytype) !void {
            if (self.keycodesName) |it| {
                writer.writeInt(u32, @intCast(@intFromEnum(it)));
            }
            if (self.geometryName) |it| {
                writer.writeInt(u32, @intCast(@intFromEnum(it)));
            }
            if (self.symbolsName) |it| {
                writer.writeInt(u32, @intCast(@intFromEnum(it)));
            }
            if (self.physSymbolsName) |it| {
                writer.writeInt(u32, @intCast(@intFromEnum(it)));
            }
            if (self.typesName) |it| {
                writer.writeInt(u32, @intCast(@intFromEnum(it)));
            }
            if (self.compatName) |it| {
                writer.writeInt(u32, @intCast(@intFromEnum(it)));
            }
            if (self.typeNames) |it| {
                for (it.typeNames) |elem| {
                    writer.writeInt(u32, @intCast(@intFromEnum(elem)));
                }
            }
            if (self.KTLevelNames) |it| {
                writer.write(it.nLevelsPerType);
                writer.splatByte(0, wire.pad(writer.seek, 4));
                for (it.ktLevelNames) |elem| {
                    writer.writeInt(u32, @intCast(@intFromEnum(elem)));
                }
            }
            if (self.indicatorNames) |it| {
                for (it.indicatorNames) |elem| {
                    writer.writeInt(u32, @intCast(@intFromEnum(elem)));
                }
            }
            if (self.virtualModNames) |it| {
                for (it.virtualModNames) |elem| {
                    writer.writeInt(u32, @intCast(@intFromEnum(elem)));
                }
            }
            if (self.groups) |it| {
                for (it.groups) |elem| {
                    writer.writeInt(u32, @intCast(@intFromEnum(elem)));
                }
            }
            if (self.keyNames) |it| {
                for (it.keyNames) |elem| {
                    try elem.encode(writer);
                }
            }
            if (self.keyAliases) |it| {
                for (it.keyAliases) |elem| {
                    try elem.encode(writer);
                }
            }
            if (self.radioGroupNames) |it| {
                for (it.radioGroupNames) |elem| {
                    writer.writeInt(u32, @intCast(@intFromEnum(elem)));
                }
            }
        }

        pub fn switchValue(self: *const @This()) u32 {
            var result: u32 = 0;
            if (self.keycodesName != null) result |= 1;
            if (self.geometryName != null) result |= 2;
            if (self.symbolsName != null) result |= 4;
            if (self.physSymbolsName != null) result |= 8;
            if (self.typesName != null) result |= 16;
            if (self.compatName != null) result |= 32;
            if (self.typeNames != null) result |= 64;
            if (self.KTLevelNames != null) result |= 128;
            if (self.indicatorNames != null) result |= 256;
            if (self.virtualModNames != null) result |= 2048;
            if (self.groups != null) result |= 4096;
            if (self.keyNames != null) result |= 512;
            if (self.keyAliases != null) result |= 1024;
            if (self.radioGroupNames != null) result |= 8192;
            return result;
        }

        pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader, switch_value: u32, nTypes: u8, nKeys: u8, nKeyAliases: u8, nRadioGroups: u8) !@This() {
            var result: @This() = .{};
            if ((switch_value & 1) != 0) {
                const keycodesName = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
                result.keycodesName = keycodesName;
            }
            if ((switch_value & 2) != 0) {
                const geometryName = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
                result.geometryName = geometryName;
            }
            if ((switch_value & 4) != 0) {
                const symbolsName = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
                result.symbolsName = symbolsName;
            }
            if ((switch_value & 8) != 0) {
                const physSymbolsName = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
                result.physSymbolsName = physSymbolsName;
            }
            if ((switch_value & 16) != 0) {
                const typesName = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
                result.typesName = typesName;
            }
            if ((switch_value & 32) != 0) {
                const compatName = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
                result.compatName = compatName;
            }
            if ((switch_value & 64) != 0) {
                var payload: @typeInfo(@TypeOf(result.typeNames)).optional.child = undefined;
                const decoded_typeNames_buf = try allocator.alloc(xproto.Atom, @intCast(nTypes));
                for (decoded_typeNames_buf) |*elem| {
                    elem.* = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
                }
                payload.typeNames = decoded_typeNames_buf;
                payload.decoded_typeNames_buf = decoded_typeNames_buf;
                result.typeNames = payload;
            }
            if ((switch_value & 128) != 0) {
                var payload: @typeInfo(@TypeOf(result.KTLevelNames)).optional.child = undefined;
                const decoded_nLevelsPerType_buf = try allocator.dupe(u8, try reader.take(@intCast(nTypes)));
                payload.nLevelsPerType = decoded_nLevelsPerType_buf;
                payload.decoded_nLevelsPerType_buf = decoded_nLevelsPerType_buf;
                _ = try reader.take(wire.pad(reader.seek, 4));
                const decoded_ktLevelNames_buf = try allocator.alloc(xproto.Atom, @intCast((blk: { var total: usize = 0; for (payload.nLevelsPerType) |elem| total += @as(usize, elem); break :blk total; })));
                for (decoded_ktLevelNames_buf) |*elem| {
                    elem.* = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
                }
                payload.ktLevelNames = decoded_ktLevelNames_buf;
                payload.decoded_ktLevelNames_buf = decoded_ktLevelNames_buf;
                result.KTLevelNames = payload;
            }
            if ((switch_value & 256) != 0) {
                var payload: @typeInfo(@TypeOf(result.indicatorNames)).optional.child = undefined;
                const decoded_indicatorNames_buf = try allocator.alloc(xproto.Atom, @intCast(@popCount(payload.indicators)));
                for (decoded_indicatorNames_buf) |*elem| {
                    elem.* = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
                }
                payload.indicatorNames = decoded_indicatorNames_buf;
                payload.decoded_indicatorNames_buf = decoded_indicatorNames_buf;
                result.indicatorNames = payload;
            }
            if ((switch_value & 2048) != 0) {
                var payload: @typeInfo(@TypeOf(result.virtualModNames)).optional.child = undefined;
                const decoded_virtualModNames_buf = try allocator.alloc(xproto.Atom, @intCast(@popCount(payload.virtualMods)));
                for (decoded_virtualModNames_buf) |*elem| {
                    elem.* = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
                }
                payload.virtualModNames = decoded_virtualModNames_buf;
                payload.decoded_virtualModNames_buf = decoded_virtualModNames_buf;
                result.virtualModNames = payload;
            }
            if ((switch_value & 4096) != 0) {
                var payload: @typeInfo(@TypeOf(result.groups)).optional.child = undefined;
                const decoded_groups_buf = try allocator.alloc(xproto.Atom, @intCast(@popCount(payload.groupNames)));
                for (decoded_groups_buf) |*elem| {
                    elem.* = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
                }
                payload.groups = decoded_groups_buf;
                payload.decoded_groups_buf = decoded_groups_buf;
                result.groups = payload;
            }
            if ((switch_value & 512) != 0) {
                var payload: @typeInfo(@TypeOf(result.keyNames)).optional.child = undefined;
                const decoded_keyNames_buf = try allocator.alloc(KeyName, @intCast(nKeys));
                for (decoded_keyNames_buf) |*elem| {
                    elem.* = try KeyName.decode(reader);
                }
                payload.keyNames = decoded_keyNames_buf;
                payload.decoded_keyNames_buf = decoded_keyNames_buf;
                result.keyNames = payload;
            }
            if ((switch_value & 1024) != 0) {
                var payload: @typeInfo(@TypeOf(result.keyAliases)).optional.child = undefined;
                const decoded_keyAliases_buf = try allocator.alloc(KeyAlias, @intCast(nKeyAliases));
                for (decoded_keyAliases_buf) |*elem| {
                    elem.* = try KeyAlias.decode(reader);
                }
                payload.keyAliases = decoded_keyAliases_buf;
                payload.decoded_keyAliases_buf = decoded_keyAliases_buf;
                result.keyAliases = payload;
            }
            if ((switch_value & 8192) != 0) {
                var payload: @typeInfo(@TypeOf(result.radioGroupNames)).optional.child = undefined;
                const decoded_radioGroupNames_buf = try allocator.alloc(xproto.Atom, @intCast(nRadioGroups));
                for (decoded_radioGroupNames_buf) |*elem| {
                    elem.* = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
                }
                payload.radioGroupNames = decoded_radioGroupNames_buf;
                payload.decoded_radioGroupNames_buf = decoded_radioGroupNames_buf;
                result.radioGroupNames = payload;
            }
            return result;
        }

        pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
            if (self.typeNames) |*payload| {
                if (payload.decoded_typeNames_buf) |buf| {
                    allocator.free(buf);
                    payload.decoded_typeNames_buf = null;
                    payload.typeNames = &.{};
                }
            }
            if (self.KTLevelNames) |*payload| {
                if (payload.decoded_nLevelsPerType_buf) |buf| {
                    allocator.free(buf);
                    payload.decoded_nLevelsPerType_buf = null;
                    payload.nLevelsPerType = &.{};
                }
                if (payload.decoded_ktLevelNames_buf) |buf| {
                    allocator.free(buf);
                    payload.decoded_ktLevelNames_buf = null;
                    payload.ktLevelNames = &.{};
                }
            }
            if (self.indicatorNames) |*payload| {
                if (payload.decoded_indicatorNames_buf) |buf| {
                    allocator.free(buf);
                    payload.decoded_indicatorNames_buf = null;
                    payload.indicatorNames = &.{};
                }
            }
            if (self.virtualModNames) |*payload| {
                if (payload.decoded_virtualModNames_buf) |buf| {
                    allocator.free(buf);
                    payload.decoded_virtualModNames_buf = null;
                    payload.virtualModNames = &.{};
                }
            }
            if (self.groups) |*payload| {
                if (payload.decoded_groups_buf) |buf| {
                    allocator.free(buf);
                    payload.decoded_groups_buf = null;
                    payload.groups = &.{};
                }
            }
            if (self.keyNames) |*payload| {
                if (payload.decoded_keyNames_buf) |buf| {
                    allocator.free(buf);
                    payload.decoded_keyNames_buf = null;
                    payload.keyNames = &.{};
                }
            }
            if (self.keyAliases) |*payload| {
                if (payload.decoded_keyAliases_buf) |buf| {
                    allocator.free(buf);
                    payload.decoded_keyAliases_buf = null;
                    payload.keyAliases = &.{};
                }
            }
            if (self.radioGroupNames) |*payload| {
                if (payload.decoded_radioGroupNames_buf) |buf| {
                    allocator.free(buf);
                    payload.decoded_radioGroupNames_buf = null;
                    payload.radioGroupNames = &.{};
                }
            }
        }
    };

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u16, self.deviceSpec);
        writer.writeInt(u16, self.virtualMods);
        writer.writeInt(u32, @intCast(self.values.switchValue()));
        writer.writeByte(self.firstType);
        writer.writeByte(self.nTypes);
        writer.writeByte(self.firstKTLevelt);
        writer.writeByte(self.nKTLevels);
        writer.writeInt(u32, self.indicators);
        writer.writeByte(self.groupNames);
        writer.writeByte(self.nRadioGroups);
        writer.writeByte(self.firstKey);
        writer.writeByte(self.nKeys);
        writer.writeByte(self.nKeyAliases);
        writer.splatByte(0, 1);
        writer.writeInt(u16, self.totalKTLevelNames);
        try self.values.encode(writer);
    }
};

pub const PerClientFlags = struct {
    pub const opcode: u8 = 21;

    deviceSpec: u16,
    change: u32,
    value: u32,
    ctrlsToChange: u32,
    autoCtrls: u32,
    autoCtrlsValues: u32,

    pub const Reply = struct {
        deviceID: u8,
        supported: u32,
        value: u32,
        autoCtrls: u32,
        autoCtrlsValues: u32,

        pub fn decode(reader: *std.Io.Reader, header_: wire.ReplyHeader) !@This() {
            var result: @This() = undefined;
            _ = header_;
            result.deviceID = try reader.takeByte();
            result.supported = try reader.takeInt(u32, .native);
            result.value = try reader.takeInt(u32, .native);
            result.autoCtrls = try reader.takeInt(u32, .native);
            result.autoCtrlsValues = try reader.takeInt(u32, .native);
            _ = try reader.take(8);
            return result;
        }
    };


    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u16, self.deviceSpec);
        writer.splatByte(0, 2);
        writer.writeInt(u32, self.change);
        writer.writeInt(u32, self.value);
        writer.writeInt(u32, self.ctrlsToChange);
        writer.writeInt(u32, self.autoCtrls);
        writer.writeInt(u32, self.autoCtrlsValues);
    }
};

pub const ListComponents = struct {
    pub const opcode: u8 = 22;

    deviceSpec: u16,
    maxNames: u16,

    pub const Reply = struct {
        deviceID: u8,
        extra: u16,
        keymaps: []const Listing,
        decoded_keymaps_buf: ?[]Listing = null,
        keycodes: []const Listing,
        decoded_keycodes_buf: ?[]Listing = null,
        types: []const Listing,
        decoded_types_buf: ?[]Listing = null,
        compatMaps: []const Listing,
        decoded_compatMaps_buf: ?[]Listing = null,
        symbols: []const Listing,
        decoded_symbols_buf: ?[]Listing = null,
        geometries: []const Listing,
        decoded_geometries_buf: ?[]Listing = null,

        pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader, header_: wire.ReplyHeader) !@This() {
            var result: @This() = undefined;
            _ = header_;
            result.deviceID = try reader.takeByte();
            const nKeymaps = try reader.takeInt(u16, .native);
            const nKeycodes = try reader.takeInt(u16, .native);
            const nTypes = try reader.takeInt(u16, .native);
            const nCompatMaps = try reader.takeInt(u16, .native);
            const nSymbols = try reader.takeInt(u16, .native);
            const nGeometries = try reader.takeInt(u16, .native);
            result.extra = try reader.takeInt(u16, .native);
            _ = try reader.take(10);
            const decoded_keymaps_buf = try allocator.alloc(Listing, @intCast(nKeymaps));
            for (decoded_keymaps_buf) |*elem| {
                elem.* = try Listing.decode(allocator, reader);
            }
            result.keymaps = decoded_keymaps_buf;
            result.decoded_keymaps_buf = decoded_keymaps_buf;
            const decoded_keycodes_buf = try allocator.alloc(Listing, @intCast(nKeycodes));
            for (decoded_keycodes_buf) |*elem| {
                elem.* = try Listing.decode(allocator, reader);
            }
            result.keycodes = decoded_keycodes_buf;
            result.decoded_keycodes_buf = decoded_keycodes_buf;
            const decoded_types_buf = try allocator.alloc(Listing, @intCast(nTypes));
            for (decoded_types_buf) |*elem| {
                elem.* = try Listing.decode(allocator, reader);
            }
            result.types = decoded_types_buf;
            result.decoded_types_buf = decoded_types_buf;
            const decoded_compatMaps_buf = try allocator.alloc(Listing, @intCast(nCompatMaps));
            for (decoded_compatMaps_buf) |*elem| {
                elem.* = try Listing.decode(allocator, reader);
            }
            result.compatMaps = decoded_compatMaps_buf;
            result.decoded_compatMaps_buf = decoded_compatMaps_buf;
            const decoded_symbols_buf = try allocator.alloc(Listing, @intCast(nSymbols));
            for (decoded_symbols_buf) |*elem| {
                elem.* = try Listing.decode(allocator, reader);
            }
            result.symbols = decoded_symbols_buf;
            result.decoded_symbols_buf = decoded_symbols_buf;
            const decoded_geometries_buf = try allocator.alloc(Listing, @intCast(nGeometries));
            for (decoded_geometries_buf) |*elem| {
                elem.* = try Listing.decode(allocator, reader);
            }
            result.geometries = decoded_geometries_buf;
            result.decoded_geometries_buf = decoded_geometries_buf;
            return result;
        }

        pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
            if (self.decoded_keymaps_buf) |buf| {
                for (buf) |*it| {
                    it.deinit(allocator);
                }
                allocator.free(buf);
                self.decoded_keymaps_buf = null;
                self.keymaps = &.{};
            }
            if (self.decoded_keycodes_buf) |buf| {
                for (buf) |*it| {
                    it.deinit(allocator);
                }
                allocator.free(buf);
                self.decoded_keycodes_buf = null;
                self.keycodes = &.{};
            }
            if (self.decoded_types_buf) |buf| {
                for (buf) |*it| {
                    it.deinit(allocator);
                }
                allocator.free(buf);
                self.decoded_types_buf = null;
                self.types = &.{};
            }
            if (self.decoded_compatMaps_buf) |buf| {
                for (buf) |*it| {
                    it.deinit(allocator);
                }
                allocator.free(buf);
                self.decoded_compatMaps_buf = null;
                self.compatMaps = &.{};
            }
            if (self.decoded_symbols_buf) |buf| {
                for (buf) |*it| {
                    it.deinit(allocator);
                }
                allocator.free(buf);
                self.decoded_symbols_buf = null;
                self.symbols = &.{};
            }
            if (self.decoded_geometries_buf) |buf| {
                for (buf) |*it| {
                    it.deinit(allocator);
                }
                allocator.free(buf);
                self.decoded_geometries_buf = null;
                self.geometries = &.{};
            }
        }
    };


    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u16, self.deviceSpec);
        writer.writeInt(u16, self.maxNames);
    }
};

pub const GetKbdByName = struct {
    pub const opcode: u8 = 23;

    deviceSpec: u16,
    need: u16,
    want: u16,
    load: bool,

    pub const Reply = struct {
        deviceID: u8,
        minKeyCode: u8,
        maxKeyCode: u8,
        loaded: bool,
        newKeyboard: bool,
        found: u16,
        replies: Replies,

        pub const Replies = struct {
            types: ?struct {
                getmap_type: u8,
                typeDeviceID: u8,
                getmap_sequence: u16,
                getmap_length: u32,
                typeMinKeyCode: u8,
                typeMaxKeyCode: u8,
                firstType: u8,
                nTypes: u8,
                totalTypes: u8,
                firstKeySym: u8,
                totalSyms: u16,
                nKeySyms: u8,
                firstKeyAction: u8,
                totalActions: u16,
                nKeyActions: u8,
                firstKeyBehavior: u8,
                nKeyBehaviors: u8,
                totalKeyBehaviors: u8,
                firstKeyExplicit: u8,
                nKeyExplicit: u8,
                totalKeyExplicit: u8,
                firstModMapKey: u8,
                nModMapKeys: u8,
                totalModMapKeys: u8,
                firstVModMapKey: u8,
                nVModMapKeys: u8,
                totalVModMapKeys: u8,
                virtualMods: u16,
                map: Map,

                pub const Map = struct {
                    types_rtrn: ?struct {
                        types_rtrn: []const KeyType,
                        decoded_types_rtrn_buf: ?[]KeyType = null,
                    } = null,
                    syms_rtrn: ?struct {
                        syms_rtrn: []const KeySymMap,
                        decoded_syms_rtrn_buf: ?[]KeySymMap = null,
                    } = null,
                    KeyActions: ?struct {
                        acts_rtrn_count: []const u8,
                        decoded_acts_rtrn_count_buf: ?[]u8 = null,
                        acts_rtrn_acts: []const Action,
                        decoded_acts_rtrn_acts_buf: ?[]Action = null,
                    } = null,
                    behaviors_rtrn: ?struct {
                        behaviors_rtrn: []const SetBehavior,
                        decoded_behaviors_rtrn_buf: ?[]SetBehavior = null,
                    } = null,
                    VirtualMods: ?struct {
                        vmods_rtrn: []const u8,
                        decoded_vmods_rtrn_buf: ?[]u8 = null,
                    } = null,
                    ExplicitComponents: ?struct {
                        explicit_rtrn: []const SetExplicit,
                        decoded_explicit_rtrn_buf: ?[]SetExplicit = null,
                    } = null,
                    ModifierMap: ?struct {
                        modmap_rtrn: []const KeyModMap,
                        decoded_modmap_rtrn_buf: ?[]KeyModMap = null,
                    } = null,
                    vmodmap_rtrn: ?struct {
                        vmodmap_rtrn: []const KeyVModMap,
                        decoded_vmodmap_rtrn_buf: ?[]KeyVModMap = null,
                    } = null,

                    pub fn encode(self: *const @This(), writer: anytype) !void {
                        if (self.types_rtrn) |it| {
                            for (it.types_rtrn) |elem| {
                                try elem.encode(writer);
                            }
                        }
                        if (self.syms_rtrn) |it| {
                            for (it.syms_rtrn) |elem| {
                                try elem.encode(writer);
                            }
                        }
                        if (self.KeyActions) |it| {
                            writer.write(it.acts_rtrn_count);
                            writer.splatByte(0, wire.pad(writer.seek, 4));
                            for (it.acts_rtrn_acts) |elem| {
                                try elem.encode(writer);
                            }
                        }
                        if (self.behaviors_rtrn) |it| {
                            for (it.behaviors_rtrn) |elem| {
                                try elem.encode(writer);
                            }
                        }
                        if (self.VirtualMods) |it| {
                            writer.write(it.vmods_rtrn);
                            writer.splatByte(0, wire.pad(writer.seek, 4));
                        }
                        if (self.ExplicitComponents) |it| {
                            for (it.explicit_rtrn) |elem| {
                                try elem.encode(writer);
                            }
                            writer.splatByte(0, wire.pad(writer.seek, 4));
                        }
                        if (self.ModifierMap) |it| {
                            for (it.modmap_rtrn) |elem| {
                                try elem.encode(writer);
                            }
                            writer.splatByte(0, wire.pad(writer.seek, 4));
                        }
                        if (self.vmodmap_rtrn) |it| {
                            for (it.vmodmap_rtrn) |elem| {
                                try elem.encode(writer);
                            }
                        }
                    }

                    pub fn switchValue(self: *const @This()) u32 {
                        var result: u32 = 0;
                        if (self.types_rtrn != null) result |= 1;
                        if (self.syms_rtrn != null) result |= 2;
                        if (self.KeyActions != null) result |= 16;
                        if (self.behaviors_rtrn != null) result |= 32;
                        if (self.VirtualMods != null) result |= 64;
                        if (self.ExplicitComponents != null) result |= 8;
                        if (self.ModifierMap != null) result |= 4;
                        if (self.vmodmap_rtrn != null) result |= 128;
                        return result;
                    }

                    pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader, switch_value: u32, nTypes: u8, nKeySyms: u8, nKeyActions: u8, totalActions: u16, totalKeyBehaviors: u8, totalKeyExplicit: u8, totalModMapKeys: u8, totalVModMapKeys: u8) !@This() {
                        var result: @This() = .{};
                        if ((switch_value & 1) != 0) {
                            var payload: @typeInfo(@TypeOf(result.types_rtrn)).optional.child = undefined;
                            const decoded_types_rtrn_buf = try allocator.alloc(KeyType, @intCast(nTypes));
                            for (decoded_types_rtrn_buf) |*elem| {
                                elem.* = try KeyType.decode(allocator, reader);
                            }
                            payload.types_rtrn = decoded_types_rtrn_buf;
                            payload.decoded_types_rtrn_buf = decoded_types_rtrn_buf;
                            result.types_rtrn = payload;
                        }
                        if ((switch_value & 2) != 0) {
                            var payload: @typeInfo(@TypeOf(result.syms_rtrn)).optional.child = undefined;
                            const decoded_syms_rtrn_buf = try allocator.alloc(KeySymMap, @intCast(nKeySyms));
                            for (decoded_syms_rtrn_buf) |*elem| {
                                elem.* = try KeySymMap.decode(allocator, reader);
                            }
                            payload.syms_rtrn = decoded_syms_rtrn_buf;
                            payload.decoded_syms_rtrn_buf = decoded_syms_rtrn_buf;
                            result.syms_rtrn = payload;
                        }
                        if ((switch_value & 16) != 0) {
                            var payload: @typeInfo(@TypeOf(result.KeyActions)).optional.child = undefined;
                            const decoded_acts_rtrn_count_buf = try allocator.dupe(u8, try reader.take(@intCast(nKeyActions)));
                            payload.acts_rtrn_count = decoded_acts_rtrn_count_buf;
                            payload.decoded_acts_rtrn_count_buf = decoded_acts_rtrn_count_buf;
                            _ = try reader.take(wire.pad(reader.seek, 4));
                            const decoded_acts_rtrn_acts_buf = try allocator.alloc(Action, @intCast(totalActions));
                            for (decoded_acts_rtrn_acts_buf) |*elem| {
                                elem.* = try Action.decode(reader);
                            }
                            payload.acts_rtrn_acts = decoded_acts_rtrn_acts_buf;
                            payload.decoded_acts_rtrn_acts_buf = decoded_acts_rtrn_acts_buf;
                            result.KeyActions = payload;
                        }
                        if ((switch_value & 32) != 0) {
                            var payload: @typeInfo(@TypeOf(result.behaviors_rtrn)).optional.child = undefined;
                            const decoded_behaviors_rtrn_buf = try allocator.alloc(SetBehavior, @intCast(totalKeyBehaviors));
                            for (decoded_behaviors_rtrn_buf) |*elem| {
                                elem.* = try SetBehavior.decode(reader);
                            }
                            payload.behaviors_rtrn = decoded_behaviors_rtrn_buf;
                            payload.decoded_behaviors_rtrn_buf = decoded_behaviors_rtrn_buf;
                            result.behaviors_rtrn = payload;
                        }
                        if ((switch_value & 64) != 0) {
                            var payload: @typeInfo(@TypeOf(result.VirtualMods)).optional.child = undefined;
                            const decoded_vmods_rtrn_buf = try allocator.dupe(u8, try reader.take(@intCast(@popCount(payload.virtualMods))));
                            payload.vmods_rtrn = decoded_vmods_rtrn_buf;
                            payload.decoded_vmods_rtrn_buf = decoded_vmods_rtrn_buf;
                            _ = try reader.take(wire.pad(reader.seek, 4));
                            result.VirtualMods = payload;
                        }
                        if ((switch_value & 8) != 0) {
                            var payload: @typeInfo(@TypeOf(result.ExplicitComponents)).optional.child = undefined;
                            const decoded_explicit_rtrn_buf = try allocator.alloc(SetExplicit, @intCast(totalKeyExplicit));
                            for (decoded_explicit_rtrn_buf) |*elem| {
                                elem.* = try SetExplicit.decode(reader);
                            }
                            payload.explicit_rtrn = decoded_explicit_rtrn_buf;
                            payload.decoded_explicit_rtrn_buf = decoded_explicit_rtrn_buf;
                            _ = try reader.take(wire.pad(reader.seek, 4));
                            result.ExplicitComponents = payload;
                        }
                        if ((switch_value & 4) != 0) {
                            var payload: @typeInfo(@TypeOf(result.ModifierMap)).optional.child = undefined;
                            const decoded_modmap_rtrn_buf = try allocator.alloc(KeyModMap, @intCast(totalModMapKeys));
                            for (decoded_modmap_rtrn_buf) |*elem| {
                                elem.* = try KeyModMap.decode(reader);
                            }
                            payload.modmap_rtrn = decoded_modmap_rtrn_buf;
                            payload.decoded_modmap_rtrn_buf = decoded_modmap_rtrn_buf;
                            _ = try reader.take(wire.pad(reader.seek, 4));
                            result.ModifierMap = payload;
                        }
                        if ((switch_value & 128) != 0) {
                            var payload: @typeInfo(@TypeOf(result.vmodmap_rtrn)).optional.child = undefined;
                            const decoded_vmodmap_rtrn_buf = try allocator.alloc(KeyVModMap, @intCast(totalVModMapKeys));
                            for (decoded_vmodmap_rtrn_buf) |*elem| {
                                elem.* = try KeyVModMap.decode(reader);
                            }
                            payload.vmodmap_rtrn = decoded_vmodmap_rtrn_buf;
                            payload.decoded_vmodmap_rtrn_buf = decoded_vmodmap_rtrn_buf;
                            result.vmodmap_rtrn = payload;
                        }
                        return result;
                    }

                    pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
                        if (self.types_rtrn) |*payload| {
                            if (payload.decoded_types_rtrn_buf) |buf| {
                                for (buf) |*it| {
                                    it.deinit(allocator);
                                }
                                allocator.free(buf);
                                payload.decoded_types_rtrn_buf = null;
                                payload.types_rtrn = &.{};
                            }
                        }
                        if (self.syms_rtrn) |*payload| {
                            if (payload.decoded_syms_rtrn_buf) |buf| {
                                for (buf) |*it| {
                                    it.deinit(allocator);
                                }
                                allocator.free(buf);
                                payload.decoded_syms_rtrn_buf = null;
                                payload.syms_rtrn = &.{};
                            }
                        }
                        if (self.KeyActions) |*payload| {
                            if (payload.decoded_acts_rtrn_count_buf) |buf| {
                                allocator.free(buf);
                                payload.decoded_acts_rtrn_count_buf = null;
                                payload.acts_rtrn_count = &.{};
                            }
                            if (payload.decoded_acts_rtrn_acts_buf) |buf| {
                                allocator.free(buf);
                                payload.decoded_acts_rtrn_acts_buf = null;
                                payload.acts_rtrn_acts = &.{};
                            }
                        }
                        if (self.behaviors_rtrn) |*payload| {
                            if (payload.decoded_behaviors_rtrn_buf) |buf| {
                                allocator.free(buf);
                                payload.decoded_behaviors_rtrn_buf = null;
                                payload.behaviors_rtrn = &.{};
                            }
                        }
                        if (self.VirtualMods) |*payload| {
                            if (payload.decoded_vmods_rtrn_buf) |buf| {
                                allocator.free(buf);
                                payload.decoded_vmods_rtrn_buf = null;
                                payload.vmods_rtrn = &.{};
                            }
                        }
                        if (self.ExplicitComponents) |*payload| {
                            if (payload.decoded_explicit_rtrn_buf) |buf| {
                                allocator.free(buf);
                                payload.decoded_explicit_rtrn_buf = null;
                                payload.explicit_rtrn = &.{};
                            }
                        }
                        if (self.ModifierMap) |*payload| {
                            if (payload.decoded_modmap_rtrn_buf) |buf| {
                                allocator.free(buf);
                                payload.decoded_modmap_rtrn_buf = null;
                                payload.modmap_rtrn = &.{};
                            }
                        }
                        if (self.vmodmap_rtrn) |*payload| {
                            if (payload.decoded_vmodmap_rtrn_buf) |buf| {
                                allocator.free(buf);
                                payload.decoded_vmodmap_rtrn_buf = null;
                                payload.vmodmap_rtrn = &.{};
                            }
                        }
                    }
                };
            } = null,
            compat_map: ?struct {
                compatmap_type: u8,
                compatDeviceID: u8,
                compatmap_sequence: u16,
                compatmap_length: u32,
                groupsRtrn: u8,
                firstSIRtrn: u16,
                nTotalSI: u16,
                si_rtrn: []const SymInterpret,
                decoded_si_rtrn_buf: ?[]SymInterpret = null,
                group_rtrn: []const ModDef,
                decoded_group_rtrn_buf: ?[]ModDef = null,
            } = null,
            indicator_maps: ?struct {
                indicatormap_type: u8,
                indicatorDeviceID: u8,
                indicatormap_sequence: u16,
                indicatormap_length: u32,
                which: u32,
                realIndicators: u32,
                maps: []const IndicatorMap,
                decoded_maps_buf: ?[]IndicatorMap = null,
            } = null,
            key_names: ?struct {
                keyname_type: u8,
                keyDeviceID: u8,
                keyname_sequence: u16,
                keyname_length: u32,
                keyMinKeyCode: u8,
                keyMaxKeyCode: u8,
                nTypes: u8,
                groupNames: u8,
                virtualMods: u16,
                firstKey: u8,
                nKeys: u8,
                indicators: u32,
                nRadioGroups: u8,
                nKeyAliases: u8,
                nKTLevels: u16,
                valueList: ValueList,

                pub const ValueList = struct {
                    keycodesName: ?xproto.Atom = null,
                    geometryName: ?xproto.Atom = null,
                    symbolsName: ?xproto.Atom = null,
                    physSymbolsName: ?xproto.Atom = null,
                    typesName: ?xproto.Atom = null,
                    compatName: ?xproto.Atom = null,
                    typeNames: ?struct {
                        typeNames: []const xproto.Atom,
                        decoded_typeNames_buf: ?[]xproto.Atom = null,
                    } = null,
                    KTLevelNames: ?struct {
                        nLevelsPerType: []const u8,
                        decoded_nLevelsPerType_buf: ?[]u8 = null,
                        ktLevelNames: []const xproto.Atom,
                        decoded_ktLevelNames_buf: ?[]xproto.Atom = null,
                    } = null,
                    indicatorNames: ?struct {
                        indicatorNames: []const xproto.Atom,
                        decoded_indicatorNames_buf: ?[]xproto.Atom = null,
                    } = null,
                    virtualModNames: ?struct {
                        virtualModNames: []const xproto.Atom,
                        decoded_virtualModNames_buf: ?[]xproto.Atom = null,
                    } = null,
                    groups: ?struct {
                        groups: []const xproto.Atom,
                        decoded_groups_buf: ?[]xproto.Atom = null,
                    } = null,
                    keyNames: ?struct {
                        keyNames: []const KeyName,
                        decoded_keyNames_buf: ?[]KeyName = null,
                    } = null,
                    keyAliases: ?struct {
                        keyAliases: []const KeyAlias,
                        decoded_keyAliases_buf: ?[]KeyAlias = null,
                    } = null,
                    radioGroupNames: ?struct {
                        radioGroupNames: []const xproto.Atom,
                        decoded_radioGroupNames_buf: ?[]xproto.Atom = null,
                    } = null,

                    pub fn encode(self: *const @This(), writer: anytype) !void {
                        if (self.keycodesName) |it| {
                            writer.writeInt(u32, @intCast(@intFromEnum(it)));
                        }
                        if (self.geometryName) |it| {
                            writer.writeInt(u32, @intCast(@intFromEnum(it)));
                        }
                        if (self.symbolsName) |it| {
                            writer.writeInt(u32, @intCast(@intFromEnum(it)));
                        }
                        if (self.physSymbolsName) |it| {
                            writer.writeInt(u32, @intCast(@intFromEnum(it)));
                        }
                        if (self.typesName) |it| {
                            writer.writeInt(u32, @intCast(@intFromEnum(it)));
                        }
                        if (self.compatName) |it| {
                            writer.writeInt(u32, @intCast(@intFromEnum(it)));
                        }
                        if (self.typeNames) |it| {
                            for (it.typeNames) |elem| {
                                writer.writeInt(u32, @intCast(@intFromEnum(elem)));
                            }
                        }
                        if (self.KTLevelNames) |it| {
                            writer.write(it.nLevelsPerType);
                            writer.splatByte(0, wire.pad(writer.seek, 4));
                            for (it.ktLevelNames) |elem| {
                                writer.writeInt(u32, @intCast(@intFromEnum(elem)));
                            }
                        }
                        if (self.indicatorNames) |it| {
                            for (it.indicatorNames) |elem| {
                                writer.writeInt(u32, @intCast(@intFromEnum(elem)));
                            }
                        }
                        if (self.virtualModNames) |it| {
                            for (it.virtualModNames) |elem| {
                                writer.writeInt(u32, @intCast(@intFromEnum(elem)));
                            }
                        }
                        if (self.groups) |it| {
                            for (it.groups) |elem| {
                                writer.writeInt(u32, @intCast(@intFromEnum(elem)));
                            }
                        }
                        if (self.keyNames) |it| {
                            for (it.keyNames) |elem| {
                                try elem.encode(writer);
                            }
                        }
                        if (self.keyAliases) |it| {
                            for (it.keyAliases) |elem| {
                                try elem.encode(writer);
                            }
                        }
                        if (self.radioGroupNames) |it| {
                            for (it.radioGroupNames) |elem| {
                                writer.writeInt(u32, @intCast(@intFromEnum(elem)));
                            }
                        }
                    }

                    pub fn switchValue(self: *const @This()) u32 {
                        var result: u32 = 0;
                        if (self.keycodesName != null) result |= 1;
                        if (self.geometryName != null) result |= 2;
                        if (self.symbolsName != null) result |= 4;
                        if (self.physSymbolsName != null) result |= 8;
                        if (self.typesName != null) result |= 16;
                        if (self.compatName != null) result |= 32;
                        if (self.typeNames != null) result |= 64;
                        if (self.KTLevelNames != null) result |= 128;
                        if (self.indicatorNames != null) result |= 256;
                        if (self.virtualModNames != null) result |= 2048;
                        if (self.groups != null) result |= 4096;
                        if (self.keyNames != null) result |= 512;
                        if (self.keyAliases != null) result |= 1024;
                        if (self.radioGroupNames != null) result |= 8192;
                        return result;
                    }

                    pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader, switch_value: u32, nTypes: u8, nKeys: u8, nKeyAliases: u8, nRadioGroups: u8) !@This() {
                        var result: @This() = .{};
                        if ((switch_value & 1) != 0) {
                            const keycodesName = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
                            result.keycodesName = keycodesName;
                        }
                        if ((switch_value & 2) != 0) {
                            const geometryName = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
                            result.geometryName = geometryName;
                        }
                        if ((switch_value & 4) != 0) {
                            const symbolsName = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
                            result.symbolsName = symbolsName;
                        }
                        if ((switch_value & 8) != 0) {
                            const physSymbolsName = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
                            result.physSymbolsName = physSymbolsName;
                        }
                        if ((switch_value & 16) != 0) {
                            const typesName = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
                            result.typesName = typesName;
                        }
                        if ((switch_value & 32) != 0) {
                            const compatName = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
                            result.compatName = compatName;
                        }
                        if ((switch_value & 64) != 0) {
                            var payload: @typeInfo(@TypeOf(result.typeNames)).optional.child = undefined;
                            const decoded_typeNames_buf = try allocator.alloc(xproto.Atom, @intCast(nTypes));
                            for (decoded_typeNames_buf) |*elem| {
                                elem.* = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
                            }
                            payload.typeNames = decoded_typeNames_buf;
                            payload.decoded_typeNames_buf = decoded_typeNames_buf;
                            result.typeNames = payload;
                        }
                        if ((switch_value & 128) != 0) {
                            var payload: @typeInfo(@TypeOf(result.KTLevelNames)).optional.child = undefined;
                            const decoded_nLevelsPerType_buf = try allocator.dupe(u8, try reader.take(@intCast(nTypes)));
                            payload.nLevelsPerType = decoded_nLevelsPerType_buf;
                            payload.decoded_nLevelsPerType_buf = decoded_nLevelsPerType_buf;
                            _ = try reader.take(wire.pad(reader.seek, 4));
                            const decoded_ktLevelNames_buf = try allocator.alloc(xproto.Atom, @intCast((blk: { var total: usize = 0; for (payload.nLevelsPerType) |elem| total += @as(usize, elem); break :blk total; })));
                            for (decoded_ktLevelNames_buf) |*elem| {
                                elem.* = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
                            }
                            payload.ktLevelNames = decoded_ktLevelNames_buf;
                            payload.decoded_ktLevelNames_buf = decoded_ktLevelNames_buf;
                            result.KTLevelNames = payload;
                        }
                        if ((switch_value & 256) != 0) {
                            var payload: @typeInfo(@TypeOf(result.indicatorNames)).optional.child = undefined;
                            const decoded_indicatorNames_buf = try allocator.alloc(xproto.Atom, @intCast(@popCount(payload.indicators)));
                            for (decoded_indicatorNames_buf) |*elem| {
                                elem.* = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
                            }
                            payload.indicatorNames = decoded_indicatorNames_buf;
                            payload.decoded_indicatorNames_buf = decoded_indicatorNames_buf;
                            result.indicatorNames = payload;
                        }
                        if ((switch_value & 2048) != 0) {
                            var payload: @typeInfo(@TypeOf(result.virtualModNames)).optional.child = undefined;
                            const decoded_virtualModNames_buf = try allocator.alloc(xproto.Atom, @intCast(@popCount(payload.virtualMods)));
                            for (decoded_virtualModNames_buf) |*elem| {
                                elem.* = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
                            }
                            payload.virtualModNames = decoded_virtualModNames_buf;
                            payload.decoded_virtualModNames_buf = decoded_virtualModNames_buf;
                            result.virtualModNames = payload;
                        }
                        if ((switch_value & 4096) != 0) {
                            var payload: @typeInfo(@TypeOf(result.groups)).optional.child = undefined;
                            const decoded_groups_buf = try allocator.alloc(xproto.Atom, @intCast(@popCount(payload.groupNames)));
                            for (decoded_groups_buf) |*elem| {
                                elem.* = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
                            }
                            payload.groups = decoded_groups_buf;
                            payload.decoded_groups_buf = decoded_groups_buf;
                            result.groups = payload;
                        }
                        if ((switch_value & 512) != 0) {
                            var payload: @typeInfo(@TypeOf(result.keyNames)).optional.child = undefined;
                            const decoded_keyNames_buf = try allocator.alloc(KeyName, @intCast(nKeys));
                            for (decoded_keyNames_buf) |*elem| {
                                elem.* = try KeyName.decode(reader);
                            }
                            payload.keyNames = decoded_keyNames_buf;
                            payload.decoded_keyNames_buf = decoded_keyNames_buf;
                            result.keyNames = payload;
                        }
                        if ((switch_value & 1024) != 0) {
                            var payload: @typeInfo(@TypeOf(result.keyAliases)).optional.child = undefined;
                            const decoded_keyAliases_buf = try allocator.alloc(KeyAlias, @intCast(nKeyAliases));
                            for (decoded_keyAliases_buf) |*elem| {
                                elem.* = try KeyAlias.decode(reader);
                            }
                            payload.keyAliases = decoded_keyAliases_buf;
                            payload.decoded_keyAliases_buf = decoded_keyAliases_buf;
                            result.keyAliases = payload;
                        }
                        if ((switch_value & 8192) != 0) {
                            var payload: @typeInfo(@TypeOf(result.radioGroupNames)).optional.child = undefined;
                            const decoded_radioGroupNames_buf = try allocator.alloc(xproto.Atom, @intCast(nRadioGroups));
                            for (decoded_radioGroupNames_buf) |*elem| {
                                elem.* = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
                            }
                            payload.radioGroupNames = decoded_radioGroupNames_buf;
                            payload.decoded_radioGroupNames_buf = decoded_radioGroupNames_buf;
                            result.radioGroupNames = payload;
                        }
                        return result;
                    }

                    pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
                        if (self.typeNames) |*payload| {
                            if (payload.decoded_typeNames_buf) |buf| {
                                allocator.free(buf);
                                payload.decoded_typeNames_buf = null;
                                payload.typeNames = &.{};
                            }
                        }
                        if (self.KTLevelNames) |*payload| {
                            if (payload.decoded_nLevelsPerType_buf) |buf| {
                                allocator.free(buf);
                                payload.decoded_nLevelsPerType_buf = null;
                                payload.nLevelsPerType = &.{};
                            }
                            if (payload.decoded_ktLevelNames_buf) |buf| {
                                allocator.free(buf);
                                payload.decoded_ktLevelNames_buf = null;
                                payload.ktLevelNames = &.{};
                            }
                        }
                        if (self.indicatorNames) |*payload| {
                            if (payload.decoded_indicatorNames_buf) |buf| {
                                allocator.free(buf);
                                payload.decoded_indicatorNames_buf = null;
                                payload.indicatorNames = &.{};
                            }
                        }
                        if (self.virtualModNames) |*payload| {
                            if (payload.decoded_virtualModNames_buf) |buf| {
                                allocator.free(buf);
                                payload.decoded_virtualModNames_buf = null;
                                payload.virtualModNames = &.{};
                            }
                        }
                        if (self.groups) |*payload| {
                            if (payload.decoded_groups_buf) |buf| {
                                allocator.free(buf);
                                payload.decoded_groups_buf = null;
                                payload.groups = &.{};
                            }
                        }
                        if (self.keyNames) |*payload| {
                            if (payload.decoded_keyNames_buf) |buf| {
                                allocator.free(buf);
                                payload.decoded_keyNames_buf = null;
                                payload.keyNames = &.{};
                            }
                        }
                        if (self.keyAliases) |*payload| {
                            if (payload.decoded_keyAliases_buf) |buf| {
                                allocator.free(buf);
                                payload.decoded_keyAliases_buf = null;
                                payload.keyAliases = &.{};
                            }
                        }
                        if (self.radioGroupNames) |*payload| {
                            if (payload.decoded_radioGroupNames_buf) |buf| {
                                allocator.free(buf);
                                payload.decoded_radioGroupNames_buf = null;
                                payload.radioGroupNames = &.{};
                            }
                        }
                    }
                };
            } = null,
            geometry: ?struct {
                geometry_type: u8,
                geometryDeviceID: u8,
                geometry_sequence: u16,
                geometry_length: u32,
                name: xproto.Atom,
                geometryFound: bool,
                widthMM: u16,
                heightMM: u16,
                nProperties: u16,
                nColors: u16,
                nShapes: u16,
                nSections: u16,
                nDoodads: u16,
                nKeyAliases: u16,
                baseColorNdx: u8,
                labelColorNdx: u8,
                labelFont: CountedString16,
            } = null,

            pub fn encode(self: *const @This(), writer: anytype) !void {
                if (self.types) |it| {
                    writer.writeByte(it.getmap_type);
                    writer.writeByte(it.typeDeviceID);
                    writer.writeInt(u16, it.getmap_sequence);
                    writer.writeInt(u32, it.getmap_length);
                    writer.splatByte(0, 2);
                    writer.writeByte(it.typeMinKeyCode);
                    writer.writeByte(it.typeMaxKeyCode);
                    writer.writeInt(u16, @intCast(it.map.switchValue()));
                    writer.writeByte(it.firstType);
                    writer.writeByte(it.nTypes);
                    writer.writeByte(it.totalTypes);
                    writer.writeByte(it.firstKeySym);
                    writer.writeInt(u16, it.totalSyms);
                    writer.writeByte(it.nKeySyms);
                    writer.writeByte(it.firstKeyAction);
                    writer.writeInt(u16, it.totalActions);
                    writer.writeByte(it.nKeyActions);
                    writer.writeByte(it.firstKeyBehavior);
                    writer.writeByte(it.nKeyBehaviors);
                    writer.writeByte(it.totalKeyBehaviors);
                    writer.writeByte(it.firstKeyExplicit);
                    writer.writeByte(it.nKeyExplicit);
                    writer.writeByte(it.totalKeyExplicit);
                    writer.writeByte(it.firstModMapKey);
                    writer.writeByte(it.nModMapKeys);
                    writer.writeByte(it.totalModMapKeys);
                    writer.writeByte(it.firstVModMapKey);
                    writer.writeByte(it.nVModMapKeys);
                    writer.writeByte(it.totalVModMapKeys);
                    writer.splatByte(0, 1);
                    writer.writeInt(u16, it.virtualMods);
                    try it.map.encode(writer);
                }
                if (self.compat_map) |it| {
                    writer.writeByte(it.compatmap_type);
                    writer.writeByte(it.compatDeviceID);
                    writer.writeInt(u16, it.compatmap_sequence);
                    writer.writeInt(u32, it.compatmap_length);
                    writer.writeByte(it.groupsRtrn);
                    writer.splatByte(0, 1);
                    writer.writeInt(u16, it.firstSIRtrn);
                    writer.writeInt(u16, @intCast(it.si_rtrn.len));
                    writer.writeInt(u16, it.nTotalSI);
                    writer.splatByte(0, 16);
                    for (it.si_rtrn) |elem| {
                        try elem.encode(writer);
                    }
                    for (it.group_rtrn) |elem| {
                        try elem.encode(writer);
                    }
                }
                if (self.indicator_maps) |it| {
                    writer.writeByte(it.indicatormap_type);
                    writer.writeByte(it.indicatorDeviceID);
                    writer.writeInt(u16, it.indicatormap_sequence);
                    writer.writeInt(u32, it.indicatormap_length);
                    writer.writeInt(u32, it.which);
                    writer.writeInt(u32, it.realIndicators);
                    writer.writeByte(@intCast(it.maps.len));
                    writer.splatByte(0, 15);
                    for (it.maps) |elem| {
                        try elem.encode(writer);
                    }
                }
                if (self.key_names) |it| {
                    writer.writeByte(it.keyname_type);
                    writer.writeByte(it.keyDeviceID);
                    writer.writeInt(u16, it.keyname_sequence);
                    writer.writeInt(u32, it.keyname_length);
                    writer.writeInt(u32, @intCast(it.valueList.switchValue()));
                    writer.writeByte(it.keyMinKeyCode);
                    writer.writeByte(it.keyMaxKeyCode);
                    writer.writeByte(it.nTypes);
                    writer.writeByte(it.groupNames);
                    writer.writeInt(u16, it.virtualMods);
                    writer.writeByte(it.firstKey);
                    writer.writeByte(it.nKeys);
                    writer.writeInt(u32, it.indicators);
                    writer.writeByte(it.nRadioGroups);
                    writer.writeByte(it.nKeyAliases);
                    writer.writeInt(u16, it.nKTLevels);
                    writer.splatByte(0, 4);
                    try it.valueList.encode(writer);
                }
                if (self.geometry) |it| {
                    writer.writeByte(it.geometry_type);
                    writer.writeByte(it.geometryDeviceID);
                    writer.writeInt(u16, it.geometry_sequence);
                    writer.writeInt(u32, it.geometry_length);
                    writer.writeInt(u32, @intCast(@intFromEnum(it.name)));
                    writer.writeByte(@intFromBool(it.geometryFound));
                    writer.splatByte(0, 1);
                    writer.writeInt(u16, it.widthMM);
                    writer.writeInt(u16, it.heightMM);
                    writer.writeInt(u16, it.nProperties);
                    writer.writeInt(u16, it.nColors);
                    writer.writeInt(u16, it.nShapes);
                    writer.writeInt(u16, it.nSections);
                    writer.writeInt(u16, it.nDoodads);
                    writer.writeInt(u16, it.nKeyAliases);
                    writer.writeByte(it.baseColorNdx);
                    writer.writeByte(it.labelColorNdx);
                    try it.labelFont.encode(writer);
                }
            }

            pub fn switchValue(self: *const @This()) u32 {
                var result: u32 = 0;
                if (self.types != null) result |= 13;
                if (self.compat_map != null) result |= 2;
                if (self.indicator_maps != null) result |= 16;
                if (self.key_names != null) result |= 160;
                if (self.geometry != null) result |= 64;
                return result;
            }

            pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader, switch_value: u32) !@This() {
                var result: @This() = .{};
                if ((switch_value & 13) != 0) {
                    var payload: @typeInfo(@TypeOf(result.types)).optional.child = undefined;
                    payload.getmap_type = try reader.takeByte();
                    payload.typeDeviceID = try reader.takeByte();
                    payload.getmap_sequence = try reader.takeInt(u16, .native);
                    payload.getmap_length = try reader.takeInt(u32, .native);
                    _ = try reader.take(2);
                    payload.typeMinKeyCode = try reader.takeByte();
                    payload.typeMaxKeyCode = try reader.takeByte();
                    const present = try reader.takeInt(u16, .native);
                    payload.firstType = try reader.takeByte();
                    payload.nTypes = try reader.takeByte();
                    payload.totalTypes = try reader.takeByte();
                    payload.firstKeySym = try reader.takeByte();
                    payload.totalSyms = try reader.takeInt(u16, .native);
                    payload.nKeySyms = try reader.takeByte();
                    payload.firstKeyAction = try reader.takeByte();
                    payload.totalActions = try reader.takeInt(u16, .native);
                    payload.nKeyActions = try reader.takeByte();
                    payload.firstKeyBehavior = try reader.takeByte();
                    payload.nKeyBehaviors = try reader.takeByte();
                    payload.totalKeyBehaviors = try reader.takeByte();
                    payload.firstKeyExplicit = try reader.takeByte();
                    payload.nKeyExplicit = try reader.takeByte();
                    payload.totalKeyExplicit = try reader.takeByte();
                    payload.firstModMapKey = try reader.takeByte();
                    payload.nModMapKeys = try reader.takeByte();
                    payload.totalModMapKeys = try reader.takeByte();
                    payload.firstVModMapKey = try reader.takeByte();
                    payload.nVModMapKeys = try reader.takeByte();
                    payload.totalVModMapKeys = try reader.takeByte();
                    _ = try reader.take(1);
                    payload.virtualMods = try reader.takeInt(u16, .native);
                    payload.map = try @TypeOf(payload).Map.decode(allocator, reader, present, payload.nTypes, payload.nKeySyms, payload.nKeyActions, payload.totalActions, payload.totalKeyBehaviors, payload.totalKeyExplicit, payload.totalModMapKeys, payload.totalVModMapKeys);
                    result.types = payload;
                }
                if ((switch_value & 2) != 0) {
                    var payload: @typeInfo(@TypeOf(result.compat_map)).optional.child = undefined;
                    payload.compatmap_type = try reader.takeByte();
                    payload.compatDeviceID = try reader.takeByte();
                    payload.compatmap_sequence = try reader.takeInt(u16, .native);
                    payload.compatmap_length = try reader.takeInt(u32, .native);
                    payload.groupsRtrn = try reader.takeByte();
                    _ = try reader.take(1);
                    payload.firstSIRtrn = try reader.takeInt(u16, .native);
                    const nSIRtrn = try reader.takeInt(u16, .native);
                    payload.nTotalSI = try reader.takeInt(u16, .native);
                    _ = try reader.take(16);
                    const decoded_si_rtrn_buf = try allocator.alloc(SymInterpret, @intCast(nSIRtrn));
                    for (decoded_si_rtrn_buf) |*elem| {
                        elem.* = try SymInterpret.decode(reader);
                    }
                    payload.si_rtrn = decoded_si_rtrn_buf;
                    payload.decoded_si_rtrn_buf = decoded_si_rtrn_buf;
                    const decoded_group_rtrn_buf = try allocator.alloc(ModDef, @intCast(@popCount(payload.groupsRtrn)));
                    for (decoded_group_rtrn_buf) |*elem| {
                        elem.* = try ModDef.decode(reader);
                    }
                    payload.group_rtrn = decoded_group_rtrn_buf;
                    payload.decoded_group_rtrn_buf = decoded_group_rtrn_buf;
                    result.compat_map = payload;
                }
                if ((switch_value & 16) != 0) {
                    var payload: @typeInfo(@TypeOf(result.indicator_maps)).optional.child = undefined;
                    payload.indicatormap_type = try reader.takeByte();
                    payload.indicatorDeviceID = try reader.takeByte();
                    payload.indicatormap_sequence = try reader.takeInt(u16, .native);
                    payload.indicatormap_length = try reader.takeInt(u32, .native);
                    payload.which = try reader.takeInt(u32, .native);
                    payload.realIndicators = try reader.takeInt(u32, .native);
                    const nIndicators = try reader.takeByte();
                    _ = try reader.take(15);
                    const decoded_maps_buf = try allocator.alloc(IndicatorMap, @intCast(nIndicators));
                    for (decoded_maps_buf) |*elem| {
                        elem.* = try IndicatorMap.decode(reader);
                    }
                    payload.maps = decoded_maps_buf;
                    payload.decoded_maps_buf = decoded_maps_buf;
                    result.indicator_maps = payload;
                }
                if ((switch_value & 160) != 0) {
                    var payload: @typeInfo(@TypeOf(result.key_names)).optional.child = undefined;
                    payload.keyname_type = try reader.takeByte();
                    payload.keyDeviceID = try reader.takeByte();
                    payload.keyname_sequence = try reader.takeInt(u16, .native);
                    payload.keyname_length = try reader.takeInt(u32, .native);
                    const which = try reader.takeInt(u32, .native);
                    payload.keyMinKeyCode = try reader.takeByte();
                    payload.keyMaxKeyCode = try reader.takeByte();
                    payload.nTypes = try reader.takeByte();
                    payload.groupNames = try reader.takeByte();
                    payload.virtualMods = try reader.takeInt(u16, .native);
                    payload.firstKey = try reader.takeByte();
                    payload.nKeys = try reader.takeByte();
                    payload.indicators = try reader.takeInt(u32, .native);
                    payload.nRadioGroups = try reader.takeByte();
                    payload.nKeyAliases = try reader.takeByte();
                    payload.nKTLevels = try reader.takeInt(u16, .native);
                    _ = try reader.take(4);
                    payload.valueList = try @TypeOf(payload).ValueList.decode(allocator, reader, which, payload.nTypes, payload.nKeys, payload.nKeyAliases, payload.nRadioGroups);
                    result.key_names = payload;
                }
                if ((switch_value & 64) != 0) {
                    var payload: @typeInfo(@TypeOf(result.geometry)).optional.child = undefined;
                    payload.geometry_type = try reader.takeByte();
                    payload.geometryDeviceID = try reader.takeByte();
                    payload.geometry_sequence = try reader.takeInt(u16, .native);
                    payload.geometry_length = try reader.takeInt(u32, .native);
                    payload.name = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
                    payload.geometryFound = (try reader.takeByte()) != 0;
                    _ = try reader.take(1);
                    payload.widthMM = try reader.takeInt(u16, .native);
                    payload.heightMM = try reader.takeInt(u16, .native);
                    payload.nProperties = try reader.takeInt(u16, .native);
                    payload.nColors = try reader.takeInt(u16, .native);
                    payload.nShapes = try reader.takeInt(u16, .native);
                    payload.nSections = try reader.takeInt(u16, .native);
                    payload.nDoodads = try reader.takeInt(u16, .native);
                    payload.nKeyAliases = try reader.takeInt(u16, .native);
                    payload.baseColorNdx = try reader.takeByte();
                    payload.labelColorNdx = try reader.takeByte();
                    payload.labelFont = try CountedString16.decode(allocator, reader);
                    result.geometry = payload;
                }
                return result;
            }

            pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
                if (self.types) |*payload| {
                    payload.map.deinit(allocator);
                }
                if (self.compat_map) |*payload| {
                    if (payload.decoded_si_rtrn_buf) |buf| {
                        allocator.free(buf);
                        payload.decoded_si_rtrn_buf = null;
                        payload.si_rtrn = &.{};
                    }
                    if (payload.decoded_group_rtrn_buf) |buf| {
                        allocator.free(buf);
                        payload.decoded_group_rtrn_buf = null;
                        payload.group_rtrn = &.{};
                    }
                }
                if (self.indicator_maps) |*payload| {
                    if (payload.decoded_maps_buf) |buf| {
                        allocator.free(buf);
                        payload.decoded_maps_buf = null;
                        payload.maps = &.{};
                    }
                }
                if (self.key_names) |*payload| {
                    payload.valueList.deinit(allocator);
                }
                if (self.geometry) |*payload| {
                    payload.labelFont.deinit(allocator);
                }
            }
        };

        pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader, header_: wire.ReplyHeader) !@This() {
            var result: @This() = undefined;
            _ = header_;
            result.deviceID = try reader.takeByte();
            result.minKeyCode = try reader.takeByte();
            result.maxKeyCode = try reader.takeByte();
            result.loaded = (try reader.takeByte()) != 0;
            result.newKeyboard = (try reader.takeByte()) != 0;
            result.found = try reader.takeInt(u16, .native);
            const reported = try reader.takeInt(u16, .native);
            _ = try reader.take(16);
            result.replies = try @TypeOf(result).Replies.decode(allocator, reader, reported);
            return result;
        }

        pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
            self.replies.deinit(allocator);
        }
    };


    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u16, self.deviceSpec);
        writer.writeInt(u16, self.need);
        writer.writeInt(u16, self.want);
        writer.writeByte(@intFromBool(self.load));
        writer.splatByte(0, 1);
    }
};

pub const GetDeviceInfo = struct {
    pub const opcode: u8 = 24;

    deviceSpec: u16,
    wanted: u16,
    allButtons: bool,
    firstButton: u8,
    nButtons: u8,
    ledClass: LedClass,
    ledID: u16,

    pub const Reply = struct {
        deviceID: u8,
        present: u16,
        supported: u16,
        unsupported: u16,
        firstBtnWanted: u8,
        nBtnsWanted: u8,
        firstBtnRtrn: u8,
        totalBtns: u8,
        hasOwnState: bool,
        dfltKbdFB: u16,
        dfltLedFB: u16,
        devType: xproto.Atom,
        name: []const u8,
        decoded_name_buf: ?[]u8 = null,
        btnActions: []const Action,
        decoded_btnActions_buf: ?[]Action = null,
        leds: []const DeviceLedInfo,
        decoded_leds_buf: ?[]DeviceLedInfo = null,

        pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader, header_: wire.ReplyHeader) !@This() {
            var result: @This() = undefined;
            _ = header_;
            result.deviceID = try reader.takeByte();
            result.present = try reader.takeInt(u16, .native);
            result.supported = try reader.takeInt(u16, .native);
            result.unsupported = try reader.takeInt(u16, .native);
            const nDeviceLedFBs = try reader.takeInt(u16, .native);
            result.firstBtnWanted = try reader.takeByte();
            result.nBtnsWanted = try reader.takeByte();
            result.firstBtnRtrn = try reader.takeByte();
            const nBtnsRtrn = try reader.takeByte();
            result.totalBtns = try reader.takeByte();
            result.hasOwnState = (try reader.takeByte()) != 0;
            result.dfltKbdFB = try reader.takeInt(u16, .native);
            result.dfltLedFB = try reader.takeInt(u16, .native);
            _ = try reader.take(2);
            result.devType = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
            const nameLen = try reader.takeInt(u16, .native);
            const decoded_name_buf = try allocator.dupe(u8, try reader.take(@intCast(nameLen)));
            result.name = decoded_name_buf;
            result.decoded_name_buf = decoded_name_buf;
            _ = try reader.take(wire.pad(reader.seek, 4));
            const decoded_btnActions_buf = try allocator.alloc(Action, @intCast(nBtnsRtrn));
            for (decoded_btnActions_buf) |*elem| {
                elem.* = try Action.decode(reader);
            }
            result.btnActions = decoded_btnActions_buf;
            result.decoded_btnActions_buf = decoded_btnActions_buf;
            const decoded_leds_buf = try allocator.alloc(DeviceLedInfo, @intCast(nDeviceLedFBs));
            for (decoded_leds_buf) |*elem| {
                elem.* = try DeviceLedInfo.decode(allocator, reader);
            }
            result.leds = decoded_leds_buf;
            result.decoded_leds_buf = decoded_leds_buf;
            return result;
        }

        pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
            if (self.decoded_name_buf) |buf| {
                allocator.free(buf);
                self.decoded_name_buf = null;
                self.name = &.{};
            }
            if (self.decoded_btnActions_buf) |buf| {
                allocator.free(buf);
                self.decoded_btnActions_buf = null;
                self.btnActions = &.{};
            }
            if (self.decoded_leds_buf) |buf| {
                for (buf) |*it| {
                    it.deinit(allocator);
                }
                allocator.free(buf);
                self.decoded_leds_buf = null;
                self.leds = &.{};
            }
        }
    };


    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u16, self.deviceSpec);
        writer.writeInt(u16, self.wanted);
        writer.writeByte(@intFromBool(self.allButtons));
        writer.writeByte(self.firstButton);
        writer.writeByte(self.nButtons);
        writer.splatByte(0, 1);
        writer.writeInt(u16, @intCast(@intFromEnum(self.ledClass)));
        writer.writeInt(u16, self.ledID);
    }
};

pub const SetDeviceInfo = struct {
    pub const opcode: u8 = 25;

    deviceSpec: u16,
    firstBtn: u8,
    change: u16,
    btnActions: []const Action,
    decoded_btnActions_buf: ?[]Action = null,
    leds: []const DeviceLedInfo,
    decoded_leds_buf: ?[]DeviceLedInfo = null,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u16, self.deviceSpec);
        writer.writeByte(self.firstBtn);
        writer.writeByte(@intCast(self.btnActions.len));
        writer.writeInt(u16, self.change);
        writer.writeInt(u16, @intCast(self.leds.len));
        for (self.btnActions) |elem| {
            try elem.encode(writer);
        }
        for (self.leds) |elem| {
            try elem.encode(writer);
        }
    }
};

pub const SetDebuggingFlags = struct {
    pub const opcode: u8 = 101;

    affectFlags: u32,
    flags: u32,
    affectCtrls: u32,
    ctrls: u32,
    message: []const u8,
    decoded_message_buf: ?[]u8 = null,

    pub const Reply = struct {
        currentFlags: u32,
        currentCtrls: u32,
        supportedFlags: u32,
        supportedCtrls: u32,

        pub fn decode(reader: *std.Io.Reader, header_: wire.ReplyHeader) !@This() {
            var result: @This() = undefined;
            _ = header_;
            _ = try reader.take(1);
            result.currentFlags = try reader.takeInt(u32, .native);
            result.currentCtrls = try reader.takeInt(u32, .native);
            result.supportedFlags = try reader.takeInt(u32, .native);
            result.supportedCtrls = try reader.takeInt(u32, .native);
            _ = try reader.take(8);
            return result;
        }
    };


    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u16, @intCast(self.message.len));
        writer.splatByte(0, 2);
        writer.writeInt(u32, self.affectFlags);
        writer.writeInt(u32, self.flags);
        writer.writeInt(u32, self.affectCtrls);
        writer.writeInt(u32, self.ctrls);
        writer.write(self.message);
    }
};

pub const NewKeyboardNotifyEvent = struct {
    xkbType: u8,
    time: u32,
    deviceID: u8,
    oldDeviceID: u8,
    minKeyCode: u8,
    maxKeyCode: u8,
    oldMinKeyCode: u8,
    oldMaxKeyCode: u8,
    requestMajor: u8,
    requestMinor: u8,
    changed: u16,

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        var result: @This() = undefined;
        _ = try reader.takeByte();
        result.xkbType = try reader.takeByte();
        _ = try reader.takeInt(u16, .native);
        result.time = try reader.takeInt(u32, .native);
        result.deviceID = try reader.takeByte();
        result.oldDeviceID = try reader.takeByte();
        result.minKeyCode = try reader.takeByte();
        result.maxKeyCode = try reader.takeByte();
        result.oldMinKeyCode = try reader.takeByte();
        result.oldMaxKeyCode = try reader.takeByte();
        result.requestMajor = try reader.takeByte();
        result.requestMinor = try reader.takeByte();
        result.changed = try reader.takeInt(u16, .native);
        _ = try reader.take(14);
        return result;
    }
};

pub const MapNotifyEvent = struct {
    xkbType: u8,
    time: u32,
    deviceID: u8,
    ptrBtnActions: u8,
    changed: u16,
    minKeyCode: u8,
    maxKeyCode: u8,
    firstType: u8,
    nTypes: u8,
    firstKeySym: u8,
    nKeySyms: u8,
    firstKeyAct: u8,
    nKeyActs: u8,
    firstKeyBehavior: u8,
    nKeyBehavior: u8,
    firstKeyExplicit: u8,
    nKeyExplicit: u8,
    firstModMapKey: u8,
    nModMapKeys: u8,
    firstVModMapKey: u8,
    nVModMapKeys: u8,
    virtualMods: u16,

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        var result: @This() = undefined;
        _ = try reader.takeByte();
        result.xkbType = try reader.takeByte();
        _ = try reader.takeInt(u16, .native);
        result.time = try reader.takeInt(u32, .native);
        result.deviceID = try reader.takeByte();
        result.ptrBtnActions = try reader.takeByte();
        result.changed = try reader.takeInt(u16, .native);
        result.minKeyCode = try reader.takeByte();
        result.maxKeyCode = try reader.takeByte();
        result.firstType = try reader.takeByte();
        result.nTypes = try reader.takeByte();
        result.firstKeySym = try reader.takeByte();
        result.nKeySyms = try reader.takeByte();
        result.firstKeyAct = try reader.takeByte();
        result.nKeyActs = try reader.takeByte();
        result.firstKeyBehavior = try reader.takeByte();
        result.nKeyBehavior = try reader.takeByte();
        result.firstKeyExplicit = try reader.takeByte();
        result.nKeyExplicit = try reader.takeByte();
        result.firstModMapKey = try reader.takeByte();
        result.nModMapKeys = try reader.takeByte();
        result.firstVModMapKey = try reader.takeByte();
        result.nVModMapKeys = try reader.takeByte();
        result.virtualMods = try reader.takeInt(u16, .native);
        _ = try reader.take(2);
        return result;
    }
};

pub const StateNotifyEvent = struct {
    xkbType: u8,
    time: u32,
    deviceID: u8,
    mods: u8,
    baseMods: u8,
    latchedMods: u8,
    lockedMods: u8,
    group: Group,
    baseGroup: i16,
    latchedGroup: i16,
    lockedGroup: Group,
    compatState: u8,
    grabMods: u8,
    compatGrabMods: u8,
    lookupMods: u8,
    compatLoockupMods: u8,
    ptrBtnState: u16,
    changed: u16,
    keycode: u8,
    eventType: u8,
    requestMajor: u8,
    requestMinor: u8,

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        var result: @This() = undefined;
        _ = try reader.takeByte();
        result.xkbType = try reader.takeByte();
        _ = try reader.takeInt(u16, .native);
        result.time = try reader.takeInt(u32, .native);
        result.deviceID = try reader.takeByte();
        result.mods = try reader.takeByte();
        result.baseMods = try reader.takeByte();
        result.latchedMods = try reader.takeByte();
        result.lockedMods = try reader.takeByte();
        result.group = @as(Group, @enumFromInt(try reader.takeByte()));
        result.baseGroup = try reader.takeInt(i16, .native);
        result.latchedGroup = try reader.takeInt(i16, .native);
        result.lockedGroup = @as(Group, @enumFromInt(try reader.takeByte()));
        result.compatState = try reader.takeByte();
        result.grabMods = try reader.takeByte();
        result.compatGrabMods = try reader.takeByte();
        result.lookupMods = try reader.takeByte();
        result.compatLoockupMods = try reader.takeByte();
        result.ptrBtnState = try reader.takeInt(u16, .native);
        result.changed = try reader.takeInt(u16, .native);
        result.keycode = try reader.takeByte();
        result.eventType = try reader.takeByte();
        result.requestMajor = try reader.takeByte();
        result.requestMinor = try reader.takeByte();
        return result;
    }
};

pub const ControlsNotifyEvent = struct {
    xkbType: u8,
    time: u32,
    deviceID: u8,
    numGroups: u8,
    changedControls: u32,
    enabledControls: u32,
    enabledControlChanges: u32,
    keycode: u8,
    eventType: u8,
    requestMajor: u8,
    requestMinor: u8,

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        var result: @This() = undefined;
        _ = try reader.takeByte();
        result.xkbType = try reader.takeByte();
        _ = try reader.takeInt(u16, .native);
        result.time = try reader.takeInt(u32, .native);
        result.deviceID = try reader.takeByte();
        result.numGroups = try reader.takeByte();
        _ = try reader.take(2);
        result.changedControls = try reader.takeInt(u32, .native);
        result.enabledControls = try reader.takeInt(u32, .native);
        result.enabledControlChanges = try reader.takeInt(u32, .native);
        result.keycode = try reader.takeByte();
        result.eventType = try reader.takeByte();
        result.requestMajor = try reader.takeByte();
        result.requestMinor = try reader.takeByte();
        _ = try reader.take(4);
        return result;
    }
};

pub const IndicatorStateNotifyEvent = struct {
    xkbType: u8,
    time: u32,
    deviceID: u8,
    state: u32,
    stateChanged: u32,

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        var result: @This() = undefined;
        _ = try reader.takeByte();
        result.xkbType = try reader.takeByte();
        _ = try reader.takeInt(u16, .native);
        result.time = try reader.takeInt(u32, .native);
        result.deviceID = try reader.takeByte();
        _ = try reader.take(3);
        result.state = try reader.takeInt(u32, .native);
        result.stateChanged = try reader.takeInt(u32, .native);
        _ = try reader.take(12);
        return result;
    }
};

pub const IndicatorMapNotifyEvent = struct {
    xkbType: u8,
    time: u32,
    deviceID: u8,
    state: u32,
    mapChanged: u32,

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        var result: @This() = undefined;
        _ = try reader.takeByte();
        result.xkbType = try reader.takeByte();
        _ = try reader.takeInt(u16, .native);
        result.time = try reader.takeInt(u32, .native);
        result.deviceID = try reader.takeByte();
        _ = try reader.take(3);
        result.state = try reader.takeInt(u32, .native);
        result.mapChanged = try reader.takeInt(u32, .native);
        _ = try reader.take(12);
        return result;
    }
};

pub const NamesNotifyEvent = struct {
    xkbType: u8,
    time: u32,
    deviceID: u8,
    changed: u16,
    firstType: u8,
    nTypes: u8,
    firstLevelName: u8,
    nLevelNames: u8,
    nRadioGroups: u8,
    nKeyAliases: u8,
    changedGroupNames: u8,
    changedVirtualMods: u16,
    firstKey: u8,
    nKeys: u8,
    changedIndicators: u32,

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        var result: @This() = undefined;
        _ = try reader.takeByte();
        result.xkbType = try reader.takeByte();
        _ = try reader.takeInt(u16, .native);
        result.time = try reader.takeInt(u32, .native);
        result.deviceID = try reader.takeByte();
        _ = try reader.take(1);
        result.changed = try reader.takeInt(u16, .native);
        result.firstType = try reader.takeByte();
        result.nTypes = try reader.takeByte();
        result.firstLevelName = try reader.takeByte();
        result.nLevelNames = try reader.takeByte();
        _ = try reader.take(1);
        result.nRadioGroups = try reader.takeByte();
        result.nKeyAliases = try reader.takeByte();
        result.changedGroupNames = try reader.takeByte();
        result.changedVirtualMods = try reader.takeInt(u16, .native);
        result.firstKey = try reader.takeByte();
        result.nKeys = try reader.takeByte();
        result.changedIndicators = try reader.takeInt(u32, .native);
        _ = try reader.take(4);
        return result;
    }
};

pub const CompatMapNotifyEvent = struct {
    xkbType: u8,
    time: u32,
    deviceID: u8,
    changedGroups: u8,
    firstSI: u16,
    nSI: u16,
    nTotalSI: u16,

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        var result: @This() = undefined;
        _ = try reader.takeByte();
        result.xkbType = try reader.takeByte();
        _ = try reader.takeInt(u16, .native);
        result.time = try reader.takeInt(u32, .native);
        result.deviceID = try reader.takeByte();
        result.changedGroups = try reader.takeByte();
        result.firstSI = try reader.takeInt(u16, .native);
        result.nSI = try reader.takeInt(u16, .native);
        result.nTotalSI = try reader.takeInt(u16, .native);
        _ = try reader.take(16);
        return result;
    }
};

pub const BellNotifyEvent = struct {
    xkbType: u8,
    time: u32,
    deviceID: u8,
    bellClass: BellClassResult,
    bellID: u8,
    percent: u8,
    pitch: u16,
    duration: u16,
    name: xproto.Atom,
    window: xproto.Window,
    eventOnly: bool,

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        var result: @This() = undefined;
        _ = try reader.takeByte();
        result.xkbType = try reader.takeByte();
        _ = try reader.takeInt(u16, .native);
        result.time = try reader.takeInt(u32, .native);
        result.deviceID = try reader.takeByte();
        result.bellClass = @as(BellClassResult, @enumFromInt(try reader.takeByte()));
        result.bellID = try reader.takeByte();
        result.percent = try reader.takeByte();
        result.pitch = try reader.takeInt(u16, .native);
        result.duration = try reader.takeInt(u16, .native);
        result.name = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
        result.window = @as(xproto.Window, @enumFromInt(try reader.takeInt(u32, .native)));
        result.eventOnly = (try reader.takeByte()) != 0;
        _ = try reader.take(7);
        return result;
    }
};

pub const ActionMessageEvent = struct {
    xkbType: u8,
    time: u32,
    deviceID: u8,
    keycode: u8,
    press: bool,
    keyEventFollows: bool,
    mods: u8,
    group: Group,
    message: [8]u8,

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        var result: @This() = undefined;
        _ = try reader.takeByte();
        result.xkbType = try reader.takeByte();
        _ = try reader.takeInt(u16, .native);
        result.time = try reader.takeInt(u32, .native);
        result.deviceID = try reader.takeByte();
        result.keycode = try reader.takeByte();
        result.press = (try reader.takeByte()) != 0;
        result.keyEventFollows = (try reader.takeByte()) != 0;
        result.mods = try reader.takeByte();
        result.group = @as(Group, @enumFromInt(try reader.takeByte()));
        for (&result.message) |*elem| {
            elem.* = try reader.takeByte();
        }
        _ = try reader.take(10);
        return result;
    }
};

pub const AccessXNotifyEvent = struct {
    xkbType: u8,
    time: u32,
    deviceID: u8,
    keycode: u8,
    detailt: u16,
    slowKeysDelay: u16,
    debounceDelay: u16,

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        var result: @This() = undefined;
        _ = try reader.takeByte();
        result.xkbType = try reader.takeByte();
        _ = try reader.takeInt(u16, .native);
        result.time = try reader.takeInt(u32, .native);
        result.deviceID = try reader.takeByte();
        result.keycode = try reader.takeByte();
        result.detailt = try reader.takeInt(u16, .native);
        result.slowKeysDelay = try reader.takeInt(u16, .native);
        result.debounceDelay = try reader.takeInt(u16, .native);
        _ = try reader.take(16);
        return result;
    }
};

pub const ExtensionDeviceNotifyEvent = struct {
    xkbType: u8,
    time: u32,
    deviceID: u8,
    reason: u16,
    ledClass: LedClassResult,
    ledID: u16,
    ledsDefined: u32,
    ledState: u32,
    firstButton: u8,
    nButtons: u8,
    supported: u16,
    unsupported: u16,

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        var result: @This() = undefined;
        _ = try reader.takeByte();
        result.xkbType = try reader.takeByte();
        _ = try reader.takeInt(u16, .native);
        result.time = try reader.takeInt(u32, .native);
        result.deviceID = try reader.takeByte();
        _ = try reader.take(1);
        result.reason = try reader.takeInt(u16, .native);
        result.ledClass = @as(LedClassResult, @enumFromInt(try reader.takeInt(u16, .native)));
        result.ledID = try reader.takeInt(u16, .native);
        result.ledsDefined = try reader.takeInt(u32, .native);
        result.ledState = try reader.takeInt(u32, .native);
        result.firstButton = try reader.takeByte();
        result.nButtons = try reader.takeByte();
        result.supported = try reader.takeInt(u16, .native);
        result.unsupported = try reader.takeInt(u16, .native);
        _ = try reader.take(2);
        return result;
    }
};

pub fn decodeEvent(reader: *std.Io.Reader) DecodeError!global_events.Event {
    const code = (try reader.peek(1))[0] & 0x7f;
    return switch (code) {
        0 => .{ .XkbNewKeyboardNotify = try NewKeyboardNotifyEvent.decode(reader) },
        1 => .{ .XkbMapNotify = try MapNotifyEvent.decode(reader) },
        2 => .{ .XkbStateNotify = try StateNotifyEvent.decode(reader) },
        3 => .{ .XkbControlsNotify = try ControlsNotifyEvent.decode(reader) },
        4 => .{ .XkbIndicatorStateNotify = try IndicatorStateNotifyEvent.decode(reader) },
        5 => .{ .XkbIndicatorMapNotify = try IndicatorMapNotifyEvent.decode(reader) },
        6 => .{ .XkbNamesNotify = try NamesNotifyEvent.decode(reader) },
        7 => .{ .XkbCompatMapNotify = try CompatMapNotifyEvent.decode(reader) },
        8 => .{ .XkbBellNotify = try BellNotifyEvent.decode(reader) },
        9 => .{ .XkbActionMessage = try ActionMessageEvent.decode(reader) },
        10 => .{ .XkbAccessXNotify = try AccessXNotifyEvent.decode(reader) },
        11 => .{ .XkbExtensionDeviceNotify = try ExtensionDeviceNotifyEvent.decode(reader) },
        else => blk: {
            const packet = try reader.take(32);
            var raw: [32]u8 = undefined;
            @memcpy(raw[0..], packet);
            break :blk .{ .Unknown = .{
                .code = packet[0] & 0x7f,
                .sequence = std.mem.readInt(u16, packet[2..4], .native),
                .raw = raw,
            } };
        },
    };
}

