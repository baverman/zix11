// zig fmt: off
// This file is generated by tools/genproto.py

const std = @import("std");
const zio = @import("../io.zig");
const wire = @import("../_wire.zig");
const errors = @import("../_errors.zig");
const extensions = @import("../_ext.zig");
const global_events = @import("events.zig");
const xproto = @import("xproto.zig");
const render = @import("render.zig");
const DecodeError = errors.DecodeError;
const AllocDecodeError = errors.AllocDecodeError;
const BufferDecodeError = errors.BufferDecodeError;

pub const Crtc = enum(u32) {
    _,
};

pub const Lease = enum(u32) {
    _,
};

pub const Mode = enum(u32) {
    _,
};

pub const Output = enum(u32) {
    _,
};

pub const Provider = enum(u32) {
    _,
};

pub const NotifyData = struct {
    raw: [28]u8,

    pub fn fromRaw(raw: [28]u8) @This() {
        return .{ .raw = raw };
    }

    pub fn asRaw(self: @This()) [28]u8 {
        return self.raw;
    }

    pub fn fromEvent(event: anytype) @This() {
        return .{ .raw = event.toBytes() };
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.write(self.raw[0..]);
    }

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        var raw: [28]u8 = undefined;
        @memcpy(raw[0..], try reader.take(28));
        return .{ .raw = raw };
    }

    pub fn fromCc(value: CrtcChange) @This() {
        var raw = std.mem.zeroes([28]u8);
        var writer_impl = zio.FixedBufferWriter.init(&raw);
        const writer = &writer_impl;
        value.encode(writer);
        return .{ .raw = raw };
    }

    pub fn asCc(self: @This()) DecodeError!CrtcChange {
        var reader_impl: std.Io.Reader = .fixed(&self.raw);
        const reader = &reader_impl;
        const value = try CrtcChange.decode(reader);
        return value;
    }

    pub fn fromOc(value: OutputChange) @This() {
        var raw = std.mem.zeroes([28]u8);
        var writer_impl = zio.FixedBufferWriter.init(&raw);
        const writer = &writer_impl;
        value.encode(writer);
        return .{ .raw = raw };
    }

    pub fn asOc(self: @This()) DecodeError!OutputChange {
        var reader_impl: std.Io.Reader = .fixed(&self.raw);
        const reader = &reader_impl;
        const value = try OutputChange.decode(reader);
        return value;
    }

    pub fn fromOp(value: OutputProperty) @This() {
        var raw = std.mem.zeroes([28]u8);
        var writer_impl = zio.FixedBufferWriter.init(&raw);
        const writer = &writer_impl;
        value.encode(writer);
        return .{ .raw = raw };
    }

    pub fn asOp(self: @This()) DecodeError!OutputProperty {
        var reader_impl: std.Io.Reader = .fixed(&self.raw);
        const reader = &reader_impl;
        const value = try OutputProperty.decode(reader);
        return value;
    }

    pub fn fromPc(value: ProviderChange) @This() {
        var raw = std.mem.zeroes([28]u8);
        var writer_impl = zio.FixedBufferWriter.init(&raw);
        const writer = &writer_impl;
        value.encode(writer);
        return .{ .raw = raw };
    }

    pub fn asPc(self: @This()) DecodeError!ProviderChange {
        var reader_impl: std.Io.Reader = .fixed(&self.raw);
        const reader = &reader_impl;
        const value = try ProviderChange.decode(reader);
        return value;
    }

    pub fn fromPp(value: ProviderProperty) @This() {
        var raw = std.mem.zeroes([28]u8);
        var writer_impl = zio.FixedBufferWriter.init(&raw);
        const writer = &writer_impl;
        value.encode(writer);
        return .{ .raw = raw };
    }

    pub fn asPp(self: @This()) DecodeError!ProviderProperty {
        var reader_impl: std.Io.Reader = .fixed(&self.raw);
        const reader = &reader_impl;
        const value = try ProviderProperty.decode(reader);
        return value;
    }

    pub fn fromRc(value: ResourceChange) @This() {
        var raw = std.mem.zeroes([28]u8);
        var writer_impl = zio.FixedBufferWriter.init(&raw);
        const writer = &writer_impl;
        value.encode(writer);
        return .{ .raw = raw };
    }

    pub fn asRc(self: @This()) DecodeError!ResourceChange {
        var reader_impl: std.Io.Reader = .fixed(&self.raw);
        const reader = &reader_impl;
        const value = try ResourceChange.decode(reader);
        return value;
    }

    pub fn fromLc(value: LeaseNotify) @This() {
        var raw = std.mem.zeroes([28]u8);
        var writer_impl = zio.FixedBufferWriter.init(&raw);
        const writer = &writer_impl;
        value.encode(writer);
        return .{ .raw = raw };
    }

    pub fn asLc(self: @This()) DecodeError!LeaseNotify {
        var reader_impl: std.Io.Reader = .fixed(&self.raw);
        const reader = &reader_impl;
        const value = try LeaseNotify.decode(reader);
        return value;
    }
};

pub const Error = enum(u32) {
    RandrBadOutput = 0,
    RandrBadCrtc = 1,
    RandrBadMode = 2,
    RandrBadProvider = 3,
    _,
};

pub const Connection = enum(u32) {
    Connected = 0,
    Disconnected = 1,
    Unknown = 2,
    _,
};

pub const ModeFlag = enum(u32) {
    HsyncPositive = 1,
    HsyncNegative = 2,
    VsyncPositive = 4,
    VsyncNegative = 8,
    Interlace = 16,
    DoubleScan = 32,
    Csync = 64,
    CsyncPositive = 128,
    CsyncNegative = 256,
    HskewPresent = 512,
    Bcast = 1024,
    PixelMultiplex = 2048,
    DoubleClock = 4096,
    HalveClock = 8192,
    _,

    pub fn of(flags: []const @This()) u32 {
        return wire.maskOf(@This(), flags);
    }
};

pub const Notify = enum(u32) {
    CrtcChange = 0,
    OutputChange = 1,
    OutputProperty = 2,
    ProviderChange = 3,
    ProviderProperty = 4,
    ResourceChange = 5,
    Lease = 6,
    _,
};

pub const NotifyMask = enum(u32) {
    ScreenChange = 1,
    CrtcChange = 2,
    OutputChange = 4,
    OutputProperty = 8,
    ProviderChange = 16,
    ProviderProperty = 32,
    ResourceChange = 64,
    Lease = 128,
    _,

    pub fn of(flags: []const @This()) u32 {
        return wire.maskOf(@This(), flags);
    }
};

pub const ProviderCapability = enum(u32) {
    SourceOutput = 1,
    SinkOutput = 2,
    SourceOffload = 4,
    SinkOffload = 8,
    _,

    pub fn of(flags: []const @This()) u32 {
        return wire.maskOf(@This(), flags);
    }
};

pub const Rotation = enum(u32) {
    Rotate0 = 1,
    Rotate90 = 2,
    Rotate180 = 4,
    Rotate270 = 8,
    ReflectX = 16,
    ReflectY = 32,
    _,

    pub fn of(flags: []const @This()) u32 {
        return wire.maskOf(@This(), flags);
    }
};

pub const SetConfig = enum(u32) {
    Success = 0,
    InvalidConfigTime = 1,
    InvalidTime = 2,
    Failed = 3,
    _,
};

pub const Transform = enum(u32) {
    Unit = 1,
    ScaleUp = 2,
    ScaleDown = 4,
    Projective = 8,
    _,
};

pub const CrtcChange = struct {
    timestamp: u32,
    window: xproto.Window,
    crtc: Crtc,
    mode: Mode,
    rotation: u16,
    x: i16,
    y: i16,
    width: u16,
    height: u16,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, self.timestamp);
        writer.writeInt(u32, @intFromEnum(self.window));
        writer.writeInt(u32, @intFromEnum(self.crtc));
        writer.writeInt(u32, @intFromEnum(self.mode));
        writer.writeInt(u16, self.rotation);
        writer.splatByte(0, 2);
        writer.writeInt(i16, self.x);
        writer.writeInt(i16, self.y);
        writer.writeInt(u16, self.width);
        writer.writeInt(u16, self.height);
    }

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        const timestamp = try reader.takeInt(u32, .native);
        const window = @as(xproto.Window, @enumFromInt(try reader.takeInt(u32, .native)));
        const crtc = @as(Crtc, @enumFromInt(try reader.takeInt(u32, .native)));
        const mode = @as(Mode, @enumFromInt(try reader.takeInt(u32, .native)));
        const rotation = try reader.takeInt(u16, .native);
        _ = try reader.take(2);
        const x = try reader.takeInt(i16, .native);
        const y = try reader.takeInt(i16, .native);
        const width = try reader.takeInt(u16, .native);
        const height = try reader.takeInt(u16, .native);
        return .{
            .timestamp = timestamp,
            .window = window,
            .crtc = crtc,
            .mode = mode,
            .rotation = rotation,
            .x = x,
            .y = y,
            .width = width,
            .height = height,
        };
    }

};

pub const LeaseNotify = struct {
    timestamp: u32,
    window: xproto.Window,
    lease: Lease,
    created: u8,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, self.timestamp);
        writer.writeInt(u32, @intFromEnum(self.window));
        writer.writeInt(u32, @intFromEnum(self.lease));
        writer.writeByte(self.created);
        writer.splatByte(0, 15);
    }

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        const timestamp = try reader.takeInt(u32, .native);
        const window = @as(xproto.Window, @enumFromInt(try reader.takeInt(u32, .native)));
        const lease = @as(Lease, @enumFromInt(try reader.takeInt(u32, .native)));
        const created = try reader.takeByte();
        _ = try reader.take(15);
        return .{
            .timestamp = timestamp,
            .window = window,
            .lease = lease,
            .created = created,
        };
    }

};

pub const ModeInfo = struct {
    id: u32,
    width: u16,
    height: u16,
    dot_clock: u32,
    hsync_start: u16,
    hsync_end: u16,
    htotal: u16,
    hskew: u16,
    vsync_start: u16,
    vsync_end: u16,
    vtotal: u16,
    name_len: u16,
    mode_flags: u32,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, self.id);
        writer.writeInt(u16, self.width);
        writer.writeInt(u16, self.height);
        writer.writeInt(u32, self.dot_clock);
        writer.writeInt(u16, self.hsync_start);
        writer.writeInt(u16, self.hsync_end);
        writer.writeInt(u16, self.htotal);
        writer.writeInt(u16, self.hskew);
        writer.writeInt(u16, self.vsync_start);
        writer.writeInt(u16, self.vsync_end);
        writer.writeInt(u16, self.vtotal);
        writer.writeInt(u16, self.name_len);
        writer.writeInt(u32, self.mode_flags);
    }

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        const id = try reader.takeInt(u32, .native);
        const width = try reader.takeInt(u16, .native);
        const height = try reader.takeInt(u16, .native);
        const dot_clock = try reader.takeInt(u32, .native);
        const hsync_start = try reader.takeInt(u16, .native);
        const hsync_end = try reader.takeInt(u16, .native);
        const htotal = try reader.takeInt(u16, .native);
        const hskew = try reader.takeInt(u16, .native);
        const vsync_start = try reader.takeInt(u16, .native);
        const vsync_end = try reader.takeInt(u16, .native);
        const vtotal = try reader.takeInt(u16, .native);
        const name_len = try reader.takeInt(u16, .native);
        const mode_flags = try reader.takeInt(u32, .native);
        return .{
            .id = id,
            .width = width,
            .height = height,
            .dot_clock = dot_clock,
            .hsync_start = hsync_start,
            .hsync_end = hsync_end,
            .htotal = htotal,
            .hskew = hskew,
            .vsync_start = vsync_start,
            .vsync_end = vsync_end,
            .vtotal = vtotal,
            .name_len = name_len,
            .mode_flags = mode_flags,
        };
    }

};

pub const MonitorInfo = struct {
    name: xproto.Atom,
    primary: bool,
    automatic: bool,
    x: i16,
    y: i16,
    width: u16,
    height: u16,
    width_in_millimeters: u32,
    height_in_millimeters: u32,
    outputs: []const Output,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.name));
        writer.writeByte(@intFromBool(self.primary));
        writer.writeByte(@intFromBool(self.automatic));
        writer.writeInt(u16, @intCast(self.outputs.len));
        writer.writeInt(i16, self.x);
        writer.writeInt(i16, self.y);
        writer.writeInt(u16, self.width);
        writer.writeInt(u16, self.height);
        writer.writeInt(u32, self.width_in_millimeters);
        writer.writeInt(u32, self.height_in_millimeters);
        for (self.outputs) |elem| {
            writer.writeInt(u32, @intFromEnum(elem));
        }
    }

    pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader) AllocDecodeError!@This() {
        const name = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
        const primary = (try reader.takeByte()) != 0;
        const automatic = (try reader.takeByte()) != 0;
        const nOutput = try reader.takeInt(u16, .native);
        const x = try reader.takeInt(i16, .native);
        const y = try reader.takeInt(i16, .native);
        const width = try reader.takeInt(u16, .native);
        const height = try reader.takeInt(u16, .native);
        const width_in_millimeters = try reader.takeInt(u32, .native);
        const height_in_millimeters = try reader.takeInt(u32, .native);
        const outputs = try allocator.alloc(Output, @as(usize, nOutput));
        errdefer allocator.free(outputs);
        var outputs_decoded: usize = 0;
        for (outputs) |*elem| {
            const elem_value = @as(Output, @enumFromInt(try reader.takeInt(u32, .native)));
            elem.* = elem_value;
            outputs_decoded += 1;
        }
        return .{
            .name = name,
            .primary = primary,
            .automatic = automatic,
            .x = x,
            .y = y,
            .width = width,
            .height = height,
            .width_in_millimeters = width_in_millimeters,
            .height_in_millimeters = height_in_millimeters,
            .outputs = outputs,
        };
    }

    pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
        allocator.free(self.outputs);
    }

};

pub const OutputChange = struct {
    timestamp: u32,
    config_timestamp: u32,
    window: xproto.Window,
    output: Output,
    crtc: Crtc,
    mode: Mode,
    rotation: u16,
    connection: Connection,
    subpixel_order: render.SubPixel,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, self.timestamp);
        writer.writeInt(u32, self.config_timestamp);
        writer.writeInt(u32, @intFromEnum(self.window));
        writer.writeInt(u32, @intFromEnum(self.output));
        writer.writeInt(u32, @intFromEnum(self.crtc));
        writer.writeInt(u32, @intFromEnum(self.mode));
        writer.writeInt(u16, self.rotation);
        writer.writeByte(@intCast(@intFromEnum(self.connection)));
        writer.writeByte(@intCast(@intFromEnum(self.subpixel_order)));
    }

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        const timestamp = try reader.takeInt(u32, .native);
        const config_timestamp = try reader.takeInt(u32, .native);
        const window = @as(xproto.Window, @enumFromInt(try reader.takeInt(u32, .native)));
        const output = @as(Output, @enumFromInt(try reader.takeInt(u32, .native)));
        const crtc = @as(Crtc, @enumFromInt(try reader.takeInt(u32, .native)));
        const mode = @as(Mode, @enumFromInt(try reader.takeInt(u32, .native)));
        const rotation = try reader.takeInt(u16, .native);
        const connection = @as(Connection, @enumFromInt(try reader.takeInt(u8, .native)));
        const subpixel_order = @as(render.SubPixel, @enumFromInt(try reader.takeInt(u8, .native)));
        return .{
            .timestamp = timestamp,
            .config_timestamp = config_timestamp,
            .window = window,
            .output = output,
            .crtc = crtc,
            .mode = mode,
            .rotation = rotation,
            .connection = connection,
            .subpixel_order = subpixel_order,
        };
    }

};

pub const OutputProperty = struct {
    window: xproto.Window,
    output: Output,
    atom: xproto.Atom,
    timestamp: u32,
    status: xproto.Property,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.window));
        writer.writeInt(u32, @intFromEnum(self.output));
        writer.writeInt(u32, @intFromEnum(self.atom));
        writer.writeInt(u32, self.timestamp);
        writer.writeByte(@intCast(@intFromEnum(self.status)));
        writer.splatByte(0, 11);
    }

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        const window = @as(xproto.Window, @enumFromInt(try reader.takeInt(u32, .native)));
        const output = @as(Output, @enumFromInt(try reader.takeInt(u32, .native)));
        const atom = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
        const timestamp = try reader.takeInt(u32, .native);
        const status = @as(xproto.Property, @enumFromInt(try reader.takeInt(u8, .native)));
        _ = try reader.take(11);
        return .{
            .window = window,
            .output = output,
            .atom = atom,
            .timestamp = timestamp,
            .status = status,
        };
    }

};

pub const ProviderChange = struct {
    timestamp: u32,
    window: xproto.Window,
    provider: Provider,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, self.timestamp);
        writer.writeInt(u32, @intFromEnum(self.window));
        writer.writeInt(u32, @intFromEnum(self.provider));
        writer.splatByte(0, 16);
    }

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        const timestamp = try reader.takeInt(u32, .native);
        const window = @as(xproto.Window, @enumFromInt(try reader.takeInt(u32, .native)));
        const provider = @as(Provider, @enumFromInt(try reader.takeInt(u32, .native)));
        _ = try reader.take(16);
        return .{
            .timestamp = timestamp,
            .window = window,
            .provider = provider,
        };
    }

};

pub const ProviderProperty = struct {
    window: xproto.Window,
    provider: Provider,
    atom: xproto.Atom,
    timestamp: u32,
    state: u8,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.window));
        writer.writeInt(u32, @intFromEnum(self.provider));
        writer.writeInt(u32, @intFromEnum(self.atom));
        writer.writeInt(u32, self.timestamp);
        writer.writeByte(self.state);
        writer.splatByte(0, 11);
    }

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        const window = @as(xproto.Window, @enumFromInt(try reader.takeInt(u32, .native)));
        const provider = @as(Provider, @enumFromInt(try reader.takeInt(u32, .native)));
        const atom = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
        const timestamp = try reader.takeInt(u32, .native);
        const state = try reader.takeByte();
        _ = try reader.take(11);
        return .{
            .window = window,
            .provider = provider,
            .atom = atom,
            .timestamp = timestamp,
            .state = state,
        };
    }

};

pub const RefreshRates = struct {
    rates: []const u16,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u16, @intCast(self.rates.len));
        for (self.rates) |elem| {
            writer.writeInt(u16, elem);
        }
    }

    pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader) AllocDecodeError!@This() {
        const nRates = try reader.takeInt(u16, .native);
        const rates = try allocator.alloc(u16, @as(usize, nRates));
        errdefer allocator.free(rates);
        var rates_decoded: usize = 0;
        for (rates) |*elem| {
            const elem_value = try reader.takeInt(u16, .native);
            elem.* = elem_value;
            rates_decoded += 1;
        }
        return .{
            .rates = rates,
        };
    }

    pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
        allocator.free(self.rates);
    }

};

pub const ResourceChange = struct {
    timestamp: u32,
    window: xproto.Window,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, self.timestamp);
        writer.writeInt(u32, @intFromEnum(self.window));
        writer.splatByte(0, 20);
    }

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        const timestamp = try reader.takeInt(u32, .native);
        const window = @as(xproto.Window, @enumFromInt(try reader.takeInt(u32, .native)));
        _ = try reader.take(20);
        return .{
            .timestamp = timestamp,
            .window = window,
        };
    }

};

pub const ScreenSize = struct {
    width: u16,
    height: u16,
    mwidth: u16,
    mheight: u16,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u16, self.width);
        writer.writeInt(u16, self.height);
        writer.writeInt(u16, self.mwidth);
        writer.writeInt(u16, self.mheight);
    }

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        const width = try reader.takeInt(u16, .native);
        const height = try reader.takeInt(u16, .native);
        const mwidth = try reader.takeInt(u16, .native);
        const mheight = try reader.takeInt(u16, .native);
        return .{
            .width = width,
            .height = height,
            .mwidth = mwidth,
            .mheight = mheight,
        };
    }

};

pub const AddOutputMode = struct {
    pub const opcode: u8 = 18;
    pub const extension: ?extensions.Extension = .RANDR;
    pub const Reply = void;

    output: Output,
    mode: Mode,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.output));
        writer.writeInt(u32, @intFromEnum(self.mode));
    }

};

pub const ChangeOutputProperty = struct {
    pub const opcode: u8 = 13;
    pub const extension: ?extensions.Extension = .RANDR;
    pub const Reply = void;

    output: Output,
    property: xproto.Atom,
    type: xproto.Atom,
    format: u8,
    mode: xproto.PropMode,
    num_units: u32,
    data: []const u8,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.output));
        writer.writeInt(u32, @intFromEnum(self.property));
        writer.writeInt(u32, @intFromEnum(self.type));
        writer.writeByte(self.format);
        writer.writeByte(@intCast(@intFromEnum(self.mode)));
        writer.splatByte(0, 2);
        writer.writeInt(u32, self.num_units);
        writer.write(self.data);
    }

};

pub const ChangeProviderProperty = struct {
    pub const opcode: u8 = 39;
    pub const extension: ?extensions.Extension = .RANDR;
    pub const Reply = void;

    provider: Provider,
    property: xproto.Atom,
    type: xproto.Atom,
    format: u8,
    mode: u8,
    num_items: u32,
    data: []const u8,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.provider));
        writer.writeInt(u32, @intFromEnum(self.property));
        writer.writeInt(u32, @intFromEnum(self.type));
        writer.writeByte(self.format);
        writer.writeByte(self.mode);
        writer.splatByte(0, 2);
        writer.writeInt(u32, self.num_items);
        writer.write(self.data);
    }

};

pub const ConfigureOutputProperty = struct {
    pub const opcode: u8 = 12;
    pub const extension: ?extensions.Extension = .RANDR;
    pub const Reply = void;

    output: Output,
    property: xproto.Atom,
    pending: bool,
    range: bool,
    values: []const i32,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.output));
        writer.writeInt(u32, @intFromEnum(self.property));
        writer.writeByte(@intFromBool(self.pending));
        writer.writeByte(@intFromBool(self.range));
        writer.splatByte(0, 2);
        for (self.values) |elem| {
            writer.writeInt(i32, elem);
        }
    }

};

pub const ConfigureProviderProperty = struct {
    pub const opcode: u8 = 38;
    pub const extension: ?extensions.Extension = .RANDR;
    pub const Reply = void;

    provider: Provider,
    property: xproto.Atom,
    pending: bool,
    range: bool,
    values: []const i32,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.provider));
        writer.writeInt(u32, @intFromEnum(self.property));
        writer.writeByte(@intFromBool(self.pending));
        writer.writeByte(@intFromBool(self.range));
        writer.splatByte(0, 2);
        for (self.values) |elem| {
            writer.writeInt(i32, elem);
        }
    }

};

pub const CreateModeReply = struct {
    mode: Mode,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.splatByte(0, 1);
        writer.writeInt(u32, @intFromEnum(self.mode));
        writer.splatByte(0, 20);
    }

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        _ = try reader.takeByte();
        _ = try reader.take(1);
        _ = try reader.takeInt(u16, .native);
        _ = try reader.takeInt(u32, .native);
        const mode = @as(Mode, @enumFromInt(try reader.takeInt(u32, .native)));
        _ = try reader.take(20);
        return .{
            .mode = mode,
        };
    }

};

pub const CreateMode = struct {
    pub const opcode: u8 = 16;
    pub const extension: ?extensions.Extension = .RANDR;
    pub const Reply = CreateModeReply;

    window: xproto.Window,
    mode_info: ModeInfo,
    name: []const u8,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.window));
        self.mode_info.encode(writer);
        writer.write(self.name);
    }

};

pub const DeleteMonitor = struct {
    pub const opcode: u8 = 44;
    pub const extension: ?extensions.Extension = .RANDR;
    pub const Reply = void;

    window: xproto.Window,
    name: xproto.Atom,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.window));
        writer.writeInt(u32, @intFromEnum(self.name));
    }

};

pub const DeleteOutputMode = struct {
    pub const opcode: u8 = 19;
    pub const extension: ?extensions.Extension = .RANDR;
    pub const Reply = void;

    output: Output,
    mode: Mode,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.output));
        writer.writeInt(u32, @intFromEnum(self.mode));
    }

};

pub const DeleteOutputProperty = struct {
    pub const opcode: u8 = 14;
    pub const extension: ?extensions.Extension = .RANDR;
    pub const Reply = void;

    output: Output,
    property: xproto.Atom,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.output));
        writer.writeInt(u32, @intFromEnum(self.property));
    }

};

pub const DeleteProviderProperty = struct {
    pub const opcode: u8 = 40;
    pub const extension: ?extensions.Extension = .RANDR;
    pub const Reply = void;

    provider: Provider,
    property: xproto.Atom,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.provider));
        writer.writeInt(u32, @intFromEnum(self.property));
    }

};

pub const DestroyMode = struct {
    pub const opcode: u8 = 17;
    pub const extension: ?extensions.Extension = .RANDR;
    pub const Reply = void;

    mode: Mode,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.mode));
    }

};

pub const FreeLease = struct {
    pub const opcode: u8 = 46;
    pub const extension: ?extensions.Extension = .RANDR;
    pub const Reply = void;

    lid: Lease,
    terminate: u8,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.lid));
        writer.writeByte(self.terminate);
    }

};

pub const GetCrtcGammaReply = struct {
    red: []const u16,
    green: []const u16,
    blue: []const u16,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.splatByte(0, 1);
        writer.writeInt(u16, @intCast(self.blue.len));
        writer.splatByte(0, 22);
        for (self.red) |elem| {
            writer.writeInt(u16, elem);
        }
        for (self.green) |elem| {
            writer.writeInt(u16, elem);
        }
        for (self.blue) |elem| {
            writer.writeInt(u16, elem);
        }
    }

    pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader) AllocDecodeError!@This() {
        _ = try reader.takeByte();
        _ = try reader.take(1);
        _ = try reader.takeInt(u16, .native);
        _ = try reader.takeInt(u32, .native);
        const size = try reader.takeInt(u16, .native);
        _ = try reader.take(22);
        const red = try allocator.alloc(u16, @as(usize, size));
        errdefer allocator.free(red);
        var red_decoded: usize = 0;
        for (red) |*elem| {
            const elem_value = try reader.takeInt(u16, .native);
            elem.* = elem_value;
            red_decoded += 1;
        }
        const green = try allocator.alloc(u16, @as(usize, size));
        errdefer allocator.free(green);
        var green_decoded: usize = 0;
        for (green) |*elem| {
            const elem_value = try reader.takeInt(u16, .native);
            elem.* = elem_value;
            green_decoded += 1;
        }
        const blue = try allocator.alloc(u16, @as(usize, size));
        errdefer allocator.free(blue);
        var blue_decoded: usize = 0;
        for (blue) |*elem| {
            const elem_value = try reader.takeInt(u16, .native);
            elem.* = elem_value;
            blue_decoded += 1;
        }
        return .{
            .red = red,
            .green = green,
            .blue = blue,
        };
    }

    pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
        allocator.free(self.red);
        allocator.free(self.green);
        allocator.free(self.blue);
    }

};

pub const GetCrtcGamma = struct {
    pub const opcode: u8 = 23;
    pub const extension: ?extensions.Extension = .RANDR;
    pub const Reply = GetCrtcGammaReply;

    crtc: Crtc,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.crtc));
    }

};

pub const GetCrtcGammaSizeReply = struct {
    size: u16,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.splatByte(0, 1);
        writer.writeInt(u16, self.size);
        writer.splatByte(0, 22);
    }

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        _ = try reader.takeByte();
        _ = try reader.take(1);
        _ = try reader.takeInt(u16, .native);
        _ = try reader.takeInt(u32, .native);
        const size = try reader.takeInt(u16, .native);
        _ = try reader.take(22);
        return .{
            .size = size,
        };
    }

};

pub const GetCrtcGammaSize = struct {
    pub const opcode: u8 = 22;
    pub const extension: ?extensions.Extension = .RANDR;
    pub const Reply = GetCrtcGammaSizeReply;

    crtc: Crtc,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.crtc));
    }

};

pub const GetCrtcInfoReply = struct {
    status: SetConfig,
    timestamp: u32,
    x: i16,
    y: i16,
    width: u16,
    height: u16,
    mode: Mode,
    rotation: u16,
    rotations: u16,
    outputs: []const Output,
    possible: []const Output,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeByte(@intCast(@intFromEnum(self.status)));
        writer.writeInt(u32, self.timestamp);
        writer.writeInt(i16, self.x);
        writer.writeInt(i16, self.y);
        writer.writeInt(u16, self.width);
        writer.writeInt(u16, self.height);
        writer.writeInt(u32, @intFromEnum(self.mode));
        writer.writeInt(u16, self.rotation);
        writer.writeInt(u16, self.rotations);
        writer.writeInt(u16, @intCast(self.outputs.len));
        writer.writeInt(u16, @intCast(self.possible.len));
        for (self.outputs) |elem| {
            writer.writeInt(u32, @intFromEnum(elem));
        }
        for (self.possible) |elem| {
            writer.writeInt(u32, @intFromEnum(elem));
        }
    }

    pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader) AllocDecodeError!@This() {
        _ = try reader.takeByte();
        const status = @as(SetConfig, @enumFromInt(try reader.takeInt(u8, .native)));
        _ = try reader.takeInt(u16, .native);
        _ = try reader.takeInt(u32, .native);
        const timestamp = try reader.takeInt(u32, .native);
        const x = try reader.takeInt(i16, .native);
        const y = try reader.takeInt(i16, .native);
        const width = try reader.takeInt(u16, .native);
        const height = try reader.takeInt(u16, .native);
        const mode = @as(Mode, @enumFromInt(try reader.takeInt(u32, .native)));
        const rotation = try reader.takeInt(u16, .native);
        const rotations = try reader.takeInt(u16, .native);
        const num_outputs = try reader.takeInt(u16, .native);
        const num_possible_outputs = try reader.takeInt(u16, .native);
        const outputs = try allocator.alloc(Output, @as(usize, num_outputs));
        errdefer allocator.free(outputs);
        var outputs_decoded: usize = 0;
        for (outputs) |*elem| {
            const elem_value = @as(Output, @enumFromInt(try reader.takeInt(u32, .native)));
            elem.* = elem_value;
            outputs_decoded += 1;
        }
        const possible = try allocator.alloc(Output, @as(usize, num_possible_outputs));
        errdefer allocator.free(possible);
        var possible_decoded: usize = 0;
        for (possible) |*elem| {
            const elem_value = @as(Output, @enumFromInt(try reader.takeInt(u32, .native)));
            elem.* = elem_value;
            possible_decoded += 1;
        }
        return .{
            .status = status,
            .timestamp = timestamp,
            .x = x,
            .y = y,
            .width = width,
            .height = height,
            .mode = mode,
            .rotation = rotation,
            .rotations = rotations,
            .outputs = outputs,
            .possible = possible,
        };
    }

    pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
        allocator.free(self.outputs);
        allocator.free(self.possible);
    }

};

pub const GetCrtcInfo = struct {
    pub const opcode: u8 = 20;
    pub const extension: ?extensions.Extension = .RANDR;
    pub const Reply = GetCrtcInfoReply;

    crtc: Crtc,
    config_timestamp: u32,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.crtc));
        writer.writeInt(u32, self.config_timestamp);
    }

};

pub const GetCrtcTransformReply = struct {
    pending_transform: render.TRANSFORM,
    has_transforms: bool,
    current_transform: render.TRANSFORM,
    pending_filter_name: []const u8,
    pending_params: []const i32,
    current_filter_name: []const u8,
    current_params: []const i32,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.splatByte(0, 1);
        self.pending_transform.encode(writer);
        writer.writeByte(@intFromBool(self.has_transforms));
        writer.splatByte(0, 3);
        self.current_transform.encode(writer);
        writer.splatByte(0, 4);
        writer.writeInt(u16, @intCast(self.pending_filter_name.len));
        writer.writeInt(u16, @intCast(self.pending_params.len));
        writer.writeInt(u16, @intCast(self.current_filter_name.len));
        writer.writeInt(u16, @intCast(self.current_params.len));
        writer.write(self.pending_filter_name);
        writer.splatByte(0, wire.pad4(self.pending_filter_name.len));
        for (self.pending_params) |elem| {
            writer.writeInt(i32, elem);
        }
        writer.write(self.current_filter_name);
        writer.splatByte(0, wire.pad4(self.current_filter_name.len));
        for (self.current_params) |elem| {
            writer.writeInt(i32, elem);
        }
    }

    pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader) AllocDecodeError!@This() {
        _ = try reader.takeByte();
        _ = try reader.take(1);
        _ = try reader.takeInt(u16, .native);
        _ = try reader.takeInt(u32, .native);
        const pending_transform = try render.TRANSFORM.decode(reader);
        const has_transforms = (try reader.takeByte()) != 0;
        _ = try reader.take(3);
        const current_transform = try render.TRANSFORM.decode(reader);
        _ = try reader.take(4);
        const pending_len = try reader.takeInt(u16, .native);
        const pending_nparams = try reader.takeInt(u16, .native);
        const current_len = try reader.takeInt(u16, .native);
        const current_nparams = try reader.takeInt(u16, .native);
        const pending_filter_name_byte_len = @as(usize, pending_len);
        const pending_filter_name_temp = try reader.take(pending_filter_name_byte_len);
        const pending_filter_name = try allocator.dupe(u8, pending_filter_name_temp);
        _ = try reader.take(wire.pad4(pending_filter_name.len));
        const pending_params = try allocator.alloc(i32, @as(usize, pending_nparams));
        errdefer allocator.free(pending_params);
        var pending_params_decoded: usize = 0;
        for (pending_params) |*elem| {
            const elem_value = try reader.takeInt(i32, .native);
            elem.* = elem_value;
            pending_params_decoded += 1;
        }
        const current_filter_name_byte_len = @as(usize, current_len);
        const current_filter_name_temp = try reader.take(current_filter_name_byte_len);
        const current_filter_name = try allocator.dupe(u8, current_filter_name_temp);
        _ = try reader.take(wire.pad4(current_filter_name.len));
        const current_params = try allocator.alloc(i32, @as(usize, current_nparams));
        errdefer allocator.free(current_params);
        var current_params_decoded: usize = 0;
        for (current_params) |*elem| {
            const elem_value = try reader.takeInt(i32, .native);
            elem.* = elem_value;
            current_params_decoded += 1;
        }
        return .{
            .pending_transform = pending_transform,
            .has_transforms = has_transforms,
            .current_transform = current_transform,
            .pending_filter_name = pending_filter_name,
            .pending_params = pending_params,
            .current_filter_name = current_filter_name,
            .current_params = current_params,
        };
    }

    pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
        allocator.free(self.pending_filter_name);
        allocator.free(self.pending_params);
        allocator.free(self.current_filter_name);
        allocator.free(self.current_params);
    }

};

pub const GetCrtcTransform = struct {
    pub const opcode: u8 = 27;
    pub const extension: ?extensions.Extension = .RANDR;
    pub const Reply = GetCrtcTransformReply;

    crtc: Crtc,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.crtc));
    }

};

pub const GetMonitorsReply = struct {
    timestamp: u32,
    nOutputs: u32,
    monitors: []MonitorInfo,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.splatByte(0, 1);
        writer.writeInt(u32, self.timestamp);
        writer.writeInt(u32, @intCast(self.monitors.len));
        writer.writeInt(u32, self.nOutputs);
        writer.splatByte(0, 12);
        for (self.monitors) |elem| {
            elem.encode(writer);
        }
    }

    pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader) AllocDecodeError!@This() {
        _ = try reader.takeByte();
        _ = try reader.take(1);
        _ = try reader.takeInt(u16, .native);
        _ = try reader.takeInt(u32, .native);
        const timestamp = try reader.takeInt(u32, .native);
        const nMonitors = try reader.takeInt(u32, .native);
        const nOutputs = try reader.takeInt(u32, .native);
        _ = try reader.take(12);
        const monitors = try allocator.alloc(MonitorInfo, @as(usize, nMonitors));
        errdefer allocator.free(monitors);
        var monitors_decoded: usize = 0;
        errdefer for (monitors[0..monitors_decoded]) |*elem| elem.deinit(allocator);
        for (monitors) |*elem| {
            const elem_value = try MonitorInfo.decode(allocator, reader);
            elem.* = elem_value;
            monitors_decoded += 1;
        }
        return .{
            .timestamp = timestamp,
            .nOutputs = nOutputs,
            .monitors = monitors,
        };
    }

    pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
        for (self.monitors) |*elem| elem.deinit(allocator);
        allocator.free(self.monitors);
    }

};

pub const GetMonitors = struct {
    pub const opcode: u8 = 42;
    pub const extension: ?extensions.Extension = .RANDR;
    pub const Reply = GetMonitorsReply;

    window: xproto.Window,
    get_active: bool,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.window));
        writer.writeByte(@intFromBool(self.get_active));
    }

};

pub const GetOutputInfoReply = struct {
    status: SetConfig,
    timestamp: u32,
    crtc: Crtc,
    mm_width: u32,
    mm_height: u32,
    connection: Connection,
    subpixel_order: render.SubPixel,
    num_preferred: u16,
    crtcs: []const Crtc,
    modes: []const Mode,
    clones: []const Output,
    name: []const u8,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeByte(@intCast(@intFromEnum(self.status)));
        writer.writeInt(u32, self.timestamp);
        writer.writeInt(u32, @intFromEnum(self.crtc));
        writer.writeInt(u32, self.mm_width);
        writer.writeInt(u32, self.mm_height);
        writer.writeByte(@intCast(@intFromEnum(self.connection)));
        writer.writeByte(@intCast(@intFromEnum(self.subpixel_order)));
        writer.writeInt(u16, @intCast(self.crtcs.len));
        writer.writeInt(u16, @intCast(self.modes.len));
        writer.writeInt(u16, self.num_preferred);
        writer.writeInt(u16, @intCast(self.clones.len));
        writer.writeInt(u16, @intCast(self.name.len));
        for (self.crtcs) |elem| {
            writer.writeInt(u32, @intFromEnum(elem));
        }
        for (self.modes) |elem| {
            writer.writeInt(u32, @intFromEnum(elem));
        }
        for (self.clones) |elem| {
            writer.writeInt(u32, @intFromEnum(elem));
        }
        writer.write(self.name);
    }

    pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader) AllocDecodeError!@This() {
        _ = try reader.takeByte();
        const status = @as(SetConfig, @enumFromInt(try reader.takeInt(u8, .native)));
        _ = try reader.takeInt(u16, .native);
        _ = try reader.takeInt(u32, .native);
        const timestamp = try reader.takeInt(u32, .native);
        const crtc = @as(Crtc, @enumFromInt(try reader.takeInt(u32, .native)));
        const mm_width = try reader.takeInt(u32, .native);
        const mm_height = try reader.takeInt(u32, .native);
        const connection = @as(Connection, @enumFromInt(try reader.takeInt(u8, .native)));
        const subpixel_order = @as(render.SubPixel, @enumFromInt(try reader.takeInt(u8, .native)));
        const num_crtcs = try reader.takeInt(u16, .native);
        const num_modes = try reader.takeInt(u16, .native);
        const num_preferred = try reader.takeInt(u16, .native);
        const num_clones = try reader.takeInt(u16, .native);
        const name_len = try reader.takeInt(u16, .native);
        const crtcs = try allocator.alloc(Crtc, @as(usize, num_crtcs));
        errdefer allocator.free(crtcs);
        var crtcs_decoded: usize = 0;
        for (crtcs) |*elem| {
            const elem_value = @as(Crtc, @enumFromInt(try reader.takeInt(u32, .native)));
            elem.* = elem_value;
            crtcs_decoded += 1;
        }
        const modes = try allocator.alloc(Mode, @as(usize, num_modes));
        errdefer allocator.free(modes);
        var modes_decoded: usize = 0;
        for (modes) |*elem| {
            const elem_value = @as(Mode, @enumFromInt(try reader.takeInt(u32, .native)));
            elem.* = elem_value;
            modes_decoded += 1;
        }
        const clones = try allocator.alloc(Output, @as(usize, num_clones));
        errdefer allocator.free(clones);
        var clones_decoded: usize = 0;
        for (clones) |*elem| {
            const elem_value = @as(Output, @enumFromInt(try reader.takeInt(u32, .native)));
            elem.* = elem_value;
            clones_decoded += 1;
        }
        const name_byte_len = @as(usize, name_len);
        const name_temp = try reader.take(name_byte_len);
        const name = try allocator.dupe(u8, name_temp);
        return .{
            .status = status,
            .timestamp = timestamp,
            .crtc = crtc,
            .mm_width = mm_width,
            .mm_height = mm_height,
            .connection = connection,
            .subpixel_order = subpixel_order,
            .num_preferred = num_preferred,
            .crtcs = crtcs,
            .modes = modes,
            .clones = clones,
            .name = name,
        };
    }

    pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
        allocator.free(self.crtcs);
        allocator.free(self.modes);
        allocator.free(self.clones);
        allocator.free(self.name);
    }

};

pub const GetOutputInfo = struct {
    pub const opcode: u8 = 9;
    pub const extension: ?extensions.Extension = .RANDR;
    pub const Reply = GetOutputInfoReply;

    output: Output,
    config_timestamp: u32,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.output));
        writer.writeInt(u32, self.config_timestamp);
    }

};

pub const GetOutputPrimaryReply = struct {
    output: Output,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.splatByte(0, 1);
        writer.writeInt(u32, @intFromEnum(self.output));
    }

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        _ = try reader.takeByte();
        _ = try reader.take(1);
        _ = try reader.takeInt(u16, .native);
        _ = try reader.takeInt(u32, .native);
        const output = @as(Output, @enumFromInt(try reader.takeInt(u32, .native)));
        return .{
            .output = output,
        };
    }

};

pub const GetOutputPrimary = struct {
    pub const opcode: u8 = 31;
    pub const extension: ?extensions.Extension = .RANDR;
    pub const Reply = GetOutputPrimaryReply;

    window: xproto.Window,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.window));
    }

};

pub const GetOutputPropertyReply = struct {
    format: u8,
    type: xproto.Atom,
    bytes_after: u32,
    num_items: u32,
    data: []const u8,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeByte(self.format);
        writer.writeInt(u32, @intFromEnum(self.type));
        writer.writeInt(u32, self.bytes_after);
        writer.writeInt(u32, self.num_items);
        writer.splatByte(0, 12);
        writer.write(self.data);
    }

    pub fn decode(scratch: []u8, reader: *std.Io.Reader) BufferDecodeError!@This() {
        _ = try reader.takeByte();
        const format = try reader.takeByte();
        _ = try reader.takeInt(u16, .native);
        _ = try reader.takeInt(u32, .native);
        var scratch_used: usize = 0;
        const @"type" = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
        const bytes_after = try reader.takeInt(u32, .native);
        const num_items = try reader.takeInt(u32, .native);
        _ = try reader.take(12);
        const data_byte_len = @as(usize, (num_items * (format / 8)));
        const data_temp = try reader.take(data_byte_len);
        if (scratch_used + data_byte_len > scratch.len) return error.BufferTooSmall;
        @memcpy(scratch[scratch_used..][0..data_byte_len], data_temp);
        const data = scratch[scratch_used..][0..data_byte_len];
        scratch_used += data_byte_len;
        return .{
            .format = format,
            .type = @"type",
            .bytes_after = bytes_after,
            .num_items = num_items,
            .data = data,
        };
    }

};

pub const GetOutputProperty = struct {
    pub const opcode: u8 = 15;
    pub const extension: ?extensions.Extension = .RANDR;
    pub const Reply = GetOutputPropertyReply;

    output: Output,
    property: xproto.Atom,
    type: xproto.Atom,
    long_offset: u32,
    long_length: u32,
    delete: bool,
    pending: bool,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.output));
        writer.writeInt(u32, @intFromEnum(self.property));
        writer.writeInt(u32, @intFromEnum(self.type));
        writer.writeInt(u32, self.long_offset);
        writer.writeInt(u32, self.long_length);
        writer.writeByte(@intFromBool(self.delete));
        writer.writeByte(@intFromBool(self.pending));
        writer.splatByte(0, 2);
    }

};

pub const GetPanningReply = struct {
    status: SetConfig,
    timestamp: u32,
    left: u16,
    top: u16,
    width: u16,
    height: u16,
    track_left: u16,
    track_top: u16,
    track_width: u16,
    track_height: u16,
    border_left: i16,
    border_top: i16,
    border_right: i16,
    border_bottom: i16,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeByte(@intCast(@intFromEnum(self.status)));
        writer.writeInt(u32, self.timestamp);
        writer.writeInt(u16, self.left);
        writer.writeInt(u16, self.top);
        writer.writeInt(u16, self.width);
        writer.writeInt(u16, self.height);
        writer.writeInt(u16, self.track_left);
        writer.writeInt(u16, self.track_top);
        writer.writeInt(u16, self.track_width);
        writer.writeInt(u16, self.track_height);
        writer.writeInt(i16, self.border_left);
        writer.writeInt(i16, self.border_top);
        writer.writeInt(i16, self.border_right);
        writer.writeInt(i16, self.border_bottom);
    }

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        _ = try reader.takeByte();
        const status = @as(SetConfig, @enumFromInt(try reader.takeInt(u8, .native)));
        _ = try reader.takeInt(u16, .native);
        _ = try reader.takeInt(u32, .native);
        const timestamp = try reader.takeInt(u32, .native);
        const left = try reader.takeInt(u16, .native);
        const top = try reader.takeInt(u16, .native);
        const width = try reader.takeInt(u16, .native);
        const height = try reader.takeInt(u16, .native);
        const track_left = try reader.takeInt(u16, .native);
        const track_top = try reader.takeInt(u16, .native);
        const track_width = try reader.takeInt(u16, .native);
        const track_height = try reader.takeInt(u16, .native);
        const border_left = try reader.takeInt(i16, .native);
        const border_top = try reader.takeInt(i16, .native);
        const border_right = try reader.takeInt(i16, .native);
        const border_bottom = try reader.takeInt(i16, .native);
        return .{
            .status = status,
            .timestamp = timestamp,
            .left = left,
            .top = top,
            .width = width,
            .height = height,
            .track_left = track_left,
            .track_top = track_top,
            .track_width = track_width,
            .track_height = track_height,
            .border_left = border_left,
            .border_top = border_top,
            .border_right = border_right,
            .border_bottom = border_bottom,
        };
    }

};

pub const GetPanning = struct {
    pub const opcode: u8 = 28;
    pub const extension: ?extensions.Extension = .RANDR;
    pub const Reply = GetPanningReply;

    crtc: Crtc,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.crtc));
    }

};

pub const GetProviderInfoReply = struct {
    status: u8,
    timestamp: u32,
    capabilities: u32,
    crtcs: []const Crtc,
    outputs: []const Output,
    associated_providers: []const Provider,
    associated_capability: []const u32,
    name: []const u8,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeByte(self.status);
        writer.writeInt(u32, self.timestamp);
        writer.writeInt(u32, self.capabilities);
        writer.writeInt(u16, @intCast(self.crtcs.len));
        writer.writeInt(u16, @intCast(self.outputs.len));
        writer.writeInt(u16, @intCast(self.associated_capability.len));
        writer.writeInt(u16, @intCast(self.name.len));
        writer.splatByte(0, 8);
        for (self.crtcs) |elem| {
            writer.writeInt(u32, @intFromEnum(elem));
        }
        for (self.outputs) |elem| {
            writer.writeInt(u32, @intFromEnum(elem));
        }
        for (self.associated_providers) |elem| {
            writer.writeInt(u32, @intFromEnum(elem));
        }
        for (self.associated_capability) |elem| {
            writer.writeInt(u32, elem);
        }
        writer.write(self.name);
    }

    pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader) AllocDecodeError!@This() {
        _ = try reader.takeByte();
        const status = try reader.takeByte();
        _ = try reader.takeInt(u16, .native);
        _ = try reader.takeInt(u32, .native);
        const timestamp = try reader.takeInt(u32, .native);
        const capabilities = try reader.takeInt(u32, .native);
        const num_crtcs = try reader.takeInt(u16, .native);
        const num_outputs = try reader.takeInt(u16, .native);
        const num_associated_providers = try reader.takeInt(u16, .native);
        const name_len = try reader.takeInt(u16, .native);
        _ = try reader.take(8);
        const crtcs = try allocator.alloc(Crtc, @as(usize, num_crtcs));
        errdefer allocator.free(crtcs);
        var crtcs_decoded: usize = 0;
        for (crtcs) |*elem| {
            const elem_value = @as(Crtc, @enumFromInt(try reader.takeInt(u32, .native)));
            elem.* = elem_value;
            crtcs_decoded += 1;
        }
        const outputs = try allocator.alloc(Output, @as(usize, num_outputs));
        errdefer allocator.free(outputs);
        var outputs_decoded: usize = 0;
        for (outputs) |*elem| {
            const elem_value = @as(Output, @enumFromInt(try reader.takeInt(u32, .native)));
            elem.* = elem_value;
            outputs_decoded += 1;
        }
        const associated_providers = try allocator.alloc(Provider, @as(usize, num_associated_providers));
        errdefer allocator.free(associated_providers);
        var associated_providers_decoded: usize = 0;
        for (associated_providers) |*elem| {
            const elem_value = @as(Provider, @enumFromInt(try reader.takeInt(u32, .native)));
            elem.* = elem_value;
            associated_providers_decoded += 1;
        }
        const associated_capability = try allocator.alloc(u32, @as(usize, num_associated_providers));
        errdefer allocator.free(associated_capability);
        var associated_capability_decoded: usize = 0;
        for (associated_capability) |*elem| {
            const elem_value = try reader.takeInt(u32, .native);
            elem.* = elem_value;
            associated_capability_decoded += 1;
        }
        const name_byte_len = @as(usize, name_len);
        const name_temp = try reader.take(name_byte_len);
        const name = try allocator.dupe(u8, name_temp);
        return .{
            .status = status,
            .timestamp = timestamp,
            .capabilities = capabilities,
            .crtcs = crtcs,
            .outputs = outputs,
            .associated_providers = associated_providers,
            .associated_capability = associated_capability,
            .name = name,
        };
    }

    pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
        allocator.free(self.crtcs);
        allocator.free(self.outputs);
        allocator.free(self.associated_providers);
        allocator.free(self.associated_capability);
        allocator.free(self.name);
    }

};

pub const GetProviderInfo = struct {
    pub const opcode: u8 = 33;
    pub const extension: ?extensions.Extension = .RANDR;
    pub const Reply = GetProviderInfoReply;

    provider: Provider,
    config_timestamp: u32,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.provider));
        writer.writeInt(u32, self.config_timestamp);
    }

};

pub const GetProviderPropertyReply = struct {
    format: u8,
    type: xproto.Atom,
    bytes_after: u32,
    num_items: u32,
    data: []const u8,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeByte(self.format);
        writer.writeInt(u32, @intFromEnum(self.type));
        writer.writeInt(u32, self.bytes_after);
        writer.writeInt(u32, self.num_items);
        writer.splatByte(0, 12);
        writer.write(self.data);
    }

    pub fn decode(scratch: []u8, reader: *std.Io.Reader) BufferDecodeError!@This() {
        _ = try reader.takeByte();
        const format = try reader.takeByte();
        _ = try reader.takeInt(u16, .native);
        _ = try reader.takeInt(u32, .native);
        var scratch_used: usize = 0;
        const @"type" = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
        const bytes_after = try reader.takeInt(u32, .native);
        const num_items = try reader.takeInt(u32, .native);
        _ = try reader.take(12);
        const data_byte_len = @as(usize, (num_items * (format / 8)));
        const data_temp = try reader.take(data_byte_len);
        if (scratch_used + data_byte_len > scratch.len) return error.BufferTooSmall;
        @memcpy(scratch[scratch_used..][0..data_byte_len], data_temp);
        const data = scratch[scratch_used..][0..data_byte_len];
        scratch_used += data_byte_len;
        return .{
            .format = format,
            .type = @"type",
            .bytes_after = bytes_after,
            .num_items = num_items,
            .data = data,
        };
    }

};

pub const GetProviderProperty = struct {
    pub const opcode: u8 = 41;
    pub const extension: ?extensions.Extension = .RANDR;
    pub const Reply = GetProviderPropertyReply;

    provider: Provider,
    property: xproto.Atom,
    type: xproto.Atom,
    long_offset: u32,
    long_length: u32,
    delete: bool,
    pending: bool,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.provider));
        writer.writeInt(u32, @intFromEnum(self.property));
        writer.writeInt(u32, @intFromEnum(self.type));
        writer.writeInt(u32, self.long_offset);
        writer.writeInt(u32, self.long_length);
        writer.writeByte(@intFromBool(self.delete));
        writer.writeByte(@intFromBool(self.pending));
        writer.splatByte(0, 2);
    }

};

pub const GetProvidersReply = struct {
    timestamp: u32,
    providers: []const Provider,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.splatByte(0, 1);
        writer.writeInt(u32, self.timestamp);
        writer.writeInt(u16, @intCast(self.providers.len));
        writer.splatByte(0, 18);
        for (self.providers) |elem| {
            writer.writeInt(u32, @intFromEnum(elem));
        }
    }

    pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader) AllocDecodeError!@This() {
        _ = try reader.takeByte();
        _ = try reader.take(1);
        _ = try reader.takeInt(u16, .native);
        _ = try reader.takeInt(u32, .native);
        const timestamp = try reader.takeInt(u32, .native);
        const num_providers = try reader.takeInt(u16, .native);
        _ = try reader.take(18);
        const providers = try allocator.alloc(Provider, @as(usize, num_providers));
        errdefer allocator.free(providers);
        var providers_decoded: usize = 0;
        for (providers) |*elem| {
            const elem_value = @as(Provider, @enumFromInt(try reader.takeInt(u32, .native)));
            elem.* = elem_value;
            providers_decoded += 1;
        }
        return .{
            .timestamp = timestamp,
            .providers = providers,
        };
    }

    pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
        allocator.free(self.providers);
    }

};

pub const GetProviders = struct {
    pub const opcode: u8 = 32;
    pub const extension: ?extensions.Extension = .RANDR;
    pub const Reply = GetProvidersReply;

    window: xproto.Window,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.window));
    }

};

pub const GetScreenInfoReply = struct {
    rotations: u8,
    root: xproto.Window,
    timestamp: u32,
    config_timestamp: u32,
    sizeID: u16,
    rotation: u16,
    rate: u16,
    nInfo: u16,
    sizes: []ScreenSize,
    rates: []RefreshRates,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeByte(self.rotations);
        writer.writeInt(u32, @intFromEnum(self.root));
        writer.writeInt(u32, self.timestamp);
        writer.writeInt(u32, self.config_timestamp);
        writer.writeInt(u16, @intCast(self.sizes.len));
        writer.writeInt(u16, self.sizeID);
        writer.writeInt(u16, self.rotation);
        writer.writeInt(u16, self.rate);
        writer.writeInt(u16, self.nInfo);
        writer.splatByte(0, 2);
        for (self.sizes) |elem| {
            elem.encode(writer);
        }
        for (self.rates) |elem| {
            elem.encode(writer);
        }
    }

    pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader) AllocDecodeError!@This() {
        _ = try reader.takeByte();
        const rotations = try reader.takeByte();
        _ = try reader.takeInt(u16, .native);
        _ = try reader.takeInt(u32, .native);
        const root = @as(xproto.Window, @enumFromInt(try reader.takeInt(u32, .native)));
        const timestamp = try reader.takeInt(u32, .native);
        const config_timestamp = try reader.takeInt(u32, .native);
        const nSizes = try reader.takeInt(u16, .native);
        const sizeID = try reader.takeInt(u16, .native);
        const rotation = try reader.takeInt(u16, .native);
        const rate = try reader.takeInt(u16, .native);
        const nInfo = try reader.takeInt(u16, .native);
        _ = try reader.take(2);
        const sizes = try allocator.alloc(ScreenSize, @as(usize, nSizes));
        errdefer allocator.free(sizes);
        var sizes_decoded: usize = 0;
        for (sizes) |*elem| {
            const elem_value = try ScreenSize.decode(reader);
            elem.* = elem_value;
            sizes_decoded += 1;
        }
        const rates = try allocator.alloc(RefreshRates, @as(usize, (nInfo - nSizes)));
        errdefer allocator.free(rates);
        var rates_decoded: usize = 0;
        errdefer for (rates[0..rates_decoded]) |*elem| elem.deinit(allocator);
        for (rates) |*elem| {
            const elem_value = try RefreshRates.decode(allocator, reader);
            elem.* = elem_value;
            rates_decoded += 1;
        }
        return .{
            .rotations = rotations,
            .root = root,
            .timestamp = timestamp,
            .config_timestamp = config_timestamp,
            .sizeID = sizeID,
            .rotation = rotation,
            .rate = rate,
            .nInfo = nInfo,
            .sizes = sizes,
            .rates = rates,
        };
    }

    pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
        allocator.free(self.sizes);
        for (self.rates) |*elem| elem.deinit(allocator);
        allocator.free(self.rates);
    }

};

pub const GetScreenInfo = struct {
    pub const opcode: u8 = 5;
    pub const extension: ?extensions.Extension = .RANDR;
    pub const Reply = GetScreenInfoReply;

    window: xproto.Window,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.window));
    }

};

pub const GetScreenResourcesReply = struct {
    timestamp: u32,
    config_timestamp: u32,
    crtcs: []const Crtc,
    outputs: []const Output,
    modes: []ModeInfo,
    names: []const u8,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.splatByte(0, 1);
        writer.writeInt(u32, self.timestamp);
        writer.writeInt(u32, self.config_timestamp);
        writer.writeInt(u16, @intCast(self.crtcs.len));
        writer.writeInt(u16, @intCast(self.outputs.len));
        writer.writeInt(u16, @intCast(self.modes.len));
        writer.writeInt(u16, @intCast(self.names.len));
        writer.splatByte(0, 8);
        for (self.crtcs) |elem| {
            writer.writeInt(u32, @intFromEnum(elem));
        }
        for (self.outputs) |elem| {
            writer.writeInt(u32, @intFromEnum(elem));
        }
        for (self.modes) |elem| {
            elem.encode(writer);
        }
        writer.write(self.names);
    }

    pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader) AllocDecodeError!@This() {
        _ = try reader.takeByte();
        _ = try reader.take(1);
        _ = try reader.takeInt(u16, .native);
        _ = try reader.takeInt(u32, .native);
        const timestamp = try reader.takeInt(u32, .native);
        const config_timestamp = try reader.takeInt(u32, .native);
        const num_crtcs = try reader.takeInt(u16, .native);
        const num_outputs = try reader.takeInt(u16, .native);
        const num_modes = try reader.takeInt(u16, .native);
        const names_len = try reader.takeInt(u16, .native);
        _ = try reader.take(8);
        const crtcs = try allocator.alloc(Crtc, @as(usize, num_crtcs));
        errdefer allocator.free(crtcs);
        var crtcs_decoded: usize = 0;
        for (crtcs) |*elem| {
            const elem_value = @as(Crtc, @enumFromInt(try reader.takeInt(u32, .native)));
            elem.* = elem_value;
            crtcs_decoded += 1;
        }
        const outputs = try allocator.alloc(Output, @as(usize, num_outputs));
        errdefer allocator.free(outputs);
        var outputs_decoded: usize = 0;
        for (outputs) |*elem| {
            const elem_value = @as(Output, @enumFromInt(try reader.takeInt(u32, .native)));
            elem.* = elem_value;
            outputs_decoded += 1;
        }
        const modes = try allocator.alloc(ModeInfo, @as(usize, num_modes));
        errdefer allocator.free(modes);
        var modes_decoded: usize = 0;
        for (modes) |*elem| {
            const elem_value = try ModeInfo.decode(reader);
            elem.* = elem_value;
            modes_decoded += 1;
        }
        const names_byte_len = @as(usize, names_len);
        const names_temp = try reader.take(names_byte_len);
        const names = try allocator.dupe(u8, names_temp);
        return .{
            .timestamp = timestamp,
            .config_timestamp = config_timestamp,
            .crtcs = crtcs,
            .outputs = outputs,
            .modes = modes,
            .names = names,
        };
    }

    pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
        allocator.free(self.crtcs);
        allocator.free(self.outputs);
        allocator.free(self.modes);
        allocator.free(self.names);
    }

};

pub const GetScreenResources = struct {
    pub const opcode: u8 = 8;
    pub const extension: ?extensions.Extension = .RANDR;
    pub const Reply = GetScreenResourcesReply;

    window: xproto.Window,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.window));
    }

};

pub const GetScreenResourcesCurrentReply = struct {
    timestamp: u32,
    config_timestamp: u32,
    crtcs: []const Crtc,
    outputs: []const Output,
    modes: []ModeInfo,
    names: []const u8,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.splatByte(0, 1);
        writer.writeInt(u32, self.timestamp);
        writer.writeInt(u32, self.config_timestamp);
        writer.writeInt(u16, @intCast(self.crtcs.len));
        writer.writeInt(u16, @intCast(self.outputs.len));
        writer.writeInt(u16, @intCast(self.modes.len));
        writer.writeInt(u16, @intCast(self.names.len));
        writer.splatByte(0, 8);
        for (self.crtcs) |elem| {
            writer.writeInt(u32, @intFromEnum(elem));
        }
        for (self.outputs) |elem| {
            writer.writeInt(u32, @intFromEnum(elem));
        }
        for (self.modes) |elem| {
            elem.encode(writer);
        }
        writer.write(self.names);
    }

    pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader) AllocDecodeError!@This() {
        _ = try reader.takeByte();
        _ = try reader.take(1);
        _ = try reader.takeInt(u16, .native);
        _ = try reader.takeInt(u32, .native);
        const timestamp = try reader.takeInt(u32, .native);
        const config_timestamp = try reader.takeInt(u32, .native);
        const num_crtcs = try reader.takeInt(u16, .native);
        const num_outputs = try reader.takeInt(u16, .native);
        const num_modes = try reader.takeInt(u16, .native);
        const names_len = try reader.takeInt(u16, .native);
        _ = try reader.take(8);
        const crtcs = try allocator.alloc(Crtc, @as(usize, num_crtcs));
        errdefer allocator.free(crtcs);
        var crtcs_decoded: usize = 0;
        for (crtcs) |*elem| {
            const elem_value = @as(Crtc, @enumFromInt(try reader.takeInt(u32, .native)));
            elem.* = elem_value;
            crtcs_decoded += 1;
        }
        const outputs = try allocator.alloc(Output, @as(usize, num_outputs));
        errdefer allocator.free(outputs);
        var outputs_decoded: usize = 0;
        for (outputs) |*elem| {
            const elem_value = @as(Output, @enumFromInt(try reader.takeInt(u32, .native)));
            elem.* = elem_value;
            outputs_decoded += 1;
        }
        const modes = try allocator.alloc(ModeInfo, @as(usize, num_modes));
        errdefer allocator.free(modes);
        var modes_decoded: usize = 0;
        for (modes) |*elem| {
            const elem_value = try ModeInfo.decode(reader);
            elem.* = elem_value;
            modes_decoded += 1;
        }
        const names_byte_len = @as(usize, names_len);
        const names_temp = try reader.take(names_byte_len);
        const names = try allocator.dupe(u8, names_temp);
        return .{
            .timestamp = timestamp,
            .config_timestamp = config_timestamp,
            .crtcs = crtcs,
            .outputs = outputs,
            .modes = modes,
            .names = names,
        };
    }

    pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
        allocator.free(self.crtcs);
        allocator.free(self.outputs);
        allocator.free(self.modes);
        allocator.free(self.names);
    }

};

pub const GetScreenResourcesCurrent = struct {
    pub const opcode: u8 = 25;
    pub const extension: ?extensions.Extension = .RANDR;
    pub const Reply = GetScreenResourcesCurrentReply;

    window: xproto.Window,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.window));
    }

};

pub const GetScreenSizeRangeReply = struct {
    min_width: u16,
    min_height: u16,
    max_width: u16,
    max_height: u16,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.splatByte(0, 1);
        writer.writeInt(u16, self.min_width);
        writer.writeInt(u16, self.min_height);
        writer.writeInt(u16, self.max_width);
        writer.writeInt(u16, self.max_height);
        writer.splatByte(0, 16);
    }

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        _ = try reader.takeByte();
        _ = try reader.take(1);
        _ = try reader.takeInt(u16, .native);
        _ = try reader.takeInt(u32, .native);
        const min_width = try reader.takeInt(u16, .native);
        const min_height = try reader.takeInt(u16, .native);
        const max_width = try reader.takeInt(u16, .native);
        const max_height = try reader.takeInt(u16, .native);
        _ = try reader.take(16);
        return .{
            .min_width = min_width,
            .min_height = min_height,
            .max_width = max_width,
            .max_height = max_height,
        };
    }

};

pub const GetScreenSizeRange = struct {
    pub const opcode: u8 = 6;
    pub const extension: ?extensions.Extension = .RANDR;
    pub const Reply = GetScreenSizeRangeReply;

    window: xproto.Window,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.window));
    }

};

pub const ListOutputPropertiesReply = struct {
    atoms: []const xproto.Atom,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.splatByte(0, 1);
        writer.writeInt(u16, @intCast(self.atoms.len));
        writer.splatByte(0, 22);
        for (self.atoms) |elem| {
            writer.writeInt(u32, @intFromEnum(elem));
        }
    }

    pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader) AllocDecodeError!@This() {
        _ = try reader.takeByte();
        _ = try reader.take(1);
        _ = try reader.takeInt(u16, .native);
        _ = try reader.takeInt(u32, .native);
        const num_atoms = try reader.takeInt(u16, .native);
        _ = try reader.take(22);
        const atoms = try allocator.alloc(xproto.Atom, @as(usize, num_atoms));
        errdefer allocator.free(atoms);
        var atoms_decoded: usize = 0;
        for (atoms) |*elem| {
            const elem_value = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
            elem.* = elem_value;
            atoms_decoded += 1;
        }
        return .{
            .atoms = atoms,
        };
    }

    pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
        allocator.free(self.atoms);
    }

};

pub const ListOutputProperties = struct {
    pub const opcode: u8 = 10;
    pub const extension: ?extensions.Extension = .RANDR;
    pub const Reply = ListOutputPropertiesReply;

    output: Output,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.output));
    }

};

pub const ListProviderPropertiesReply = struct {
    atoms: []const xproto.Atom,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.splatByte(0, 1);
        writer.writeInt(u16, @intCast(self.atoms.len));
        writer.splatByte(0, 22);
        for (self.atoms) |elem| {
            writer.writeInt(u32, @intFromEnum(elem));
        }
    }

    pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader) AllocDecodeError!@This() {
        _ = try reader.takeByte();
        _ = try reader.take(1);
        _ = try reader.takeInt(u16, .native);
        _ = try reader.takeInt(u32, .native);
        const num_atoms = try reader.takeInt(u16, .native);
        _ = try reader.take(22);
        const atoms = try allocator.alloc(xproto.Atom, @as(usize, num_atoms));
        errdefer allocator.free(atoms);
        var atoms_decoded: usize = 0;
        for (atoms) |*elem| {
            const elem_value = @as(xproto.Atom, @enumFromInt(try reader.takeInt(u32, .native)));
            elem.* = elem_value;
            atoms_decoded += 1;
        }
        return .{
            .atoms = atoms,
        };
    }

    pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
        allocator.free(self.atoms);
    }

};

pub const ListProviderProperties = struct {
    pub const opcode: u8 = 36;
    pub const extension: ?extensions.Extension = .RANDR;
    pub const Reply = ListProviderPropertiesReply;

    provider: Provider,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.provider));
    }

};

pub const QueryOutputPropertyReply = struct {
    pending: bool,
    range: bool,
    immutable: bool,
    validValues: []const i32,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.splatByte(0, 1);
        writer.writeByte(@intFromBool(self.pending));
        writer.writeByte(@intFromBool(self.range));
        writer.writeByte(@intFromBool(self.immutable));
        writer.splatByte(0, 21);
        for (self.validValues) |elem| {
            writer.writeInt(i32, elem);
        }
    }

    pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader) AllocDecodeError!@This() {
        _ = try reader.takeByte();
        _ = try reader.take(1);
        _ = try reader.takeInt(u16, .native);
        const length = try reader.takeInt(u32, .native);
        const pending = (try reader.takeByte()) != 0;
        const range = (try reader.takeByte()) != 0;
        const immutable = (try reader.takeByte()) != 0;
        _ = try reader.take(21);
        const validValues = try allocator.alloc(i32, @as(usize, length));
        errdefer allocator.free(validValues);
        var validValues_decoded: usize = 0;
        for (validValues) |*elem| {
            const elem_value = try reader.takeInt(i32, .native);
            elem.* = elem_value;
            validValues_decoded += 1;
        }
        return .{
            .pending = pending,
            .range = range,
            .immutable = immutable,
            .validValues = validValues,
        };
    }

    pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
        allocator.free(self.validValues);
    }

};

pub const QueryOutputProperty = struct {
    pub const opcode: u8 = 11;
    pub const extension: ?extensions.Extension = .RANDR;
    pub const Reply = QueryOutputPropertyReply;

    output: Output,
    property: xproto.Atom,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.output));
        writer.writeInt(u32, @intFromEnum(self.property));
    }

};

pub const QueryProviderPropertyReply = struct {
    pending: bool,
    range: bool,
    immutable: bool,
    valid_values: []const i32,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.splatByte(0, 1);
        writer.writeByte(@intFromBool(self.pending));
        writer.writeByte(@intFromBool(self.range));
        writer.writeByte(@intFromBool(self.immutable));
        writer.splatByte(0, 21);
        for (self.valid_values) |elem| {
            writer.writeInt(i32, elem);
        }
    }

    pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader) AllocDecodeError!@This() {
        _ = try reader.takeByte();
        _ = try reader.take(1);
        _ = try reader.takeInt(u16, .native);
        const length = try reader.takeInt(u32, .native);
        const pending = (try reader.takeByte()) != 0;
        const range = (try reader.takeByte()) != 0;
        const immutable = (try reader.takeByte()) != 0;
        _ = try reader.take(21);
        const valid_values = try allocator.alloc(i32, @as(usize, length));
        errdefer allocator.free(valid_values);
        var valid_values_decoded: usize = 0;
        for (valid_values) |*elem| {
            const elem_value = try reader.takeInt(i32, .native);
            elem.* = elem_value;
            valid_values_decoded += 1;
        }
        return .{
            .pending = pending,
            .range = range,
            .immutable = immutable,
            .valid_values = valid_values,
        };
    }

    pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
        allocator.free(self.valid_values);
    }

};

pub const QueryProviderProperty = struct {
    pub const opcode: u8 = 37;
    pub const extension: ?extensions.Extension = .RANDR;
    pub const Reply = QueryProviderPropertyReply;

    provider: Provider,
    property: xproto.Atom,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.provider));
        writer.writeInt(u32, @intFromEnum(self.property));
    }

};

pub const QueryVersionReply = struct {
    major_version: u32,
    minor_version: u32,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.splatByte(0, 1);
        writer.writeInt(u32, self.major_version);
        writer.writeInt(u32, self.minor_version);
        writer.splatByte(0, 16);
    }

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        _ = try reader.takeByte();
        _ = try reader.take(1);
        _ = try reader.takeInt(u16, .native);
        _ = try reader.takeInt(u32, .native);
        const major_version = try reader.takeInt(u32, .native);
        const minor_version = try reader.takeInt(u32, .native);
        _ = try reader.take(16);
        return .{
            .major_version = major_version,
            .minor_version = minor_version,
        };
    }

};

pub const QueryVersion = struct {
    pub const opcode: u8 = 0;
    pub const extension: ?extensions.Extension = .RANDR;
    pub const Reply = QueryVersionReply;

    major_version: u32,
    minor_version: u32,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, self.major_version);
        writer.writeInt(u32, self.minor_version);
    }

};

pub const SelectInput = struct {
    pub const opcode: u8 = 4;
    pub const extension: ?extensions.Extension = .RANDR;
    pub const Reply = void;

    window: xproto.Window,
    enable: u16,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.window));
        writer.writeInt(u16, self.enable);
        writer.splatByte(0, 2);
    }

};

pub const SetCrtcConfigReply = struct {
    status: SetConfig,
    timestamp: u32,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeByte(@intCast(@intFromEnum(self.status)));
        writer.writeInt(u32, self.timestamp);
        writer.splatByte(0, 20);
    }

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        _ = try reader.takeByte();
        const status = @as(SetConfig, @enumFromInt(try reader.takeInt(u8, .native)));
        _ = try reader.takeInt(u16, .native);
        _ = try reader.takeInt(u32, .native);
        const timestamp = try reader.takeInt(u32, .native);
        _ = try reader.take(20);
        return .{
            .status = status,
            .timestamp = timestamp,
        };
    }

};

pub const SetCrtcConfig = struct {
    pub const opcode: u8 = 21;
    pub const extension: ?extensions.Extension = .RANDR;
    pub const Reply = SetCrtcConfigReply;

    crtc: Crtc,
    timestamp: u32,
    config_timestamp: u32,
    x: i16,
    y: i16,
    mode: Mode,
    rotation: u16,
    outputs: []const Output,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.crtc));
        writer.writeInt(u32, self.timestamp);
        writer.writeInt(u32, self.config_timestamp);
        writer.writeInt(i16, self.x);
        writer.writeInt(i16, self.y);
        writer.writeInt(u32, @intFromEnum(self.mode));
        writer.writeInt(u16, self.rotation);
        writer.splatByte(0, 2);
        for (self.outputs) |elem| {
            writer.writeInt(u32, @intFromEnum(elem));
        }
    }

};

pub const SetCrtcGamma = struct {
    pub const opcode: u8 = 24;
    pub const extension: ?extensions.Extension = .RANDR;
    pub const Reply = void;

    crtc: Crtc,
    red: []const u16,
    green: []const u16,
    blue: []const u16,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.crtc));
        writer.writeInt(u16, @intCast(self.blue.len));
        writer.splatByte(0, 2);
        for (self.red) |elem| {
            writer.writeInt(u16, elem);
        }
        for (self.green) |elem| {
            writer.writeInt(u16, elem);
        }
        for (self.blue) |elem| {
            writer.writeInt(u16, elem);
        }
    }

};

pub const SetCrtcTransform = struct {
    pub const opcode: u8 = 26;
    pub const extension: ?extensions.Extension = .RANDR;
    pub const Reply = void;

    crtc: Crtc,
    transform: render.TRANSFORM,
    filter_name: []const u8,
    filter_params: []const i32,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.crtc));
        self.transform.encode(writer);
        writer.writeInt(u16, @intCast(self.filter_name.len));
        writer.splatByte(0, 2);
        writer.write(self.filter_name);
        writer.splatByte(0, wire.pad4(self.filter_name.len));
        for (self.filter_params) |elem| {
            writer.writeInt(i32, elem);
        }
    }

};

pub const SetMonitor = struct {
    pub const opcode: u8 = 43;
    pub const extension: ?extensions.Extension = .RANDR;
    pub const Reply = void;

    window: xproto.Window,
    monitorinfo: MonitorInfo,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.window));
        self.monitorinfo.encode(writer);
    }

};

pub const SetOutputPrimary = struct {
    pub const opcode: u8 = 30;
    pub const extension: ?extensions.Extension = .RANDR;
    pub const Reply = void;

    window: xproto.Window,
    output: Output,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.window));
        writer.writeInt(u32, @intFromEnum(self.output));
    }

};

pub const SetPanningReply = struct {
    status: SetConfig,
    timestamp: u32,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeByte(@intCast(@intFromEnum(self.status)));
        writer.writeInt(u32, self.timestamp);
    }

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        _ = try reader.takeByte();
        const status = @as(SetConfig, @enumFromInt(try reader.takeInt(u8, .native)));
        _ = try reader.takeInt(u16, .native);
        _ = try reader.takeInt(u32, .native);
        const timestamp = try reader.takeInt(u32, .native);
        return .{
            .status = status,
            .timestamp = timestamp,
        };
    }

};

pub const SetPanning = struct {
    pub const opcode: u8 = 29;
    pub const extension: ?extensions.Extension = .RANDR;
    pub const Reply = SetPanningReply;

    crtc: Crtc,
    timestamp: u32,
    left: u16,
    top: u16,
    width: u16,
    height: u16,
    track_left: u16,
    track_top: u16,
    track_width: u16,
    track_height: u16,
    border_left: i16,
    border_top: i16,
    border_right: i16,
    border_bottom: i16,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.crtc));
        writer.writeInt(u32, self.timestamp);
        writer.writeInt(u16, self.left);
        writer.writeInt(u16, self.top);
        writer.writeInt(u16, self.width);
        writer.writeInt(u16, self.height);
        writer.writeInt(u16, self.track_left);
        writer.writeInt(u16, self.track_top);
        writer.writeInt(u16, self.track_width);
        writer.writeInt(u16, self.track_height);
        writer.writeInt(i16, self.border_left);
        writer.writeInt(i16, self.border_top);
        writer.writeInt(i16, self.border_right);
        writer.writeInt(i16, self.border_bottom);
    }

};

pub const SetProviderOffloadSink = struct {
    pub const opcode: u8 = 34;
    pub const extension: ?extensions.Extension = .RANDR;
    pub const Reply = void;

    provider: Provider,
    sink_provider: Provider,
    config_timestamp: u32,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.provider));
        writer.writeInt(u32, @intFromEnum(self.sink_provider));
        writer.writeInt(u32, self.config_timestamp);
    }

};

pub const SetProviderOutputSource = struct {
    pub const opcode: u8 = 35;
    pub const extension: ?extensions.Extension = .RANDR;
    pub const Reply = void;

    provider: Provider,
    source_provider: Provider,
    config_timestamp: u32,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.provider));
        writer.writeInt(u32, @intFromEnum(self.source_provider));
        writer.writeInt(u32, self.config_timestamp);
    }

};

pub const SetScreenConfigReply = struct {
    status: SetConfig,
    new_timestamp: u32,
    config_timestamp: u32,
    root: xproto.Window,
    subpixel_order: render.SubPixel,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeByte(@intCast(@intFromEnum(self.status)));
        writer.writeInt(u32, self.new_timestamp);
        writer.writeInt(u32, self.config_timestamp);
        writer.writeInt(u32, @intFromEnum(self.root));
        writer.writeInt(u16, @intCast(@intFromEnum(self.subpixel_order)));
        writer.splatByte(0, 10);
    }

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        _ = try reader.takeByte();
        const status = @as(SetConfig, @enumFromInt(try reader.takeInt(u8, .native)));
        _ = try reader.takeInt(u16, .native);
        _ = try reader.takeInt(u32, .native);
        const new_timestamp = try reader.takeInt(u32, .native);
        const config_timestamp = try reader.takeInt(u32, .native);
        const root = @as(xproto.Window, @enumFromInt(try reader.takeInt(u32, .native)));
        const subpixel_order = @as(render.SubPixel, @enumFromInt(try reader.takeInt(u16, .native)));
        _ = try reader.take(10);
        return .{
            .status = status,
            .new_timestamp = new_timestamp,
            .config_timestamp = config_timestamp,
            .root = root,
            .subpixel_order = subpixel_order,
        };
    }

};

pub const SetScreenConfig = struct {
    pub const opcode: u8 = 2;
    pub const extension: ?extensions.Extension = .RANDR;
    pub const Reply = SetScreenConfigReply;

    window: xproto.Window,
    timestamp: u32,
    config_timestamp: u32,
    sizeID: u16,
    rotation: u16,
    rate: u16,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.window));
        writer.writeInt(u32, self.timestamp);
        writer.writeInt(u32, self.config_timestamp);
        writer.writeInt(u16, self.sizeID);
        writer.writeInt(u16, self.rotation);
        writer.writeInt(u16, self.rate);
        writer.splatByte(0, 2);
    }

};

pub const SetScreenSize = struct {
    pub const opcode: u8 = 7;
    pub const extension: ?extensions.Extension = .RANDR;
    pub const Reply = void;

    window: xproto.Window,
    width: u16,
    height: u16,
    mm_width: u32,
    mm_height: u32,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.window));
        writer.writeInt(u16, self.width);
        writer.writeInt(u16, self.height);
        writer.writeInt(u32, self.mm_width);
        writer.writeInt(u32, self.mm_height);
    }

};

pub const ScreenChangeNotifyEvent = struct {
    rotation: u8,
    timestamp: u32,
    config_timestamp: u32,
    root: xproto.Window,
    request_window: xproto.Window,
    sizeID: u16,
    subpixel_order: render.SubPixel,
    width: u16,
    height: u16,
    mwidth: u16,
    mheight: u16,

    pub fn toBytes(self: @This()) [32]u8 {
        var packet: [32]u8 = std.mem.zeroes([32]u8);
        var writer_impl = zio.FixedBufferWriter.init(&packet);
        const writer = &writer_impl;
        writer.writeByte(0);
        writer.writeByte(self.rotation);
        writer.writeInt(u16, 0);
        writer.writeInt(u32, self.timestamp);
        writer.writeInt(u32, self.config_timestamp);
        writer.writeInt(u32, @intFromEnum(self.root));
        writer.writeInt(u32, @intFromEnum(self.request_window));
        writer.writeInt(u16, self.sizeID);
        writer.writeInt(u16, @intCast(@intFromEnum(self.subpixel_order)));
        writer.writeInt(u16, self.width);
        writer.writeInt(u16, self.height);
        writer.writeInt(u16, self.mwidth);
        writer.writeInt(u16, self.mheight);
        return packet;
    }

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        _ = try reader.takeByte();
        const rotation = try reader.takeByte();
        _ = try reader.takeInt(u16, .native);
        const timestamp = try reader.takeInt(u32, .native);
        const config_timestamp = try reader.takeInt(u32, .native);
        const root = @as(xproto.Window, @enumFromInt(try reader.takeInt(u32, .native)));
        const request_window = @as(xproto.Window, @enumFromInt(try reader.takeInt(u32, .native)));
        const sizeID = try reader.takeInt(u16, .native);
        const subpixel_order = @as(render.SubPixel, @enumFromInt(try reader.takeInt(u16, .native)));
        const width = try reader.takeInt(u16, .native);
        const height = try reader.takeInt(u16, .native);
        const mwidth = try reader.takeInt(u16, .native);
        const mheight = try reader.takeInt(u16, .native);
        return .{
            .rotation = rotation,
            .timestamp = timestamp,
            .config_timestamp = config_timestamp,
            .root = root,
            .request_window = request_window,
            .sizeID = sizeID,
            .subpixel_order = subpixel_order,
            .width = width,
            .height = height,
            .mwidth = mwidth,
            .mheight = mheight,
        };
    }
};

pub const NotifyEvent = struct {
    subCode: Notify,
    u: NotifyData,

    pub fn toBytes(self: @This()) [32]u8 {
        var packet: [32]u8 = std.mem.zeroes([32]u8);
        var writer_impl = zio.FixedBufferWriter.init(&packet);
        const writer = &writer_impl;
        writer.writeByte(1);
        writer.writeByte(@intCast(@intFromEnum(self.subCode)));
        writer.writeInt(u16, 0);
        self.u.encode(writer);
        return packet;
    }

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        _ = try reader.takeByte();
        const subCode = @as(Notify, @enumFromInt(try reader.takeInt(u8, .native)));
        _ = try reader.takeInt(u16, .native);
        const u = try NotifyData.decode(reader);
        return .{
            .subCode = subCode,
            .u = u,
        };
    }
};

pub fn decodeEvent(reader: *std.Io.Reader) DecodeError!global_events.Event {
    const code = (try reader.peek(1))[0] & 0x7f;
    return switch (code) {
        0 => .{ .RandrScreenChangeNotify = try ScreenChangeNotifyEvent.decode(reader) },
        1 => .{ .RandrNotify = try NotifyEvent.decode(reader) },
        else => blk: {
            const packet = try reader.take(32);
            var raw: [32]u8 = undefined;
            @memcpy(raw[0..], packet);
            break :blk .{ .Unknown = .{
                .code = packet[0] & 0x7f,
                .sequence = std.mem.readInt(u16, packet[2..4], .native),
                .raw = raw,
            } };
        },
    };
}

