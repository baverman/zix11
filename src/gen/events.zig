// zig fmt: off
// This file is generated by tools/genproto.py

const std = @import("std");
const extensions = @import("../_ext.zig");
const DecodeError = @import("../_errors.zig").DecodeError;
const current = @import("toy.zig");

pub const UnknownEvent = struct {
    code: u8,
    sequence: u16,
    raw: [32]u8,
};

pub const Event = union(enum) {
    Unknown: UnknownEvent,
    GEUnknown: current.GeGenericEvent,
    SimpleNotify: current.SimpleNotifyEvent,
    SimpleNotifyCopy: current.SimpleNotifyEvent,
    InfoNotify: current.InfoNotifyEvent,
    InfoNotifyCopy: current.InfoNotifyEvent,
};

pub const ExtensionEventSpec = struct {
    max_event_num: u8,
    decode: ?*const fn (*std.Io.Reader) DecodeError!Event,
    max_xge_event_num: u16,
    decode_xge: ?*const fn (*std.Io.Reader) DecodeError!Event,
};

const current_event_spec: ExtensionEventSpec = .{
    .max_event_num = 2,
    .decode = current.decodeEvent,
    .max_xge_event_num = 8,
    .decode_xge = current.decodeXgeEvent,
};

pub fn eventSpec(extension: extensions.Extension) ?*const ExtensionEventSpec {
    _ = extension;
    return null;
}
