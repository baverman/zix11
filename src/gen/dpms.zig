// zig fmt: off
// This file is generated by tools/genproto.py

const std = @import("std");
const zio = @import("../io.zig");
const wire = @import("../_wire.zig");
const errors = @import("../_errors.zig");
const extensions = @import("../_ext.zig");
const global_events = @import("events.zig");
const xproto = @import("xproto.zig");
const DecodeError = errors.DecodeError;
const AllocDecodeError = errors.AllocDecodeError;
const BufferDecodeError = errors.BufferDecodeError;

pub const DPMSMode = enum(u32) {
    On = 0,
    Standby = 1,
    Suspend = 2,
    Off = 3,
    _,
};

pub const EventMask = enum(u32) {
    InfoNotify = 1,
    _,

    pub fn of(flags: []const @This()) u32 {
        return wire.maskOf(@This(), flags);
    }
};

pub const CapableReply = struct {
    capable: bool,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.splatByte(0, 1);
        writer.writeByte(@intFromBool(self.capable));
        writer.splatByte(0, 23);
    }

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        _ = try reader.takeByte();
        _ = try reader.take(1);
        _ = try reader.takeInt(u16, .native);
        _ = try reader.takeInt(u32, .native);
        const capable = (try reader.takeByte()) != 0;
        _ = try reader.take(23);
        return .{
            .capable = capable,
        };
    }

};

pub const Capable = struct {
    pub const opcode: u8 = 1;
    pub const extension: ?extensions.Extension = .DPMS;
    pub const Reply = CapableReply;


    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        _ = self;
        _ = writer;
    }

};

pub const Disable = struct {
    pub const opcode: u8 = 5;
    pub const extension: ?extensions.Extension = .DPMS;
    pub const Reply = void;


    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        _ = self;
        _ = writer;
    }

};

pub const Enable = struct {
    pub const opcode: u8 = 4;
    pub const extension: ?extensions.Extension = .DPMS;
    pub const Reply = void;


    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        _ = self;
        _ = writer;
    }

};

pub const ForceLevel = struct {
    pub const opcode: u8 = 6;
    pub const extension: ?extensions.Extension = .DPMS;
    pub const Reply = void;

    power_level: DPMSMode,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u16, @intCast(@intFromEnum(self.power_level)));
    }

};

pub const GetTimeoutsReply = struct {
    standby_timeout: u16,
    suspend_timeout: u16,
    off_timeout: u16,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.splatByte(0, 1);
        writer.writeInt(u16, self.standby_timeout);
        writer.writeInt(u16, self.suspend_timeout);
        writer.writeInt(u16, self.off_timeout);
        writer.splatByte(0, 18);
    }

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        _ = try reader.takeByte();
        _ = try reader.take(1);
        _ = try reader.takeInt(u16, .native);
        _ = try reader.takeInt(u32, .native);
        const standby_timeout = try reader.takeInt(u16, .native);
        const suspend_timeout = try reader.takeInt(u16, .native);
        const off_timeout = try reader.takeInt(u16, .native);
        _ = try reader.take(18);
        return .{
            .standby_timeout = standby_timeout,
            .suspend_timeout = suspend_timeout,
            .off_timeout = off_timeout,
        };
    }

};

pub const GetTimeouts = struct {
    pub const opcode: u8 = 2;
    pub const extension: ?extensions.Extension = .DPMS;
    pub const Reply = GetTimeoutsReply;


    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        _ = self;
        _ = writer;
    }

};

pub const GetVersionReply = struct {
    server_major_version: u16,
    server_minor_version: u16,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.splatByte(0, 1);
        writer.writeInt(u16, self.server_major_version);
        writer.writeInt(u16, self.server_minor_version);
    }

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        _ = try reader.takeByte();
        _ = try reader.take(1);
        _ = try reader.takeInt(u16, .native);
        _ = try reader.takeInt(u32, .native);
        const server_major_version = try reader.takeInt(u16, .native);
        const server_minor_version = try reader.takeInt(u16, .native);
        return .{
            .server_major_version = server_major_version,
            .server_minor_version = server_minor_version,
        };
    }

};

pub const GetVersion = struct {
    pub const opcode: u8 = 0;
    pub const extension: ?extensions.Extension = .DPMS;
    pub const Reply = GetVersionReply;

    client_major_version: u16,
    client_minor_version: u16,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u16, self.client_major_version);
        writer.writeInt(u16, self.client_minor_version);
    }

};

pub const InfoReply = struct {
    power_level: DPMSMode,
    state: bool,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.splatByte(0, 1);
        writer.writeInt(u16, @intCast(@intFromEnum(self.power_level)));
        writer.writeByte(@intFromBool(self.state));
        writer.splatByte(0, 21);
    }

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        _ = try reader.takeByte();
        _ = try reader.take(1);
        _ = try reader.takeInt(u16, .native);
        _ = try reader.takeInt(u32, .native);
        const power_level = @as(DPMSMode, @enumFromInt(try reader.takeInt(u16, .native)));
        const state = (try reader.takeByte()) != 0;
        _ = try reader.take(21);
        return .{
            .power_level = power_level,
            .state = state,
        };
    }

};

pub const Info = struct {
    pub const opcode: u8 = 7;
    pub const extension: ?extensions.Extension = .DPMS;
    pub const Reply = InfoReply;


    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        _ = self;
        _ = writer;
    }

};

pub const SelectInput = struct {
    pub const opcode: u8 = 8;
    pub const extension: ?extensions.Extension = .DPMS;
    pub const Reply = void;

    event_mask: u32,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, self.event_mask);
    }

};

pub const SetTimeouts = struct {
    pub const opcode: u8 = 3;
    pub const extension: ?extensions.Extension = .DPMS;
    pub const Reply = void;

    standby_timeout: u16,
    suspend_timeout: u16,
    off_timeout: u16,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u16, self.standby_timeout);
        writer.writeInt(u16, self.suspend_timeout);
        writer.writeInt(u16, self.off_timeout);
    }

};

pub const InfoNotifyEvent = struct {
    extension: u8,
    length: u32,
    event_type: u16,
    full_sequence: u32,
    timestamp: u32,
    power_level: DPMSMode,
    state: bool,

    pub fn toBytes(self: @This()) [32]u8 {
        var packet: [32]u8 = std.mem.zeroes([32]u8);
        var writer_impl = zio.FixedBufferWriter.init(&packet);
        const writer = &writer_impl;
        writer.writeByte(0);
        writer.writeByte(self.extension);
        writer.writeInt(u16, 0);
        writer.writeInt(u32, self.length);
        writer.writeInt(u16, self.event_type);
        writer.writeInt(u16, 0);
        writer.writeInt(u32, self.full_sequence);
        writer.splatByte(0, 2);
        writer.writeInt(u32, self.timestamp);
        writer.writeInt(u16, @intCast(@intFromEnum(self.power_level)));
        writer.writeByte(@intFromBool(self.state));
        writer.splatByte(0, 21);
        return packet;
    }

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        _ = try reader.takeByte();
        const extension = try reader.takeByte();
        _ = try reader.takeInt(u16, .native);
        const length = try reader.takeInt(u32, .native);
        const event_type = try reader.takeInt(u16, .native);
        _ = try reader.take(2);
        const full_sequence = try reader.takeInt(u32, .native);
        _ = try reader.take(2);
        const timestamp = try reader.takeInt(u32, .native);
        const power_level = @as(DPMSMode, @enumFromInt(try reader.takeInt(u16, .native)));
        const state = (try reader.takeByte()) != 0;
        _ = try reader.take(21);
        const xge_body_len = 2 + 4 + 2 + 1 + 21;
        const total_body_len = 16 + @as(usize, length) * 4;
        if (xge_body_len < total_body_len) _ = try reader.take(total_body_len - xge_body_len);
        return .{
            .extension = extension,
            .length = length,
            .event_type = event_type,
            .full_sequence = full_sequence,
            .timestamp = timestamp,
            .power_level = power_level,
            .state = state,
        };
    }
};

pub fn decodeXgeEvent(packet: []const u8) DecodeError!global_events.Event {
    const header = packet[0..10];
    const event_type = std.mem.readInt(u16, header[8..10], .native);
    return switch (event_type) {
        0 => blk: {
            var reader: std.Io.Reader = .fixed(packet);
            break :blk .{ .DpmsInfoNotify = try InfoNotifyEvent.decode(&reader) };
        },
        else => blk: {
            var raw: [32]u8 = undefined;
            @memcpy(raw[0..], packet[0..32]);
            break :blk .{ .Unknown = .{
                .code = packet[0] & 0x7f,
                .sequence = std.mem.readInt(u16, packet[2..4], .native),
                .raw = raw,
            } };
        },
    };
}

