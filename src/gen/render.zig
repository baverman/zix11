// zig fmt: off
// This file is generated by tools/genproto.py

const std = @import("std");
const io = @import("../io.zig");
const wire = @import("../_wire.zig");
const Atom = @import("xproto.zig").Atom;
const Colormap = @import("xproto.zig").Colormap;
const Cursor = @import("xproto.zig").Cursor;
const Drawable = @import("xproto.zig").Drawable;
const Pixmap = @import("xproto.zig").Pixmap;
const RECTANGLE = @import("xproto.zig").RECTANGLE;
const STR = @import("xproto.zig").STR;
const SubwindowMode = @import("xproto.zig").SubwindowMode;

pub const Glyphset = enum(u32) {
    _,
};

pub const Picture = enum(u32) {
    None = 0,
    _,
};

pub const Pictformat = enum(u32) {
    _,
};

pub const PictType = enum(u32) {
    Indexed = 0,
    Direct = 1,
};

pub const PictOp = enum(u32) {
    Clear = 0,
    Src = 1,
    Dst = 2,
    Over = 3,
    OverReverse = 4,
    In = 5,
    InReverse = 6,
    Out = 7,
    OutReverse = 8,
    Atop = 9,
    AtopReverse = 10,
    Xor = 11,
    Add = 12,
    Saturate = 13,
    DisjointClear = 16,
    DisjointSrc = 17,
    DisjointDst = 18,
    DisjointOver = 19,
    DisjointOverReverse = 20,
    DisjointIn = 21,
    DisjointInReverse = 22,
    DisjointOut = 23,
    DisjointOutReverse = 24,
    DisjointAtop = 25,
    DisjointAtopReverse = 26,
    DisjointXor = 27,
    ConjointClear = 32,
    ConjointSrc = 33,
    ConjointDst = 34,
    ConjointOver = 35,
    ConjointOverReverse = 36,
    ConjointIn = 37,
    ConjointInReverse = 38,
    ConjointOut = 39,
    ConjointOutReverse = 40,
    ConjointAtop = 41,
    ConjointAtopReverse = 42,
    ConjointXor = 43,
    Multiply = 48,
    Screen = 49,
    Overlay = 50,
    Darken = 51,
    Lighten = 52,
    ColorDodge = 53,
    ColorBurn = 54,
    HardLight = 55,
    SoftLight = 56,
    Difference = 57,
    Exclusion = 58,
    HSLHue = 59,
    HSLSaturation = 60,
    HSLColor = 61,
    HSLLuminosity = 62,
};

pub const PolyEdge = enum(u32) {
    Sharp = 0,
    Smooth = 1,
};

pub const PolyMode = enum(u32) {
    Precise = 0,
    Imprecise = 1,
};

pub const CP = enum(u32) {
    Repeat = 1,
    AlphaMap = 2,
    AlphaXOrigin = 4,
    AlphaYOrigin = 8,
    ClipXOrigin = 16,
    ClipYOrigin = 32,
    ClipMask = 64,
    GraphicsExposure = 128,
    SubwindowMode = 256,
    PolyEdge = 512,
    PolyMode = 1024,
    Dither = 2048,
    ComponentAlpha = 4096,
};

pub const SubPixel = enum(u32) {
    Unknown = 0,
    HorizontalRGB = 1,
    HorizontalBGR = 2,
    VerticalRGB = 3,
    VerticalBGR = 4,
    None = 5,
};

pub const Repeat = enum(u32) {
    None = 0,
    Normal = 1,
    Pad = 2,
    Reflect = 3,
};

pub const DIRECTFORMAT = struct {
    red_shift: u16,
    red_mask: u16,
    green_shift: u16,
    green_mask: u16,
    blue_shift: u16,
    blue_mask: u16,
    alpha_shift: u16,
    alpha_mask: u16,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u16, self.red_shift);
        writer.writeInt(u16, self.red_mask);
        writer.writeInt(u16, self.green_shift);
        writer.writeInt(u16, self.green_mask);
        writer.writeInt(u16, self.blue_shift);
        writer.writeInt(u16, self.blue_mask);
        writer.writeInt(u16, self.alpha_shift);
        writer.writeInt(u16, self.alpha_mask);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.red_shift = try reader.takeInt(u16, .native);
        result.red_mask = try reader.takeInt(u16, .native);
        result.green_shift = try reader.takeInt(u16, .native);
        result.green_mask = try reader.takeInt(u16, .native);
        result.blue_shift = try reader.takeInt(u16, .native);
        result.blue_mask = try reader.takeInt(u16, .native);
        result.alpha_shift = try reader.takeInt(u16, .native);
        result.alpha_mask = try reader.takeInt(u16, .native);
        return result;
    }
};

pub const PICTFORMINFO = struct {
    id: Pictformat,
    type: PictType,
    depth: u8,
    direct: DIRECTFORMAT,
    colormap: Colormap,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u32, @intCast(@intFromEnum(self.id)));
        writer.writeByte(@intCast(@intFromEnum(self.type)));
        writer.writeByte(self.depth);
        writer.splatByte(0, 2);
        try self.direct.encode(writer);
        writer.writeInt(u32, @intCast(@intFromEnum(self.colormap)));
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.id = @as(Pictformat, @enumFromInt(try reader.takeInt(u32, .native)));
        result.type = @as(PictType, @enumFromInt(try reader.takeByte()));
        result.depth = try reader.takeByte();
        _ = try reader.take(2);
        result.direct = try DIRECTFORMAT.decode(reader);
        result.colormap = @as(Colormap, @enumFromInt(try reader.takeInt(u32, .native)));
        return result;
    }
};

pub const PICTVISUAL = struct {
    visual: u32,
    format: Pictformat,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u32, self.visual);
        writer.writeInt(u32, @intCast(@intFromEnum(self.format)));
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.visual = try reader.takeInt(u32, .native);
        result.format = @as(Pictformat, @enumFromInt(try reader.takeInt(u32, .native)));
        return result;
    }
};

pub const PICTDEPTH = struct {
    depth: u8,
    visuals: []const PICTVISUAL,
    decoded_visuals_buf: ?[]PICTVISUAL = null,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeByte(self.depth);
        writer.splatByte(0, 1);
        writer.writeInt(u16, @intCast(self.visuals.len));
        writer.splatByte(0, 4);
        for (self.visuals) |elem| {
            try elem.encode(writer);
        }
    }

    pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.depth = try reader.takeByte();
        _ = try reader.take(1);
        const num_visuals = try reader.takeInt(u16, .native);
        _ = try reader.take(4);
        const decoded_visuals_buf = try allocator.alloc(PICTVISUAL, @intCast(num_visuals));
        for (decoded_visuals_buf) |*elem| {
            elem.* = try PICTVISUAL.decode(reader);
        }
        result.visuals = decoded_visuals_buf;
        result.decoded_visuals_buf = decoded_visuals_buf;
        return result;
    }

    pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
        if (self.decoded_visuals_buf) |buf| {
            allocator.free(buf);
            self.decoded_visuals_buf = null;
            self.visuals = &.{};
        }
    }
};

pub const PICTSCREEN = struct {
    fallback: Pictformat,
    depths: []const PICTDEPTH,
    decoded_depths_buf: ?[]PICTDEPTH = null,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u32, @intCast(self.depths.len));
        writer.writeInt(u32, @intCast(@intFromEnum(self.fallback)));
        for (self.depths) |elem| {
            try elem.encode(writer);
        }
    }

    pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        const num_depths = try reader.takeInt(u32, .native);
        result.fallback = @as(Pictformat, @enumFromInt(try reader.takeInt(u32, .native)));
        const decoded_depths_buf = try allocator.alloc(PICTDEPTH, @intCast(num_depths));
        for (decoded_depths_buf) |*elem| {
            elem.* = try PICTDEPTH.decode(allocator, reader);
        }
        result.depths = decoded_depths_buf;
        result.decoded_depths_buf = decoded_depths_buf;
        return result;
    }

    pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
        if (self.decoded_depths_buf) |buf| {
            for (buf) |*it| {
                it.deinit(allocator);
            }
            allocator.free(buf);
            self.decoded_depths_buf = null;
            self.depths = &.{};
        }
    }
};

pub const INDEXVALUE = struct {
    pixel: u32,
    red: u16,
    green: u16,
    blue: u16,
    alpha: u16,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u32, self.pixel);
        writer.writeInt(u16, self.red);
        writer.writeInt(u16, self.green);
        writer.writeInt(u16, self.blue);
        writer.writeInt(u16, self.alpha);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.pixel = try reader.takeInt(u32, .native);
        result.red = try reader.takeInt(u16, .native);
        result.green = try reader.takeInt(u16, .native);
        result.blue = try reader.takeInt(u16, .native);
        result.alpha = try reader.takeInt(u16, .native);
        return result;
    }
};

pub const COLOR = struct {
    red: u16,
    green: u16,
    blue: u16,
    alpha: u16,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u16, self.red);
        writer.writeInt(u16, self.green);
        writer.writeInt(u16, self.blue);
        writer.writeInt(u16, self.alpha);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.red = try reader.takeInt(u16, .native);
        result.green = try reader.takeInt(u16, .native);
        result.blue = try reader.takeInt(u16, .native);
        result.alpha = try reader.takeInt(u16, .native);
        return result;
    }
};

pub const POINTFIX = struct {
    x: i32,
    y: i32,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(i32, self.x);
        writer.writeInt(i32, self.y);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.x = try reader.takeInt(i32, .native);
        result.y = try reader.takeInt(i32, .native);
        return result;
    }
};

pub const LINEFIX = struct {
    p1: POINTFIX,
    p2: POINTFIX,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        try self.p1.encode(writer);
        try self.p2.encode(writer);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.p1 = try POINTFIX.decode(reader);
        result.p2 = try POINTFIX.decode(reader);
        return result;
    }
};

pub const TRIANGLE = struct {
    p1: POINTFIX,
    p2: POINTFIX,
    p3: POINTFIX,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        try self.p1.encode(writer);
        try self.p2.encode(writer);
        try self.p3.encode(writer);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.p1 = try POINTFIX.decode(reader);
        result.p2 = try POINTFIX.decode(reader);
        result.p3 = try POINTFIX.decode(reader);
        return result;
    }
};

pub const TRAPEZOID = struct {
    top: i32,
    bottom: i32,
    left: LINEFIX,
    right: LINEFIX,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(i32, self.top);
        writer.writeInt(i32, self.bottom);
        try self.left.encode(writer);
        try self.right.encode(writer);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.top = try reader.takeInt(i32, .native);
        result.bottom = try reader.takeInt(i32, .native);
        result.left = try LINEFIX.decode(reader);
        result.right = try LINEFIX.decode(reader);
        return result;
    }
};

pub const GLYPHINFO = struct {
    width: u16,
    height: u16,
    x: i16,
    y: i16,
    x_off: i16,
    y_off: i16,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u16, self.width);
        writer.writeInt(u16, self.height);
        writer.writeInt(i16, self.x);
        writer.writeInt(i16, self.y);
        writer.writeInt(i16, self.x_off);
        writer.writeInt(i16, self.y_off);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.width = try reader.takeInt(u16, .native);
        result.height = try reader.takeInt(u16, .native);
        result.x = try reader.takeInt(i16, .native);
        result.y = try reader.takeInt(i16, .native);
        result.x_off = try reader.takeInt(i16, .native);
        result.y_off = try reader.takeInt(i16, .native);
        return result;
    }
};

pub const TRANSFORM = struct {
    matrix11: i32,
    matrix12: i32,
    matrix13: i32,
    matrix21: i32,
    matrix22: i32,
    matrix23: i32,
    matrix31: i32,
    matrix32: i32,
    matrix33: i32,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(i32, self.matrix11);
        writer.writeInt(i32, self.matrix12);
        writer.writeInt(i32, self.matrix13);
        writer.writeInt(i32, self.matrix21);
        writer.writeInt(i32, self.matrix22);
        writer.writeInt(i32, self.matrix23);
        writer.writeInt(i32, self.matrix31);
        writer.writeInt(i32, self.matrix32);
        writer.writeInt(i32, self.matrix33);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.matrix11 = try reader.takeInt(i32, .native);
        result.matrix12 = try reader.takeInt(i32, .native);
        result.matrix13 = try reader.takeInt(i32, .native);
        result.matrix21 = try reader.takeInt(i32, .native);
        result.matrix22 = try reader.takeInt(i32, .native);
        result.matrix23 = try reader.takeInt(i32, .native);
        result.matrix31 = try reader.takeInt(i32, .native);
        result.matrix32 = try reader.takeInt(i32, .native);
        result.matrix33 = try reader.takeInt(i32, .native);
        return result;
    }
};

pub const ANIMCURSORELT = struct {
    cursor: Cursor,
    delay: u32,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u32, @intCast(@intFromEnum(self.cursor)));
        writer.writeInt(u32, self.delay);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.cursor = @as(Cursor, @enumFromInt(try reader.takeInt(u32, .native)));
        result.delay = try reader.takeInt(u32, .native);
        return result;
    }
};

pub const SPANFIX = struct {
    l: i32,
    r: i32,
    y: i32,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(i32, self.l);
        writer.writeInt(i32, self.r);
        writer.writeInt(i32, self.y);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.l = try reader.takeInt(i32, .native);
        result.r = try reader.takeInt(i32, .native);
        result.y = try reader.takeInt(i32, .native);
        return result;
    }
};

pub const TRAP = struct {
    top: SPANFIX,
    bot: SPANFIX,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        try self.top.encode(writer);
        try self.bot.encode(writer);
    }

    pub fn decode(reader: *std.Io.Reader) !@This() {
        var result: @This() = undefined;
        result.top = try SPANFIX.decode(reader);
        result.bot = try SPANFIX.decode(reader);
        return result;
    }
};

pub const QueryVersion = struct {
    pub const opcode: u8 = 0;

    client_major_version: u32,
    client_minor_version: u32,

    pub const Reply = struct {
        major_version: u32,
        minor_version: u32,

        pub fn decode(reader: *std.Io.Reader, header_: wire.ReplyHeader) !@This() {
            var result: @This() = undefined;
            _ = header_;
            _ = try reader.take(1);
            result.major_version = try reader.takeInt(u32, .native);
            result.minor_version = try reader.takeInt(u32, .native);
            _ = try reader.take(16);
            return result;
        }
    };


    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u32, self.client_major_version);
        writer.writeInt(u32, self.client_minor_version);
    }
};

pub const QueryPictFormats = struct {
    pub const opcode: u8 = 1;


    pub const Reply = struct {
        num_depths: u32,
        num_visuals: u32,
        formats: []const PICTFORMINFO,
        decoded_formats_buf: ?[]PICTFORMINFO = null,
        screens: []const PICTSCREEN,
        decoded_screens_buf: ?[]PICTSCREEN = null,
        subpixels: []const u32,
        decoded_subpixels_buf: ?[]u32 = null,

        pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader, header_: wire.ReplyHeader) !@This() {
            var result: @This() = undefined;
            _ = header_;
            _ = try reader.take(1);
            const num_formats = try reader.takeInt(u32, .native);
            const num_screens = try reader.takeInt(u32, .native);
            result.num_depths = try reader.takeInt(u32, .native);
            result.num_visuals = try reader.takeInt(u32, .native);
            const num_subpixel = try reader.takeInt(u32, .native);
            _ = try reader.take(4);
            const decoded_formats_buf = try allocator.alloc(PICTFORMINFO, @intCast(num_formats));
            for (decoded_formats_buf) |*elem| {
                elem.* = try PICTFORMINFO.decode(reader);
            }
            result.formats = decoded_formats_buf;
            result.decoded_formats_buf = decoded_formats_buf;
            const decoded_screens_buf = try allocator.alloc(PICTSCREEN, @intCast(num_screens));
            for (decoded_screens_buf) |*elem| {
                elem.* = try PICTSCREEN.decode(allocator, reader);
            }
            result.screens = decoded_screens_buf;
            result.decoded_screens_buf = decoded_screens_buf;
            const decoded_subpixels_buf = try allocator.alloc(u32, @intCast(num_subpixel));
            for (decoded_subpixels_buf) |*elem| {
                elem.* = try reader.takeInt(u32, .native);
            }
            result.subpixels = decoded_subpixels_buf;
            result.decoded_subpixels_buf = decoded_subpixels_buf;
            return result;
        }

        pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
            if (self.decoded_formats_buf) |buf| {
                allocator.free(buf);
                self.decoded_formats_buf = null;
                self.formats = &.{};
            }
            if (self.decoded_screens_buf) |buf| {
                for (buf) |*it| {
                    it.deinit(allocator);
                }
                allocator.free(buf);
                self.decoded_screens_buf = null;
                self.screens = &.{};
            }
            if (self.decoded_subpixels_buf) |buf| {
                allocator.free(buf);
                self.decoded_subpixels_buf = null;
                self.subpixels = &.{};
            }
        }
    };


    pub fn encode(self: *const @This(), writer: anytype) !void {
        _ = self;
        _ = writer;
    }
};

pub const QueryPictIndexValues = struct {
    pub const opcode: u8 = 2;

    format: Pictformat,

    pub const Reply = struct {
        values: []const INDEXVALUE,
        decoded_values_buf: ?[]INDEXVALUE = null,

        pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader, header_: wire.ReplyHeader) !@This() {
            var result: @This() = undefined;
            _ = header_;
            _ = try reader.take(1);
            const num_values = try reader.takeInt(u32, .native);
            _ = try reader.take(20);
            const decoded_values_buf = try allocator.alloc(INDEXVALUE, @intCast(num_values));
            for (decoded_values_buf) |*elem| {
                elem.* = try INDEXVALUE.decode(reader);
            }
            result.values = decoded_values_buf;
            result.decoded_values_buf = decoded_values_buf;
            return result;
        }

        pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
            if (self.decoded_values_buf) |buf| {
                allocator.free(buf);
                self.decoded_values_buf = null;
                self.values = &.{};
            }
        }
    };


    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u32, @intCast(@intFromEnum(self.format)));
    }
};

pub const CreatePicture = struct {
    pub const opcode: u8 = 4;

    pid: Picture,
    drawable: Drawable,
    format: Pictformat,
    value_list: Value_list,

    pub const Value_list = struct {
        repeat: ?Repeat = null,
        alphamap: ?Picture = null,
        alphaxorigin: ?i32 = null,
        alphayorigin: ?i32 = null,
        clipxorigin: ?i32 = null,
        clipyorigin: ?i32 = null,
        clipmask: ?Pixmap = null,
        graphicsexposure: ?u32 = null,
        subwindowmode: ?SubwindowMode = null,
        polyedge: ?PolyEdge = null,
        polymode: ?PolyMode = null,
        dither: ?Atom = null,
        componentalpha: ?u32 = null,

        pub fn encode(self: *const @This(), writer: anytype) !void {
            if (self.repeat) |it| {
                writer.writeInt(u32, @intCast(@intFromEnum(it)));
            }
            if (self.alphamap) |it| {
                writer.writeInt(u32, @intCast(@intFromEnum(it)));
            }
            if (self.alphaxorigin) |it| {
                writer.writeInt(i32, it);
            }
            if (self.alphayorigin) |it| {
                writer.writeInt(i32, it);
            }
            if (self.clipxorigin) |it| {
                writer.writeInt(i32, it);
            }
            if (self.clipyorigin) |it| {
                writer.writeInt(i32, it);
            }
            if (self.clipmask) |it| {
                writer.writeInt(u32, @intCast(@intFromEnum(it)));
            }
            if (self.graphicsexposure) |it| {
                writer.writeInt(u32, it);
            }
            if (self.subwindowmode) |it| {
                writer.writeInt(u32, @intCast(@intFromEnum(it)));
            }
            if (self.polyedge) |it| {
                writer.writeInt(u32, @intCast(@intFromEnum(it)));
            }
            if (self.polymode) |it| {
                writer.writeInt(u32, @intCast(@intFromEnum(it)));
            }
            if (self.dither) |it| {
                writer.writeInt(u32, @intCast(@intFromEnum(it)));
            }
            if (self.componentalpha) |it| {
                writer.writeInt(u32, it);
            }
        }

        pub fn switchValue(self: *const @This()) u32 {
            var result: u32 = 0;
            if (self.repeat != null) result |= 1;
            if (self.alphamap != null) result |= 2;
            if (self.alphaxorigin != null) result |= 4;
            if (self.alphayorigin != null) result |= 8;
            if (self.clipxorigin != null) result |= 16;
            if (self.clipyorigin != null) result |= 32;
            if (self.clipmask != null) result |= 64;
            if (self.graphicsexposure != null) result |= 128;
            if (self.subwindowmode != null) result |= 256;
            if (self.polyedge != null) result |= 512;
            if (self.polymode != null) result |= 1024;
            if (self.dither != null) result |= 2048;
            if (self.componentalpha != null) result |= 4096;
            return result;
        }

        pub fn decode(reader: *std.Io.Reader, switch_value: u32) !@This() {
            var result: @This() = .{};
            if ((switch_value & 1) != 0) {
                const repeat = @as(Repeat, @enumFromInt(try reader.takeInt(u32, .native)));
                result.repeat = repeat;
            }
            if ((switch_value & 2) != 0) {
                const alphamap = @as(Picture, @enumFromInt(try reader.takeInt(u32, .native)));
                result.alphamap = alphamap;
            }
            if ((switch_value & 4) != 0) {
                const alphaxorigin = try reader.takeInt(i32, .native);
                result.alphaxorigin = alphaxorigin;
            }
            if ((switch_value & 8) != 0) {
                const alphayorigin = try reader.takeInt(i32, .native);
                result.alphayorigin = alphayorigin;
            }
            if ((switch_value & 16) != 0) {
                const clipxorigin = try reader.takeInt(i32, .native);
                result.clipxorigin = clipxorigin;
            }
            if ((switch_value & 32) != 0) {
                const clipyorigin = try reader.takeInt(i32, .native);
                result.clipyorigin = clipyorigin;
            }
            if ((switch_value & 64) != 0) {
                const clipmask = @as(Pixmap, @enumFromInt(try reader.takeInt(u32, .native)));
                result.clipmask = clipmask;
            }
            if ((switch_value & 128) != 0) {
                const graphicsexposure = try reader.takeInt(u32, .native);
                result.graphicsexposure = graphicsexposure;
            }
            if ((switch_value & 256) != 0) {
                const subwindowmode = @as(SubwindowMode, @enumFromInt(try reader.takeInt(u32, .native)));
                result.subwindowmode = subwindowmode;
            }
            if ((switch_value & 512) != 0) {
                const polyedge = @as(PolyEdge, @enumFromInt(try reader.takeInt(u32, .native)));
                result.polyedge = polyedge;
            }
            if ((switch_value & 1024) != 0) {
                const polymode = @as(PolyMode, @enumFromInt(try reader.takeInt(u32, .native)));
                result.polymode = polymode;
            }
            if ((switch_value & 2048) != 0) {
                const dither = @as(Atom, @enumFromInt(try reader.takeInt(u32, .native)));
                result.dither = dither;
            }
            if ((switch_value & 4096) != 0) {
                const componentalpha = try reader.takeInt(u32, .native);
                result.componentalpha = componentalpha;
            }
            return result;
        }
    };

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u32, @intCast(@intFromEnum(self.pid)));
        writer.writeInt(u32, self.drawable.toInt());
        writer.writeInt(u32, @intCast(@intFromEnum(self.format)));
        writer.writeInt(u32, @intCast(self.value_list.switchValue()));
        try self.value_list.encode(writer);
    }
};

pub const ChangePicture = struct {
    pub const opcode: u8 = 5;

    picture: Picture,
    value_list: Value_list,

    pub const Value_list = struct {
        repeat: ?Repeat = null,
        alphamap: ?Picture = null,
        alphaxorigin: ?i32 = null,
        alphayorigin: ?i32 = null,
        clipxorigin: ?i32 = null,
        clipyorigin: ?i32 = null,
        clipmask: ?Pixmap = null,
        graphicsexposure: ?u32 = null,
        subwindowmode: ?SubwindowMode = null,
        polyedge: ?PolyEdge = null,
        polymode: ?PolyMode = null,
        dither: ?Atom = null,
        componentalpha: ?u32 = null,

        pub fn encode(self: *const @This(), writer: anytype) !void {
            if (self.repeat) |it| {
                writer.writeInt(u32, @intCast(@intFromEnum(it)));
            }
            if (self.alphamap) |it| {
                writer.writeInt(u32, @intCast(@intFromEnum(it)));
            }
            if (self.alphaxorigin) |it| {
                writer.writeInt(i32, it);
            }
            if (self.alphayorigin) |it| {
                writer.writeInt(i32, it);
            }
            if (self.clipxorigin) |it| {
                writer.writeInt(i32, it);
            }
            if (self.clipyorigin) |it| {
                writer.writeInt(i32, it);
            }
            if (self.clipmask) |it| {
                writer.writeInt(u32, @intCast(@intFromEnum(it)));
            }
            if (self.graphicsexposure) |it| {
                writer.writeInt(u32, it);
            }
            if (self.subwindowmode) |it| {
                writer.writeInt(u32, @intCast(@intFromEnum(it)));
            }
            if (self.polyedge) |it| {
                writer.writeInt(u32, @intCast(@intFromEnum(it)));
            }
            if (self.polymode) |it| {
                writer.writeInt(u32, @intCast(@intFromEnum(it)));
            }
            if (self.dither) |it| {
                writer.writeInt(u32, @intCast(@intFromEnum(it)));
            }
            if (self.componentalpha) |it| {
                writer.writeInt(u32, it);
            }
        }

        pub fn switchValue(self: *const @This()) u32 {
            var result: u32 = 0;
            if (self.repeat != null) result |= 1;
            if (self.alphamap != null) result |= 2;
            if (self.alphaxorigin != null) result |= 4;
            if (self.alphayorigin != null) result |= 8;
            if (self.clipxorigin != null) result |= 16;
            if (self.clipyorigin != null) result |= 32;
            if (self.clipmask != null) result |= 64;
            if (self.graphicsexposure != null) result |= 128;
            if (self.subwindowmode != null) result |= 256;
            if (self.polyedge != null) result |= 512;
            if (self.polymode != null) result |= 1024;
            if (self.dither != null) result |= 2048;
            if (self.componentalpha != null) result |= 4096;
            return result;
        }

        pub fn decode(reader: *std.Io.Reader, switch_value: u32) !@This() {
            var result: @This() = .{};
            if ((switch_value & 1) != 0) {
                const repeat = @as(Repeat, @enumFromInt(try reader.takeInt(u32, .native)));
                result.repeat = repeat;
            }
            if ((switch_value & 2) != 0) {
                const alphamap = @as(Picture, @enumFromInt(try reader.takeInt(u32, .native)));
                result.alphamap = alphamap;
            }
            if ((switch_value & 4) != 0) {
                const alphaxorigin = try reader.takeInt(i32, .native);
                result.alphaxorigin = alphaxorigin;
            }
            if ((switch_value & 8) != 0) {
                const alphayorigin = try reader.takeInt(i32, .native);
                result.alphayorigin = alphayorigin;
            }
            if ((switch_value & 16) != 0) {
                const clipxorigin = try reader.takeInt(i32, .native);
                result.clipxorigin = clipxorigin;
            }
            if ((switch_value & 32) != 0) {
                const clipyorigin = try reader.takeInt(i32, .native);
                result.clipyorigin = clipyorigin;
            }
            if ((switch_value & 64) != 0) {
                const clipmask = @as(Pixmap, @enumFromInt(try reader.takeInt(u32, .native)));
                result.clipmask = clipmask;
            }
            if ((switch_value & 128) != 0) {
                const graphicsexposure = try reader.takeInt(u32, .native);
                result.graphicsexposure = graphicsexposure;
            }
            if ((switch_value & 256) != 0) {
                const subwindowmode = @as(SubwindowMode, @enumFromInt(try reader.takeInt(u32, .native)));
                result.subwindowmode = subwindowmode;
            }
            if ((switch_value & 512) != 0) {
                const polyedge = @as(PolyEdge, @enumFromInt(try reader.takeInt(u32, .native)));
                result.polyedge = polyedge;
            }
            if ((switch_value & 1024) != 0) {
                const polymode = @as(PolyMode, @enumFromInt(try reader.takeInt(u32, .native)));
                result.polymode = polymode;
            }
            if ((switch_value & 2048) != 0) {
                const dither = @as(Atom, @enumFromInt(try reader.takeInt(u32, .native)));
                result.dither = dither;
            }
            if ((switch_value & 4096) != 0) {
                const componentalpha = try reader.takeInt(u32, .native);
                result.componentalpha = componentalpha;
            }
            return result;
        }
    };

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u32, @intCast(@intFromEnum(self.picture)));
        writer.writeInt(u32, @intCast(self.value_list.switchValue()));
        try self.value_list.encode(writer);
    }
};

pub const SetPictureClipRectangles = struct {
    pub const opcode: u8 = 6;

    picture: Picture,
    clip_x_origin: i16,
    clip_y_origin: i16,
    rectangles: []const RECTANGLE,
    decoded_rectangles_buf: ?[]RECTANGLE = null,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u32, @intCast(@intFromEnum(self.picture)));
        writer.writeInt(i16, self.clip_x_origin);
        writer.writeInt(i16, self.clip_y_origin);
        for (self.rectangles) |elem| {
            try elem.encode(writer);
        }
    }
};

pub const FreePicture = struct {
    pub const opcode: u8 = 7;

    picture: Picture,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u32, @intCast(@intFromEnum(self.picture)));
    }
};

pub const Composite = struct {
    pub const opcode: u8 = 8;

    op: PictOp,
    src: Picture,
    mask: Picture,
    dst: Picture,
    src_x: i16,
    src_y: i16,
    mask_x: i16,
    mask_y: i16,
    dst_x: i16,
    dst_y: i16,
    width: u16,
    height: u16,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeByte(@intCast(@intFromEnum(self.op)));
        writer.splatByte(0, 3);
        writer.writeInt(u32, @intCast(@intFromEnum(self.src)));
        writer.writeInt(u32, @intCast(@intFromEnum(self.mask)));
        writer.writeInt(u32, @intCast(@intFromEnum(self.dst)));
        writer.writeInt(i16, self.src_x);
        writer.writeInt(i16, self.src_y);
        writer.writeInt(i16, self.mask_x);
        writer.writeInt(i16, self.mask_y);
        writer.writeInt(i16, self.dst_x);
        writer.writeInt(i16, self.dst_y);
        writer.writeInt(u16, self.width);
        writer.writeInt(u16, self.height);
    }
};

pub const Trapezoids = struct {
    pub const opcode: u8 = 10;

    op: PictOp,
    src: Picture,
    dst: Picture,
    mask_format: Pictformat,
    src_x: i16,
    src_y: i16,
    traps: []const TRAPEZOID,
    decoded_traps_buf: ?[]TRAPEZOID = null,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeByte(@intCast(@intFromEnum(self.op)));
        writer.splatByte(0, 3);
        writer.writeInt(u32, @intCast(@intFromEnum(self.src)));
        writer.writeInt(u32, @intCast(@intFromEnum(self.dst)));
        writer.writeInt(u32, @intCast(@intFromEnum(self.mask_format)));
        writer.writeInt(i16, self.src_x);
        writer.writeInt(i16, self.src_y);
        for (self.traps) |elem| {
            try elem.encode(writer);
        }
    }
};

pub const Triangles = struct {
    pub const opcode: u8 = 11;

    op: PictOp,
    src: Picture,
    dst: Picture,
    mask_format: Pictformat,
    src_x: i16,
    src_y: i16,
    triangles: []const TRIANGLE,
    decoded_triangles_buf: ?[]TRIANGLE = null,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeByte(@intCast(@intFromEnum(self.op)));
        writer.splatByte(0, 3);
        writer.writeInt(u32, @intCast(@intFromEnum(self.src)));
        writer.writeInt(u32, @intCast(@intFromEnum(self.dst)));
        writer.writeInt(u32, @intCast(@intFromEnum(self.mask_format)));
        writer.writeInt(i16, self.src_x);
        writer.writeInt(i16, self.src_y);
        for (self.triangles) |elem| {
            try elem.encode(writer);
        }
    }
};

pub const TriStrip = struct {
    pub const opcode: u8 = 12;

    op: PictOp,
    src: Picture,
    dst: Picture,
    mask_format: Pictformat,
    src_x: i16,
    src_y: i16,
    points: []const POINTFIX,
    decoded_points_buf: ?[]POINTFIX = null,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeByte(@intCast(@intFromEnum(self.op)));
        writer.splatByte(0, 3);
        writer.writeInt(u32, @intCast(@intFromEnum(self.src)));
        writer.writeInt(u32, @intCast(@intFromEnum(self.dst)));
        writer.writeInt(u32, @intCast(@intFromEnum(self.mask_format)));
        writer.writeInt(i16, self.src_x);
        writer.writeInt(i16, self.src_y);
        for (self.points) |elem| {
            try elem.encode(writer);
        }
    }
};

pub const TriFan = struct {
    pub const opcode: u8 = 13;

    op: PictOp,
    src: Picture,
    dst: Picture,
    mask_format: Pictformat,
    src_x: i16,
    src_y: i16,
    points: []const POINTFIX,
    decoded_points_buf: ?[]POINTFIX = null,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeByte(@intCast(@intFromEnum(self.op)));
        writer.splatByte(0, 3);
        writer.writeInt(u32, @intCast(@intFromEnum(self.src)));
        writer.writeInt(u32, @intCast(@intFromEnum(self.dst)));
        writer.writeInt(u32, @intCast(@intFromEnum(self.mask_format)));
        writer.writeInt(i16, self.src_x);
        writer.writeInt(i16, self.src_y);
        for (self.points) |elem| {
            try elem.encode(writer);
        }
    }
};

pub const CreateGlyphSet = struct {
    pub const opcode: u8 = 17;

    gsid: Glyphset,
    format: Pictformat,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u32, @intCast(@intFromEnum(self.gsid)));
        writer.writeInt(u32, @intCast(@intFromEnum(self.format)));
    }
};

pub const ReferenceGlyphSet = struct {
    pub const opcode: u8 = 18;

    gsid: Glyphset,
    existing: Glyphset,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u32, @intCast(@intFromEnum(self.gsid)));
        writer.writeInt(u32, @intCast(@intFromEnum(self.existing)));
    }
};

pub const FreeGlyphSet = struct {
    pub const opcode: u8 = 19;

    glyphset: Glyphset,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u32, @intCast(@intFromEnum(self.glyphset)));
    }
};

pub const AddGlyphs = struct {
    pub const opcode: u8 = 20;

    glyphset: Glyphset,
    glyphids: []const u32,
    decoded_glyphids_buf: ?[]u32 = null,
    glyphs: []const GLYPHINFO,
    decoded_glyphs_buf: ?[]GLYPHINFO = null,
    data: []const u8,
    decoded_data_buf: ?[]u8 = null,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u32, @intCast(@intFromEnum(self.glyphset)));
        writer.writeInt(u32, @intCast(self.glyphs.len));
        for (self.glyphids) |elem| {
            writer.writeInt(u32, elem);
        }
        for (self.glyphs) |elem| {
            try elem.encode(writer);
        }
        writer.write(self.data);
    }
};

pub const FreeGlyphs = struct {
    pub const opcode: u8 = 22;

    glyphset: Glyphset,
    glyphs: []const u32,
    decoded_glyphs_buf: ?[]u32 = null,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u32, @intCast(@intFromEnum(self.glyphset)));
        for (self.glyphs) |elem| {
            writer.writeInt(u32, elem);
        }
    }
};

pub const CompositeGlyphs8 = struct {
    pub const opcode: u8 = 23;

    op: PictOp,
    src: Picture,
    dst: Picture,
    mask_format: Pictformat,
    glyphset: Glyphset,
    src_x: i16,
    src_y: i16,
    glyphcmds: []const u8,
    decoded_glyphcmds_buf: ?[]u8 = null,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeByte(@intCast(@intFromEnum(self.op)));
        writer.splatByte(0, 3);
        writer.writeInt(u32, @intCast(@intFromEnum(self.src)));
        writer.writeInt(u32, @intCast(@intFromEnum(self.dst)));
        writer.writeInt(u32, @intCast(@intFromEnum(self.mask_format)));
        writer.writeInt(u32, @intCast(@intFromEnum(self.glyphset)));
        writer.writeInt(i16, self.src_x);
        writer.writeInt(i16, self.src_y);
        writer.write(self.glyphcmds);
    }
};

pub const CompositeGlyphs16 = struct {
    pub const opcode: u8 = 24;

    op: PictOp,
    src: Picture,
    dst: Picture,
    mask_format: Pictformat,
    glyphset: Glyphset,
    src_x: i16,
    src_y: i16,
    glyphcmds: []const u8,
    decoded_glyphcmds_buf: ?[]u8 = null,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeByte(@intCast(@intFromEnum(self.op)));
        writer.splatByte(0, 3);
        writer.writeInt(u32, @intCast(@intFromEnum(self.src)));
        writer.writeInt(u32, @intCast(@intFromEnum(self.dst)));
        writer.writeInt(u32, @intCast(@intFromEnum(self.mask_format)));
        writer.writeInt(u32, @intCast(@intFromEnum(self.glyphset)));
        writer.writeInt(i16, self.src_x);
        writer.writeInt(i16, self.src_y);
        writer.write(self.glyphcmds);
    }
};

pub const CompositeGlyphs32 = struct {
    pub const opcode: u8 = 25;

    op: PictOp,
    src: Picture,
    dst: Picture,
    mask_format: Pictformat,
    glyphset: Glyphset,
    src_x: i16,
    src_y: i16,
    glyphcmds: []const u8,
    decoded_glyphcmds_buf: ?[]u8 = null,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeByte(@intCast(@intFromEnum(self.op)));
        writer.splatByte(0, 3);
        writer.writeInt(u32, @intCast(@intFromEnum(self.src)));
        writer.writeInt(u32, @intCast(@intFromEnum(self.dst)));
        writer.writeInt(u32, @intCast(@intFromEnum(self.mask_format)));
        writer.writeInt(u32, @intCast(@intFromEnum(self.glyphset)));
        writer.writeInt(i16, self.src_x);
        writer.writeInt(i16, self.src_y);
        writer.write(self.glyphcmds);
    }
};

pub const FillRectangles = struct {
    pub const opcode: u8 = 26;

    op: PictOp,
    dst: Picture,
    color: COLOR,
    rects: []const RECTANGLE,
    decoded_rects_buf: ?[]RECTANGLE = null,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeByte(@intCast(@intFromEnum(self.op)));
        writer.splatByte(0, 3);
        writer.writeInt(u32, @intCast(@intFromEnum(self.dst)));
        try self.color.encode(writer);
        for (self.rects) |elem| {
            try elem.encode(writer);
        }
    }
};

pub const CreateCursor = struct {
    pub const opcode: u8 = 27;

    cid: Cursor,
    source: Picture,
    x: u16,
    y: u16,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u32, @intCast(@intFromEnum(self.cid)));
        writer.writeInt(u32, @intCast(@intFromEnum(self.source)));
        writer.writeInt(u16, self.x);
        writer.writeInt(u16, self.y);
    }
};

pub const SetPictureTransform = struct {
    pub const opcode: u8 = 28;

    picture: Picture,
    transform: TRANSFORM,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u32, @intCast(@intFromEnum(self.picture)));
        try self.transform.encode(writer);
    }
};

pub const QueryFilters = struct {
    pub const opcode: u8 = 29;

    drawable: Drawable,

    pub const Reply = struct {
        aliases: []const u16,
        decoded_aliases_buf: ?[]u16 = null,
        filters: []const STR,
        decoded_filters_buf: ?[]STR = null,

        pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader, header_: wire.ReplyHeader) !@This() {
            var result: @This() = undefined;
            _ = header_;
            _ = try reader.take(1);
            const num_aliases = try reader.takeInt(u32, .native);
            const num_filters = try reader.takeInt(u32, .native);
            _ = try reader.take(16);
            const decoded_aliases_buf = try allocator.alloc(u16, @intCast(num_aliases));
            for (decoded_aliases_buf) |*elem| {
                elem.* = try reader.takeInt(u16, .native);
            }
            result.aliases = decoded_aliases_buf;
            result.decoded_aliases_buf = decoded_aliases_buf;
            const decoded_filters_buf = try allocator.alloc(STR, @intCast(num_filters));
            for (decoded_filters_buf) |*elem| {
                elem.* = try STR.decode(allocator, reader);
            }
            result.filters = decoded_filters_buf;
            result.decoded_filters_buf = decoded_filters_buf;
            return result;
        }

        pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
            if (self.decoded_aliases_buf) |buf| {
                allocator.free(buf);
                self.decoded_aliases_buf = null;
                self.aliases = &.{};
            }
            if (self.decoded_filters_buf) |buf| {
                for (buf) |*it| {
                    it.deinit(allocator);
                }
                allocator.free(buf);
                self.decoded_filters_buf = null;
                self.filters = &.{};
            }
        }
    };


    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u32, self.drawable.toInt());
    }
};

pub const SetPictureFilter = struct {
    pub const opcode: u8 = 30;

    picture: Picture,
    filter: []const u8,
    decoded_filter_buf: ?[]u8 = null,
    values: []const i32,
    decoded_values_buf: ?[]i32 = null,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u32, @intCast(@intFromEnum(self.picture)));
        writer.writeInt(u16, @intCast(self.filter.len));
        writer.splatByte(0, 2);
        writer.write(self.filter);
        writer.splatByte(0, wire.pad(writer.seek, 4));
        for (self.values) |elem| {
            writer.writeInt(i32, elem);
        }
    }
};

pub const CreateAnimCursor = struct {
    pub const opcode: u8 = 31;

    cid: Cursor,
    cursors: []const ANIMCURSORELT,
    decoded_cursors_buf: ?[]ANIMCURSORELT = null,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u32, @intCast(@intFromEnum(self.cid)));
        for (self.cursors) |elem| {
            try elem.encode(writer);
        }
    }
};

pub const AddTraps = struct {
    pub const opcode: u8 = 32;

    picture: Picture,
    x_off: i16,
    y_off: i16,
    traps: []const TRAP,
    decoded_traps_buf: ?[]TRAP = null,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u32, @intCast(@intFromEnum(self.picture)));
        writer.writeInt(i16, self.x_off);
        writer.writeInt(i16, self.y_off);
        for (self.traps) |elem| {
            try elem.encode(writer);
        }
    }
};

pub const CreateSolidFill = struct {
    pub const opcode: u8 = 33;

    picture: Picture,
    color: COLOR,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u32, @intCast(@intFromEnum(self.picture)));
        try self.color.encode(writer);
    }
};

pub const CreateLinearGradient = struct {
    pub const opcode: u8 = 34;

    picture: Picture,
    p1: POINTFIX,
    p2: POINTFIX,
    stops: []const i32,
    decoded_stops_buf: ?[]i32 = null,
    colors: []const COLOR,
    decoded_colors_buf: ?[]COLOR = null,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u32, @intCast(@intFromEnum(self.picture)));
        try self.p1.encode(writer);
        try self.p2.encode(writer);
        writer.writeInt(u32, @intCast(self.colors.len));
        for (self.stops) |elem| {
            writer.writeInt(i32, elem);
        }
        for (self.colors) |elem| {
            try elem.encode(writer);
        }
    }
};

pub const CreateRadialGradient = struct {
    pub const opcode: u8 = 35;

    picture: Picture,
    inner: POINTFIX,
    outer: POINTFIX,
    inner_radius: i32,
    outer_radius: i32,
    stops: []const i32,
    decoded_stops_buf: ?[]i32 = null,
    colors: []const COLOR,
    decoded_colors_buf: ?[]COLOR = null,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u32, @intCast(@intFromEnum(self.picture)));
        try self.inner.encode(writer);
        try self.outer.encode(writer);
        writer.writeInt(i32, self.inner_radius);
        writer.writeInt(i32, self.outer_radius);
        writer.writeInt(u32, @intCast(self.colors.len));
        for (self.stops) |elem| {
            writer.writeInt(i32, elem);
        }
        for (self.colors) |elem| {
            try elem.encode(writer);
        }
    }
};

pub const CreateConicalGradient = struct {
    pub const opcode: u8 = 36;

    picture: Picture,
    center: POINTFIX,
    angle: i32,
    stops: []const i32,
    decoded_stops_buf: ?[]i32 = null,
    colors: []const COLOR,
    decoded_colors_buf: ?[]COLOR = null,

    pub fn encode(self: *const @This(), writer: anytype) !void {
        writer.writeInt(u32, @intCast(@intFromEnum(self.picture)));
        try self.center.encode(writer);
        writer.writeInt(i32, self.angle);
        writer.writeInt(u32, @intCast(self.colors.len));
        for (self.stops) |elem| {
            writer.writeInt(i32, elem);
        }
        for (self.colors) |elem| {
            try elem.encode(writer);
        }
    }
};

