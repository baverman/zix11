// zig fmt: off
// This file is generated by tools/genproto.py

const std = @import("std");
const zio = @import("../io.zig");
const wire = @import("../_wire.zig");
const errors = @import("../_errors.zig");
const extensions = @import("../_ext.zig");
const xproto = @import("xproto.zig");
const DecodeError = errors.DecodeError;
const AllocDecodeError = errors.AllocDecodeError;
const BufferDecodeError = errors.BufferDecodeError;

pub const Glyphset = enum(u32) {
    _,
};

pub const Pictformat = enum(u32) {
    _,
};

pub const Picture = enum(u32) {
    None = 0,
    _,
};

pub const Error = enum(u32) {
    RenderPictFormat = 0,
    RenderPicture = 1,
    RenderPictOp = 2,
    RenderGlyphSet = 3,
    RenderGlyph = 4,
    _,
};

pub const CP = enum(u32) {
    Repeat = 1,
    AlphaMap = 2,
    AlphaXOrigin = 4,
    AlphaYOrigin = 8,
    ClipXOrigin = 16,
    ClipYOrigin = 32,
    ClipMask = 64,
    GraphicsExposure = 128,
    SubwindowMode = 256,
    PolyEdge = 512,
    PolyMode = 1024,
    Dither = 2048,
    ComponentAlpha = 4096,
    _,

    pub fn of(flags: []const @This()) u32 {
        return wire.maskOf(@This(), flags);
    }
};

pub const PictOp = enum(u32) {
    Clear = 0,
    Src = 1,
    Dst = 2,
    Over = 3,
    OverReverse = 4,
    In = 5,
    InReverse = 6,
    Out = 7,
    OutReverse = 8,
    Atop = 9,
    AtopReverse = 10,
    Xor = 11,
    Add = 12,
    Saturate = 13,
    DisjointClear = 16,
    DisjointSrc = 17,
    DisjointDst = 18,
    DisjointOver = 19,
    DisjointOverReverse = 20,
    DisjointIn = 21,
    DisjointInReverse = 22,
    DisjointOut = 23,
    DisjointOutReverse = 24,
    DisjointAtop = 25,
    DisjointAtopReverse = 26,
    DisjointXor = 27,
    ConjointClear = 32,
    ConjointSrc = 33,
    ConjointDst = 34,
    ConjointOver = 35,
    ConjointOverReverse = 36,
    ConjointIn = 37,
    ConjointInReverse = 38,
    ConjointOut = 39,
    ConjointOutReverse = 40,
    ConjointAtop = 41,
    ConjointAtopReverse = 42,
    ConjointXor = 43,
    Multiply = 48,
    Screen = 49,
    Overlay = 50,
    Darken = 51,
    Lighten = 52,
    ColorDodge = 53,
    ColorBurn = 54,
    HardLight = 55,
    SoftLight = 56,
    Difference = 57,
    Exclusion = 58,
    HSLHue = 59,
    HSLSaturation = 60,
    HSLColor = 61,
    HSLLuminosity = 62,
    _,
};

pub const PictType = enum(u32) {
    Indexed = 0,
    Direct = 1,
    _,
};

pub const PolyEdge = enum(u32) {
    Sharp = 0,
    Smooth = 1,
    _,
};

pub const PolyMode = enum(u32) {
    Precise = 0,
    Imprecise = 1,
    _,
};

pub const Repeat = enum(u32) {
    None = 0,
    Normal = 1,
    Pad = 2,
    Reflect = 3,
    _,
};

pub const SubPixel = enum(u32) {
    Unknown = 0,
    HorizontalRGB = 1,
    HorizontalBGR = 2,
    VerticalRGB = 3,
    VerticalBGR = 4,
    None = 5,
    _,
};

pub const ANIMCURSORELT = struct {
    cursor: xproto.Cursor,
    delay: u32,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.cursor));
        writer.writeInt(u32, self.delay);
    }

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        const cursor = @as(xproto.Cursor, @enumFromInt(try reader.takeInt(u32, .native)));
        const delay = try reader.takeInt(u32, .native);
        return .{
            .cursor = cursor,
            .delay = delay,
        };
    }

};

pub const COLOR = struct {
    red: u16,
    green: u16,
    blue: u16,
    alpha: u16,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u16, self.red);
        writer.writeInt(u16, self.green);
        writer.writeInt(u16, self.blue);
        writer.writeInt(u16, self.alpha);
    }

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        const red = try reader.takeInt(u16, .native);
        const green = try reader.takeInt(u16, .native);
        const blue = try reader.takeInt(u16, .native);
        const alpha = try reader.takeInt(u16, .native);
        return .{
            .red = red,
            .green = green,
            .blue = blue,
            .alpha = alpha,
        };
    }

};

pub const DIRECTFORMAT = struct {
    red_shift: u16,
    red_mask: u16,
    green_shift: u16,
    green_mask: u16,
    blue_shift: u16,
    blue_mask: u16,
    alpha_shift: u16,
    alpha_mask: u16,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u16, self.red_shift);
        writer.writeInt(u16, self.red_mask);
        writer.writeInt(u16, self.green_shift);
        writer.writeInt(u16, self.green_mask);
        writer.writeInt(u16, self.blue_shift);
        writer.writeInt(u16, self.blue_mask);
        writer.writeInt(u16, self.alpha_shift);
        writer.writeInt(u16, self.alpha_mask);
    }

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        const red_shift = try reader.takeInt(u16, .native);
        const red_mask = try reader.takeInt(u16, .native);
        const green_shift = try reader.takeInt(u16, .native);
        const green_mask = try reader.takeInt(u16, .native);
        const blue_shift = try reader.takeInt(u16, .native);
        const blue_mask = try reader.takeInt(u16, .native);
        const alpha_shift = try reader.takeInt(u16, .native);
        const alpha_mask = try reader.takeInt(u16, .native);
        return .{
            .red_shift = red_shift,
            .red_mask = red_mask,
            .green_shift = green_shift,
            .green_mask = green_mask,
            .blue_shift = blue_shift,
            .blue_mask = blue_mask,
            .alpha_shift = alpha_shift,
            .alpha_mask = alpha_mask,
        };
    }

};

pub const GLYPHINFO = struct {
    width: u16,
    height: u16,
    x: i16,
    y: i16,
    x_off: i16,
    y_off: i16,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u16, self.width);
        writer.writeInt(u16, self.height);
        writer.writeInt(i16, self.x);
        writer.writeInt(i16, self.y);
        writer.writeInt(i16, self.x_off);
        writer.writeInt(i16, self.y_off);
    }

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        const width = try reader.takeInt(u16, .native);
        const height = try reader.takeInt(u16, .native);
        const x = try reader.takeInt(i16, .native);
        const y = try reader.takeInt(i16, .native);
        const x_off = try reader.takeInt(i16, .native);
        const y_off = try reader.takeInt(i16, .native);
        return .{
            .width = width,
            .height = height,
            .x = x,
            .y = y,
            .x_off = x_off,
            .y_off = y_off,
        };
    }

};

pub const INDEXVALUE = struct {
    pixel: u32,
    red: u16,
    green: u16,
    blue: u16,
    alpha: u16,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, self.pixel);
        writer.writeInt(u16, self.red);
        writer.writeInt(u16, self.green);
        writer.writeInt(u16, self.blue);
        writer.writeInt(u16, self.alpha);
    }

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        const pixel = try reader.takeInt(u32, .native);
        const red = try reader.takeInt(u16, .native);
        const green = try reader.takeInt(u16, .native);
        const blue = try reader.takeInt(u16, .native);
        const alpha = try reader.takeInt(u16, .native);
        return .{
            .pixel = pixel,
            .red = red,
            .green = green,
            .blue = blue,
            .alpha = alpha,
        };
    }

};

pub const LINEFIX = struct {
    p1: POINTFIX,
    p2: POINTFIX,

    pub fn encode(self: @This(), writer: anytype) void {
        self.p1.encode(writer);
        self.p2.encode(writer);
    }

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        const p1 = try POINTFIX.decode(reader);
        const p2 = try POINTFIX.decode(reader);
        return .{
            .p1 = p1,
            .p2 = p2,
        };
    }

};

pub const PICTDEPTH = struct {
    depth: u8,
    visuals: []PICTVISUAL,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeByte(self.depth);
        writer.splatByte(0, 1);
        writer.writeInt(u16, @intCast(self.visuals.len));
        writer.splatByte(0, 4);
        for (self.visuals) |elem| {
            elem.encode(writer);
        }
    }

    pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader) AllocDecodeError!@This() {
        const depth = try reader.takeByte();
        _ = try reader.take(1);
        const num_visuals = try reader.takeInt(u16, .native);
        _ = try reader.take(4);
        const visuals = try allocator.alloc(PICTVISUAL, @as(usize, num_visuals));
        errdefer allocator.free(visuals);
        var visuals_decoded: usize = 0;
        for (visuals) |*elem| {
            const elem_value = try PICTVISUAL.decode(reader);
            elem.* = elem_value;
            visuals_decoded += 1;
        }
        return .{
            .depth = depth,
            .visuals = visuals,
        };
    }

    pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
        allocator.free(self.visuals);
    }

};

pub const PICTFORMINFO = struct {
    id: Pictformat,
    type: PictType,
    depth: u8,
    direct: DIRECTFORMAT,
    colormap: xproto.Colormap,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.id));
        writer.writeByte(@intCast(@intFromEnum(self.type)));
        writer.writeByte(self.depth);
        writer.splatByte(0, 2);
        self.direct.encode(writer);
        writer.writeInt(u32, @intFromEnum(self.colormap));
    }

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        const id = @as(Pictformat, @enumFromInt(try reader.takeInt(u32, .native)));
        const @"type" = @as(PictType, @enumFromInt(try reader.takeInt(u8, .native)));
        const depth = try reader.takeByte();
        _ = try reader.take(2);
        const direct = try DIRECTFORMAT.decode(reader);
        const colormap = @as(xproto.Colormap, @enumFromInt(try reader.takeInt(u32, .native)));
        return .{
            .id = id,
            .type = @"type",
            .depth = depth,
            .direct = direct,
            .colormap = colormap,
        };
    }

};

pub const PICTSCREEN = struct {
    fallback: Pictformat,
    depths: []PICTDEPTH,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intCast(self.depths.len));
        writer.writeInt(u32, @intFromEnum(self.fallback));
        for (self.depths) |elem| {
            elem.encode(writer);
        }
    }

    pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader) AllocDecodeError!@This() {
        const num_depths = try reader.takeInt(u32, .native);
        const fallback = @as(Pictformat, @enumFromInt(try reader.takeInt(u32, .native)));
        const depths = try allocator.alloc(PICTDEPTH, @as(usize, num_depths));
        errdefer allocator.free(depths);
        var depths_decoded: usize = 0;
        errdefer for (depths[0..depths_decoded]) |*elem| elem.deinit(allocator);
        for (depths) |*elem| {
            const elem_value = try PICTDEPTH.decode(allocator, reader);
            elem.* = elem_value;
            depths_decoded += 1;
        }
        return .{
            .fallback = fallback,
            .depths = depths,
        };
    }

    pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
        for (self.depths) |*elem| elem.deinit(allocator);
        allocator.free(self.depths);
    }

};

pub const PICTVISUAL = struct {
    visual: u32,
    format: Pictformat,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, self.visual);
        writer.writeInt(u32, @intFromEnum(self.format));
    }

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        const visual = try reader.takeInt(u32, .native);
        const format = @as(Pictformat, @enumFromInt(try reader.takeInt(u32, .native)));
        return .{
            .visual = visual,
            .format = format,
        };
    }

};

pub const POINTFIX = struct {
    x: i32,
    y: i32,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(i32, self.x);
        writer.writeInt(i32, self.y);
    }

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        const x = try reader.takeInt(i32, .native);
        const y = try reader.takeInt(i32, .native);
        return .{
            .x = x,
            .y = y,
        };
    }

};

pub const SPANFIX = struct {
    l: i32,
    r: i32,
    y: i32,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(i32, self.l);
        writer.writeInt(i32, self.r);
        writer.writeInt(i32, self.y);
    }

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        const l = try reader.takeInt(i32, .native);
        const r = try reader.takeInt(i32, .native);
        const y = try reader.takeInt(i32, .native);
        return .{
            .l = l,
            .r = r,
            .y = y,
        };
    }

};

pub const TRANSFORM = struct {
    matrix11: i32,
    matrix12: i32,
    matrix13: i32,
    matrix21: i32,
    matrix22: i32,
    matrix23: i32,
    matrix31: i32,
    matrix32: i32,
    matrix33: i32,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(i32, self.matrix11);
        writer.writeInt(i32, self.matrix12);
        writer.writeInt(i32, self.matrix13);
        writer.writeInt(i32, self.matrix21);
        writer.writeInt(i32, self.matrix22);
        writer.writeInt(i32, self.matrix23);
        writer.writeInt(i32, self.matrix31);
        writer.writeInt(i32, self.matrix32);
        writer.writeInt(i32, self.matrix33);
    }

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        const matrix11 = try reader.takeInt(i32, .native);
        const matrix12 = try reader.takeInt(i32, .native);
        const matrix13 = try reader.takeInt(i32, .native);
        const matrix21 = try reader.takeInt(i32, .native);
        const matrix22 = try reader.takeInt(i32, .native);
        const matrix23 = try reader.takeInt(i32, .native);
        const matrix31 = try reader.takeInt(i32, .native);
        const matrix32 = try reader.takeInt(i32, .native);
        const matrix33 = try reader.takeInt(i32, .native);
        return .{
            .matrix11 = matrix11,
            .matrix12 = matrix12,
            .matrix13 = matrix13,
            .matrix21 = matrix21,
            .matrix22 = matrix22,
            .matrix23 = matrix23,
            .matrix31 = matrix31,
            .matrix32 = matrix32,
            .matrix33 = matrix33,
        };
    }

};

pub const TRAP = struct {
    top: SPANFIX,
    bot: SPANFIX,

    pub fn encode(self: @This(), writer: anytype) void {
        self.top.encode(writer);
        self.bot.encode(writer);
    }

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        const top = try SPANFIX.decode(reader);
        const bot = try SPANFIX.decode(reader);
        return .{
            .top = top,
            .bot = bot,
        };
    }

};

pub const TRAPEZOID = struct {
    top: i32,
    bottom: i32,
    left: LINEFIX,
    right: LINEFIX,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(i32, self.top);
        writer.writeInt(i32, self.bottom);
        self.left.encode(writer);
        self.right.encode(writer);
    }

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        const top = try reader.takeInt(i32, .native);
        const bottom = try reader.takeInt(i32, .native);
        const left = try LINEFIX.decode(reader);
        const right = try LINEFIX.decode(reader);
        return .{
            .top = top,
            .bottom = bottom,
            .left = left,
            .right = right,
        };
    }

};

pub const TRIANGLE = struct {
    p1: POINTFIX,
    p2: POINTFIX,
    p3: POINTFIX,

    pub fn encode(self: @This(), writer: anytype) void {
        self.p1.encode(writer);
        self.p2.encode(writer);
        self.p3.encode(writer);
    }

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        const p1 = try POINTFIX.decode(reader);
        const p2 = try POINTFIX.decode(reader);
        const p3 = try POINTFIX.decode(reader);
        return .{
            .p1 = p1,
            .p2 = p2,
            .p3 = p3,
        };
    }

};

pub const AddGlyphs = struct {
    pub const opcode: u8 = 20;
    pub const extension: ?extensions.Extension = .RENDER;
    pub const Reply = void;

    glyphset: Glyphset,
    glyphids: []const u32,
    glyphs: []const GLYPHINFO,
    data: []const u8,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.glyphset));
        writer.writeInt(u32, @intCast(self.glyphs.len));
        for (self.glyphids) |elem| {
            writer.writeInt(u32, elem);
        }
        for (self.glyphs) |elem| {
            elem.encode(writer);
        }
        writer.write(self.data);
    }

};

pub const AddTraps = struct {
    pub const opcode: u8 = 32;
    pub const extension: ?extensions.Extension = .RENDER;
    pub const Reply = void;

    picture: Picture,
    x_off: i16,
    y_off: i16,
    traps: []const TRAP,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.picture));
        writer.writeInt(i16, self.x_off);
        writer.writeInt(i16, self.y_off);
        for (self.traps) |elem| {
            elem.encode(writer);
        }
    }

};

pub const ChangePictureValueList = struct {
    repeat: ?Repeat = null,
    alphamap: ?Picture = null,
    alphaxorigin: ?i32 = null,
    alphayorigin: ?i32 = null,
    clipxorigin: ?i32 = null,
    clipyorigin: ?i32 = null,
    clipmask: ?xproto.Pixmap = null,
    graphicsexposure: ?u32 = null,
    subwindowmode: ?xproto.SubwindowMode = null,
    polyedge: ?PolyEdge = null,
    polymode: ?PolyMode = null,
    dither: ?xproto.Atom = null,
    componentalpha: ?u32 = null,
};

pub const ChangePictureValueListSpec = struct {
    pub const mask_type = u32;
    pub const fields = .{
        .{ .name = "repeat", .bit = @intFromEnum(CP.Repeat), .value_type = Repeat },
        .{ .name = "alphamap", .bit = @intFromEnum(CP.AlphaMap), .value_type = Picture },
        .{ .name = "alphaxorigin", .bit = @intFromEnum(CP.AlphaXOrigin), .value_type = i32 },
        .{ .name = "alphayorigin", .bit = @intFromEnum(CP.AlphaYOrigin), .value_type = i32 },
        .{ .name = "clipxorigin", .bit = @intFromEnum(CP.ClipXOrigin), .value_type = i32 },
        .{ .name = "clipyorigin", .bit = @intFromEnum(CP.ClipYOrigin), .value_type = i32 },
        .{ .name = "clipmask", .bit = @intFromEnum(CP.ClipMask), .value_type = xproto.Pixmap },
        .{ .name = "graphicsexposure", .bit = @intFromEnum(CP.GraphicsExposure), .value_type = u32 },
        .{ .name = "subwindowmode", .bit = @intFromEnum(CP.SubwindowMode), .value_type = xproto.SubwindowMode },
        .{ .name = "polyedge", .bit = @intFromEnum(CP.PolyEdge), .value_type = PolyEdge },
        .{ .name = "polymode", .bit = @intFromEnum(CP.PolyMode), .value_type = PolyMode },
        .{ .name = "dither", .bit = @intFromEnum(CP.Dither), .value_type = xproto.Atom },
        .{ .name = "componentalpha", .bit = @intFromEnum(CP.ComponentAlpha), .value_type = u32 },
    };
};

pub const ChangePicture = struct {
    pub const opcode: u8 = 5;
    pub const extension: ?extensions.Extension = .RENDER;
    pub const Reply = void;

    picture: Picture,
    value_list: ChangePictureValueList,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.picture));
        writer.writeInt(u32, wire.computeValueMask(ChangePictureValueListSpec, self.value_list));
        wire.writeValueList(ChangePictureValueListSpec, self.value_list, writer);
    }

};

pub const Composite = struct {
    pub const opcode: u8 = 8;
    pub const extension: ?extensions.Extension = .RENDER;
    pub const Reply = void;

    op: PictOp,
    src: Picture,
    mask: Picture,
    dst: Picture,
    src_x: i16,
    src_y: i16,
    mask_x: i16,
    mask_y: i16,
    dst_x: i16,
    dst_y: i16,
    width: u16,
    height: u16,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeByte(@intCast(@intFromEnum(self.op)));
        writer.splatByte(0, 3);
        writer.writeInt(u32, @intFromEnum(self.src));
        writer.writeInt(u32, @intFromEnum(self.mask));
        writer.writeInt(u32, @intFromEnum(self.dst));
        writer.writeInt(i16, self.src_x);
        writer.writeInt(i16, self.src_y);
        writer.writeInt(i16, self.mask_x);
        writer.writeInt(i16, self.mask_y);
        writer.writeInt(i16, self.dst_x);
        writer.writeInt(i16, self.dst_y);
        writer.writeInt(u16, self.width);
        writer.writeInt(u16, self.height);
    }

};

pub const CompositeGlyphs16 = struct {
    pub const opcode: u8 = 24;
    pub const extension: ?extensions.Extension = .RENDER;
    pub const Reply = void;

    op: PictOp,
    src: Picture,
    dst: Picture,
    mask_format: Pictformat,
    glyphset: Glyphset,
    src_x: i16,
    src_y: i16,
    glyphcmds: []const u8,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeByte(@intCast(@intFromEnum(self.op)));
        writer.splatByte(0, 3);
        writer.writeInt(u32, @intFromEnum(self.src));
        writer.writeInt(u32, @intFromEnum(self.dst));
        writer.writeInt(u32, @intFromEnum(self.mask_format));
        writer.writeInt(u32, @intFromEnum(self.glyphset));
        writer.writeInt(i16, self.src_x);
        writer.writeInt(i16, self.src_y);
        writer.write(self.glyphcmds);
    }

};

pub const CompositeGlyphs32 = struct {
    pub const opcode: u8 = 25;
    pub const extension: ?extensions.Extension = .RENDER;
    pub const Reply = void;

    op: PictOp,
    src: Picture,
    dst: Picture,
    mask_format: Pictformat,
    glyphset: Glyphset,
    src_x: i16,
    src_y: i16,
    glyphcmds: []const u8,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeByte(@intCast(@intFromEnum(self.op)));
        writer.splatByte(0, 3);
        writer.writeInt(u32, @intFromEnum(self.src));
        writer.writeInt(u32, @intFromEnum(self.dst));
        writer.writeInt(u32, @intFromEnum(self.mask_format));
        writer.writeInt(u32, @intFromEnum(self.glyphset));
        writer.writeInt(i16, self.src_x);
        writer.writeInt(i16, self.src_y);
        writer.write(self.glyphcmds);
    }

};

pub const CompositeGlyphs8 = struct {
    pub const opcode: u8 = 23;
    pub const extension: ?extensions.Extension = .RENDER;
    pub const Reply = void;

    op: PictOp,
    src: Picture,
    dst: Picture,
    mask_format: Pictformat,
    glyphset: Glyphset,
    src_x: i16,
    src_y: i16,
    glyphcmds: []const u8,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeByte(@intCast(@intFromEnum(self.op)));
        writer.splatByte(0, 3);
        writer.writeInt(u32, @intFromEnum(self.src));
        writer.writeInt(u32, @intFromEnum(self.dst));
        writer.writeInt(u32, @intFromEnum(self.mask_format));
        writer.writeInt(u32, @intFromEnum(self.glyphset));
        writer.writeInt(i16, self.src_x);
        writer.writeInt(i16, self.src_y);
        writer.write(self.glyphcmds);
    }

};

pub const CreateAnimCursor = struct {
    pub const opcode: u8 = 31;
    pub const extension: ?extensions.Extension = .RENDER;
    pub const Reply = void;

    cid: xproto.Cursor,
    cursors: []const ANIMCURSORELT,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.cid));
        for (self.cursors) |elem| {
            elem.encode(writer);
        }
    }

};

pub const CreateConicalGradient = struct {
    pub const opcode: u8 = 36;
    pub const extension: ?extensions.Extension = .RENDER;
    pub const Reply = void;

    picture: Picture,
    center: POINTFIX,
    angle: i32,
    stops: []const i32,
    colors: []const COLOR,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.picture));
        self.center.encode(writer);
        writer.writeInt(i32, self.angle);
        writer.writeInt(u32, @intCast(self.colors.len));
        for (self.stops) |elem| {
            writer.writeInt(i32, elem);
        }
        for (self.colors) |elem| {
            elem.encode(writer);
        }
    }

};

pub const CreateCursor = struct {
    pub const opcode: u8 = 27;
    pub const extension: ?extensions.Extension = .RENDER;
    pub const Reply = void;

    cid: xproto.Cursor,
    source: Picture,
    x: u16,
    y: u16,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.cid));
        writer.writeInt(u32, @intFromEnum(self.source));
        writer.writeInt(u16, self.x);
        writer.writeInt(u16, self.y);
    }

};

pub const CreateGlyphSet = struct {
    pub const opcode: u8 = 17;
    pub const extension: ?extensions.Extension = .RENDER;
    pub const Reply = void;

    gsid: Glyphset,
    format: Pictformat,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.gsid));
        writer.writeInt(u32, @intFromEnum(self.format));
    }

};

pub const CreateLinearGradient = struct {
    pub const opcode: u8 = 34;
    pub const extension: ?extensions.Extension = .RENDER;
    pub const Reply = void;

    picture: Picture,
    p1: POINTFIX,
    p2: POINTFIX,
    stops: []const i32,
    colors: []const COLOR,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.picture));
        self.p1.encode(writer);
        self.p2.encode(writer);
        writer.writeInt(u32, @intCast(self.colors.len));
        for (self.stops) |elem| {
            writer.writeInt(i32, elem);
        }
        for (self.colors) |elem| {
            elem.encode(writer);
        }
    }

};

pub const CreatePictureValueList = struct {
    repeat: ?Repeat = null,
    alphamap: ?Picture = null,
    alphaxorigin: ?i32 = null,
    alphayorigin: ?i32 = null,
    clipxorigin: ?i32 = null,
    clipyorigin: ?i32 = null,
    clipmask: ?xproto.Pixmap = null,
    graphicsexposure: ?u32 = null,
    subwindowmode: ?xproto.SubwindowMode = null,
    polyedge: ?PolyEdge = null,
    polymode: ?PolyMode = null,
    dither: ?xproto.Atom = null,
    componentalpha: ?u32 = null,
};

pub const CreatePictureValueListSpec = struct {
    pub const mask_type = u32;
    pub const fields = .{
        .{ .name = "repeat", .bit = @intFromEnum(CP.Repeat), .value_type = Repeat },
        .{ .name = "alphamap", .bit = @intFromEnum(CP.AlphaMap), .value_type = Picture },
        .{ .name = "alphaxorigin", .bit = @intFromEnum(CP.AlphaXOrigin), .value_type = i32 },
        .{ .name = "alphayorigin", .bit = @intFromEnum(CP.AlphaYOrigin), .value_type = i32 },
        .{ .name = "clipxorigin", .bit = @intFromEnum(CP.ClipXOrigin), .value_type = i32 },
        .{ .name = "clipyorigin", .bit = @intFromEnum(CP.ClipYOrigin), .value_type = i32 },
        .{ .name = "clipmask", .bit = @intFromEnum(CP.ClipMask), .value_type = xproto.Pixmap },
        .{ .name = "graphicsexposure", .bit = @intFromEnum(CP.GraphicsExposure), .value_type = u32 },
        .{ .name = "subwindowmode", .bit = @intFromEnum(CP.SubwindowMode), .value_type = xproto.SubwindowMode },
        .{ .name = "polyedge", .bit = @intFromEnum(CP.PolyEdge), .value_type = PolyEdge },
        .{ .name = "polymode", .bit = @intFromEnum(CP.PolyMode), .value_type = PolyMode },
        .{ .name = "dither", .bit = @intFromEnum(CP.Dither), .value_type = xproto.Atom },
        .{ .name = "componentalpha", .bit = @intFromEnum(CP.ComponentAlpha), .value_type = u32 },
    };
};

pub const CreatePicture = struct {
    pub const opcode: u8 = 4;
    pub const extension: ?extensions.Extension = .RENDER;
    pub const Reply = void;

    pid: Picture,
    drawable: xproto.Drawable,
    format: Pictformat,
    value_list: CreatePictureValueList,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.pid));
        self.drawable.encode(writer);
        writer.writeInt(u32, @intFromEnum(self.format));
        writer.writeInt(u32, wire.computeValueMask(CreatePictureValueListSpec, self.value_list));
        wire.writeValueList(CreatePictureValueListSpec, self.value_list, writer);
    }

};

pub const CreateRadialGradient = struct {
    pub const opcode: u8 = 35;
    pub const extension: ?extensions.Extension = .RENDER;
    pub const Reply = void;

    picture: Picture,
    inner: POINTFIX,
    outer: POINTFIX,
    inner_radius: i32,
    outer_radius: i32,
    stops: []const i32,
    colors: []const COLOR,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.picture));
        self.inner.encode(writer);
        self.outer.encode(writer);
        writer.writeInt(i32, self.inner_radius);
        writer.writeInt(i32, self.outer_radius);
        writer.writeInt(u32, @intCast(self.colors.len));
        for (self.stops) |elem| {
            writer.writeInt(i32, elem);
        }
        for (self.colors) |elem| {
            elem.encode(writer);
        }
    }

};

pub const CreateSolidFill = struct {
    pub const opcode: u8 = 33;
    pub const extension: ?extensions.Extension = .RENDER;
    pub const Reply = void;

    picture: Picture,
    color: COLOR,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.picture));
        self.color.encode(writer);
    }

};

pub const FillRectangles = struct {
    pub const opcode: u8 = 26;
    pub const extension: ?extensions.Extension = .RENDER;
    pub const Reply = void;

    op: PictOp,
    dst: Picture,
    color: COLOR,
    rects: []const xproto.RECTANGLE,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeByte(@intCast(@intFromEnum(self.op)));
        writer.splatByte(0, 3);
        writer.writeInt(u32, @intFromEnum(self.dst));
        self.color.encode(writer);
        for (self.rects) |elem| {
            elem.encode(writer);
        }
    }

};

pub const FreeGlyphSet = struct {
    pub const opcode: u8 = 19;
    pub const extension: ?extensions.Extension = .RENDER;
    pub const Reply = void;

    glyphset: Glyphset,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.glyphset));
    }

};

pub const FreeGlyphs = struct {
    pub const opcode: u8 = 22;
    pub const extension: ?extensions.Extension = .RENDER;
    pub const Reply = void;

    glyphset: Glyphset,
    glyphs: []const u32,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.glyphset));
        for (self.glyphs) |elem| {
            writer.writeInt(u32, elem);
        }
    }

};

pub const FreePicture = struct {
    pub const opcode: u8 = 7;
    pub const extension: ?extensions.Extension = .RENDER;
    pub const Reply = void;

    picture: Picture,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.picture));
    }

};

pub const QueryFiltersReply = struct {
    aliases: []const u16,
    filters: []xproto.STR,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.splatByte(0, 1);
        writer.writeInt(u32, @intCast(self.aliases.len));
        writer.writeInt(u32, @intCast(self.filters.len));
        writer.splatByte(0, 16);
        for (self.aliases) |elem| {
            writer.writeInt(u16, elem);
        }
        for (self.filters) |elem| {
            elem.encode(writer);
        }
    }

    pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader) AllocDecodeError!@This() {
        _ = try reader.takeByte();
        _ = try reader.take(1);
        _ = try reader.takeInt(u16, .native);
        _ = try reader.takeInt(u32, .native);
        const num_aliases = try reader.takeInt(u32, .native);
        const num_filters = try reader.takeInt(u32, .native);
        _ = try reader.take(16);
        const aliases = try allocator.alloc(u16, @as(usize, num_aliases));
        errdefer allocator.free(aliases);
        var aliases_decoded: usize = 0;
        for (aliases) |*elem| {
            const elem_value = try reader.takeInt(u16, .native);
            elem.* = elem_value;
            aliases_decoded += 1;
        }
        const filters = try allocator.alloc(xproto.STR, @as(usize, num_filters));
        errdefer allocator.free(filters);
        var filters_decoded: usize = 0;
        errdefer for (filters[0..filters_decoded]) |*elem| elem.deinit(allocator);
        for (filters) |*elem| {
            const elem_value = try xproto.STR.decode(allocator, reader);
            elem.* = elem_value;
            filters_decoded += 1;
        }
        return .{
            .aliases = aliases,
            .filters = filters,
        };
    }

    pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
        allocator.free(self.aliases);
        for (self.filters) |*elem| elem.deinit(allocator);
        allocator.free(self.filters);
    }

};

pub const QueryFilters = struct {
    pub const opcode: u8 = 29;
    pub const extension: ?extensions.Extension = .RENDER;
    pub const Reply = QueryFiltersReply;

    drawable: xproto.Drawable,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        self.drawable.encode(writer);
    }

};

pub const QueryPictFormatsReply = struct {
    num_depths: u32,
    num_visuals: u32,
    formats: []PICTFORMINFO,
    screens: []PICTSCREEN,
    subpixels: []const SubPixel,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.splatByte(0, 1);
        writer.writeInt(u32, @intCast(self.formats.len));
        writer.writeInt(u32, @intCast(self.screens.len));
        writer.writeInt(u32, self.num_depths);
        writer.writeInt(u32, self.num_visuals);
        writer.writeInt(u32, @intCast(self.subpixels.len));
        writer.splatByte(0, 4);
        for (self.formats) |elem| {
            elem.encode(writer);
        }
        for (self.screens) |elem| {
            elem.encode(writer);
        }
        for (self.subpixels) |elem| {
            writer.writeInt(u32, @intCast(@intFromEnum(elem)));
        }
    }

    pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader) AllocDecodeError!@This() {
        _ = try reader.takeByte();
        _ = try reader.take(1);
        _ = try reader.takeInt(u16, .native);
        _ = try reader.takeInt(u32, .native);
        const num_formats = try reader.takeInt(u32, .native);
        const num_screens = try reader.takeInt(u32, .native);
        const num_depths = try reader.takeInt(u32, .native);
        const num_visuals = try reader.takeInt(u32, .native);
        const num_subpixel = try reader.takeInt(u32, .native);
        _ = try reader.take(4);
        const formats = try allocator.alloc(PICTFORMINFO, @as(usize, num_formats));
        errdefer allocator.free(formats);
        var formats_decoded: usize = 0;
        for (formats) |*elem| {
            const elem_value = try PICTFORMINFO.decode(reader);
            elem.* = elem_value;
            formats_decoded += 1;
        }
        const screens = try allocator.alloc(PICTSCREEN, @as(usize, num_screens));
        errdefer allocator.free(screens);
        var screens_decoded: usize = 0;
        errdefer for (screens[0..screens_decoded]) |*elem| elem.deinit(allocator);
        for (screens) |*elem| {
            const elem_value = try PICTSCREEN.decode(allocator, reader);
            elem.* = elem_value;
            screens_decoded += 1;
        }
        const subpixels = try allocator.alloc(SubPixel, @as(usize, num_subpixel));
        errdefer allocator.free(subpixels);
        var subpixels_decoded: usize = 0;
        for (subpixels) |*elem| {
            const elem_value = @as(SubPixel, @enumFromInt(try reader.takeInt(u32, .native)));
            elem.* = elem_value;
            subpixels_decoded += 1;
        }
        return .{
            .num_depths = num_depths,
            .num_visuals = num_visuals,
            .formats = formats,
            .screens = screens,
            .subpixels = subpixels,
        };
    }

    pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
        allocator.free(self.formats);
        for (self.screens) |*elem| elem.deinit(allocator);
        allocator.free(self.screens);
        allocator.free(self.subpixels);
    }

};

pub const QueryPictFormats = struct {
    pub const opcode: u8 = 1;
    pub const extension: ?extensions.Extension = .RENDER;
    pub const Reply = QueryPictFormatsReply;


    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        _ = self;
        _ = writer;
    }

};

pub const QueryPictIndexValuesReply = struct {
    values: []INDEXVALUE,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.splatByte(0, 1);
        writer.writeInt(u32, @intCast(self.values.len));
        writer.splatByte(0, 20);
        for (self.values) |elem| {
            elem.encode(writer);
        }
    }

    pub fn decode(allocator: std.mem.Allocator, reader: *std.Io.Reader) AllocDecodeError!@This() {
        _ = try reader.takeByte();
        _ = try reader.take(1);
        _ = try reader.takeInt(u16, .native);
        _ = try reader.takeInt(u32, .native);
        const num_values = try reader.takeInt(u32, .native);
        _ = try reader.take(20);
        const values = try allocator.alloc(INDEXVALUE, @as(usize, num_values));
        errdefer allocator.free(values);
        var values_decoded: usize = 0;
        for (values) |*elem| {
            const elem_value = try INDEXVALUE.decode(reader);
            elem.* = elem_value;
            values_decoded += 1;
        }
        return .{
            .values = values,
        };
    }

    pub fn deinit(self: *@This(), allocator: std.mem.Allocator) void {
        allocator.free(self.values);
    }

};

pub const QueryPictIndexValues = struct {
    pub const opcode: u8 = 2;
    pub const extension: ?extensions.Extension = .RENDER;
    pub const Reply = QueryPictIndexValuesReply;

    format: Pictformat,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.format));
    }

};

pub const QueryVersionReply = struct {
    major_version: u32,
    minor_version: u32,

    pub fn encode(self: @This(), writer: anytype) void {
        writer.splatByte(0, 1);
        writer.writeInt(u32, self.major_version);
        writer.writeInt(u32, self.minor_version);
        writer.splatByte(0, 16);
    }

    pub fn decode(reader: *std.Io.Reader) DecodeError!@This() {
        _ = try reader.takeByte();
        _ = try reader.take(1);
        _ = try reader.takeInt(u16, .native);
        _ = try reader.takeInt(u32, .native);
        const major_version = try reader.takeInt(u32, .native);
        const minor_version = try reader.takeInt(u32, .native);
        _ = try reader.take(16);
        return .{
            .major_version = major_version,
            .minor_version = minor_version,
        };
    }

};

pub const QueryVersion = struct {
    pub const opcode: u8 = 0;
    pub const extension: ?extensions.Extension = .RENDER;
    pub const Reply = QueryVersionReply;

    client_major_version: u32,
    client_minor_version: u32,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, self.client_major_version);
        writer.writeInt(u32, self.client_minor_version);
    }

};

pub const ReferenceGlyphSet = struct {
    pub const opcode: u8 = 18;
    pub const extension: ?extensions.Extension = .RENDER;
    pub const Reply = void;

    gsid: Glyphset,
    existing: Glyphset,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.gsid));
        writer.writeInt(u32, @intFromEnum(self.existing));
    }

};

pub const SetPictureClipRectangles = struct {
    pub const opcode: u8 = 6;
    pub const extension: ?extensions.Extension = .RENDER;
    pub const Reply = void;

    picture: Picture,
    clip_x_origin: i16,
    clip_y_origin: i16,
    rectangles: []const xproto.RECTANGLE,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.picture));
        writer.writeInt(i16, self.clip_x_origin);
        writer.writeInt(i16, self.clip_y_origin);
        for (self.rectangles) |elem| {
            elem.encode(writer);
        }
    }

};

pub const SetPictureFilter = struct {
    pub const opcode: u8 = 30;
    pub const extension: ?extensions.Extension = .RENDER;
    pub const Reply = void;

    picture: Picture,
    filter: []const u8,
    values: []const i32,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.picture));
        writer.writeInt(u16, @intCast(self.filter.len));
        writer.splatByte(0, 2);
        writer.write(self.filter);
        writer.splatByte(0, wire.pad4(self.filter.len));
        for (self.values) |elem| {
            writer.writeInt(i32, elem);
        }
    }

};

pub const SetPictureTransform = struct {
    pub const opcode: u8 = 28;
    pub const extension: ?extensions.Extension = .RENDER;
    pub const Reply = void;

    picture: Picture,
    transform: TRANSFORM,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeInt(u32, @intFromEnum(self.picture));
        self.transform.encode(writer);
    }

};

pub const Trapezoids = struct {
    pub const opcode: u8 = 10;
    pub const extension: ?extensions.Extension = .RENDER;
    pub const Reply = void;

    op: PictOp,
    src: Picture,
    dst: Picture,
    mask_format: Pictformat,
    src_x: i16,
    src_y: i16,
    traps: []const TRAPEZOID,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeByte(@intCast(@intFromEnum(self.op)));
        writer.splatByte(0, 3);
        writer.writeInt(u32, @intFromEnum(self.src));
        writer.writeInt(u32, @intFromEnum(self.dst));
        writer.writeInt(u32, @intFromEnum(self.mask_format));
        writer.writeInt(i16, self.src_x);
        writer.writeInt(i16, self.src_y);
        for (self.traps) |elem| {
            elem.encode(writer);
        }
    }

};

pub const TriFan = struct {
    pub const opcode: u8 = 13;
    pub const extension: ?extensions.Extension = .RENDER;
    pub const Reply = void;

    op: PictOp,
    src: Picture,
    dst: Picture,
    mask_format: Pictformat,
    src_x: i16,
    src_y: i16,
    points: []const POINTFIX,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeByte(@intCast(@intFromEnum(self.op)));
        writer.splatByte(0, 3);
        writer.writeInt(u32, @intFromEnum(self.src));
        writer.writeInt(u32, @intFromEnum(self.dst));
        writer.writeInt(u32, @intFromEnum(self.mask_format));
        writer.writeInt(i16, self.src_x);
        writer.writeInt(i16, self.src_y);
        for (self.points) |elem| {
            elem.encode(writer);
        }
    }

};

pub const TriStrip = struct {
    pub const opcode: u8 = 12;
    pub const extension: ?extensions.Extension = .RENDER;
    pub const Reply = void;

    op: PictOp,
    src: Picture,
    dst: Picture,
    mask_format: Pictformat,
    src_x: i16,
    src_y: i16,
    points: []const POINTFIX,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeByte(@intCast(@intFromEnum(self.op)));
        writer.splatByte(0, 3);
        writer.writeInt(u32, @intFromEnum(self.src));
        writer.writeInt(u32, @intFromEnum(self.dst));
        writer.writeInt(u32, @intFromEnum(self.mask_format));
        writer.writeInt(i16, self.src_x);
        writer.writeInt(i16, self.src_y);
        for (self.points) |elem| {
            elem.encode(writer);
        }
    }

};

pub const Triangles = struct {
    pub const opcode: u8 = 11;
    pub const extension: ?extensions.Extension = .RENDER;
    pub const Reply = void;

    op: PictOp,
    src: Picture,
    dst: Picture,
    mask_format: Pictformat,
    src_x: i16,
    src_y: i16,
    triangles: []const TRIANGLE,

    pub fn headerByte1(self: @This()) u8 {
        _ = self;
        return 0;
    }

    pub fn encode(self: @This(), writer: anytype) void {
        writer.writeByte(@intCast(@intFromEnum(self.op)));
        writer.splatByte(0, 3);
        writer.writeInt(u32, @intFromEnum(self.src));
        writer.writeInt(u32, @intFromEnum(self.dst));
        writer.writeInt(u32, @intFromEnum(self.mask_format));
        writer.writeInt(i16, self.src_x);
        writer.writeInt(i16, self.src_y);
        for (self.triangles) |elem| {
            elem.encode(writer);
        }
    }

};

