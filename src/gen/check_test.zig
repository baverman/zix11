// zig fmt: off
// This file is generated by `python -m genproto`

const std = @import("std");
const xproto = @import("xproto.zig");
const render = @import("render.zig");
const randr = @import("randr.zig");
const shape = @import("shape.zig");
const xfixes = @import("xfixes.zig");
const dpms = @import("dpms.zig");
const shm = @import("shm.zig");
const xinput = @import("xinput.zig");
const xkb = @import("xkb.zig");
const errors = @import("errors.zig");
const events = @import("events.zig");

test "analyze generated modules" {
    std.testing.refAllDecls(xproto);
    std.testing.refAllDecls(render);
    std.testing.refAllDecls(randr);
    std.testing.refAllDecls(shape);
    std.testing.refAllDecls(xfixes);
    std.testing.refAllDecls(dpms);
    std.testing.refAllDecls(shm);
    std.testing.refAllDecls(xinput);
    std.testing.refAllDecls(xkb);
    std.testing.refAllDecls(errors);
    std.testing.refAllDecls(events);
}
